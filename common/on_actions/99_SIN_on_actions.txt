on_actions = {
	# Keeps the Singaporean alignment scripted GUI's dirty counter in sync so its
	# political-power and timed-flag gated buttons refresh without per-frame evaluation.
	on_daily_SIN = {
		effect = {
			if = {
				limit = { is_ai = no }
				SIN_update_alignment_gui_dirty = yes
			}
		}
	}
}
