CUB_hate_america_while_embargoed = {
	allowed = { original_tag = CUB }
	enable = {
		has_opinion_modifier = CUB_USA_Embargo
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = antagonize id = "USA" value = 200 }
}

CUB_cancel_war_VEN = {
	allowed = { original_tag = CUB }
	enable = {
		has_war_with = VEN
		strength_ratio = { tag = VEN ratio < 1 }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = declare_war id = VEN value = -4000 }
}

CUB_cancel_war_HAI = {
	allowed = { original_tag = CUB }
	enable = {
		has_war_with = HAI
		strength_ratio = { tag = HAI ratio < 1 }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = declare_war id = HAI value = -4000 }
}

CUB_cancel_war_DOM = {
	allowed = { original_tag = CUB }
	enable = {
		has_war_with = DOM
		strength_ratio = { tag = DOM ratio < 1 }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = declare_war id = DOM value = -4000 }
}

CUB_cancel_war_JAM = {
	allowed = { original_tag = CUB }
	enable = {
		has_war_with = JAM
		strength_ratio = { tag = JAM ratio < 1 }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = declare_war id = JAM value = -4000 }
}

CUB_cancel_war_COL = {
	allowed = { original_tag = CUB }
	enable = {
		has_war_with = COL
		strength_ratio = { tag = COL ratio < 1 }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = declare_war id = COL value = -4000 }
}

CUB_cancel_war_PAN = {
	allowed = { original_tag = CUB }
	enable = {
		has_war_with = PAN
		strength_ratio = { tag = PAN ratio < 1 }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = declare_war id = PAN value = -4000 }
}

CUB_cancel_war_COS = {
	allowed = { original_tag = CUB }
	enable = {
		has_war_with = COS
		strength_ratio = { tag = COS ratio < 1 }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = declare_war id = COS value = -4000 }
}

CUB_cancel_war_NIC = {
	allowed = { original_tag = CUB }
	enable = {
		has_war_with = NIC
		strength_ratio = { tag = NIC ratio < 1 }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = declare_war id = NIC value = -4000 }
}
