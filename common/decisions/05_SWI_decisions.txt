UN_voting_category = {

	SWI_join_the_united_nations = {

		icon = GFX_decision_generic_form_nation

		fire_only_once = yes

		visible = { original_tag = SWI }

		available = { has_war = no }

		cost = 75

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision SWI_join_the_united_nations"
			add_dynamic_modifier = { modifier = united_nations_member }
			news_event = switzerland_news.1
			hidden_effect = {
				set_country_flag = un_member_flag
				if = {
					limit = { NOT = { is_in_array = { global.UN_general_assembly = THIS } } }
					add_to_array = { global.UN_general_assembly = THIS }
				}
				international_systems_update = yes
				if = {
					limit = { country_exists = USA }
					USA = { add_opinion_modifier = { target = ROOT modifier = SWI_join_un } }
				}
				if = {
					limit = { country_exists = ENG }
					ENG = { add_opinion_modifier = { target = ROOT modifier = SWI_join_un } }
				}
				if = {
					limit = { country_exists = SOV }
					SOV = { add_opinion_modifier = { target = ROOT modifier = SWI_join_un } }
				}
				if = {
					limit = { country_exists = FRA }
					FRA = { add_opinion_modifier = { target = ROOT modifier = SWI_join_un } }
				}
				if = {
					limit = { country_exists = CHI }
					CHI = { add_opinion_modifier = { target = ROOT modifier = SWI_join_un } }
				}
			}
		}

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				date < 2002.3.1
			}
		}
	}
}

SWI_pan_alpine_claims = {

	SWI_claim_french_alps = {

		icon = GFX_decision_annex

		fire_only_once = yes

		visible = { NOT = { has_country_flag = SWI_claimed_french_alps } }

		available = {
			country_exists = FRA
			FRA = { is_subject = no }
			NOT = { has_war_with = FRA }
		}

		cost = 50

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision SWI_claim_french_alps"
			set_country_flag = SWI_claimed_french_alps
			FRA = { country_event = { id = switzerland.8 days = 1 } }
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				transfer_state = 857
				transfer_state = 63
				transfer_state = 1000
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				FRA = { add_opinion_modifier = { target = ROOT modifier = insult } }
			}
		}

		ai_will_do = { base = 1 }
	}

	SWI_claim_italian_alps = {

		icon = GFX_decision_annex

		fire_only_once = yes

		visible = { NOT = { has_country_flag = SWI_claimed_italian_alps } }

		available = {
			country_exists = ITA
			ITA = { is_subject = no }
			NOT = { has_war_with = ITA }
		}

		cost = 50

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision SWI_claim_italian_alps"
			set_country_flag = SWI_claimed_italian_alps
			ITA = { country_event = { id = switzerland.11 days = 1 } }
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				transfer_state = 1059
				transfer_state = 77
				transfer_state = 80
				transfer_state = 285
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				ITA = { add_opinion_modifier = { target = ROOT modifier = insult } }
			}
		}

		ai_will_do = { base = 1 }
	}

	SWI_claim_austrian_alps = {

		icon = GFX_decision_annex

		fire_only_once = yes

		visible = { NOT = { has_country_flag = SWI_claimed_austrian_alps } }

		available = {
			country_exists = AUS
			AUS = { is_subject = no }
			NOT = { has_war_with = AUS }
		}

		cost = 50

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision SWI_claim_austrian_alps"
			set_country_flag = SWI_claimed_austrian_alps
			AUS = { country_event = { id = switzerland.14 days = 1 } }
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				transfer_state = 286
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				AUS = { add_opinion_modifier = { target = ROOT modifier = insult } }
			}
			if = {
				limit = {
					country_exists = LIC
					LIC = { is_subject = no }
					NOT = { has_war_with = LIC }
				}
				LIC = { country_event = { id = switzerland.23 days = 1 } }
				custom_effect_tooltip = TT_IF_THEY_ACCEPT
				effect_tooltip = {
					annex_country = { target = LIC transfer_troops = no }
				}
				custom_effect_tooltip = TT_IF_THEY_REJECT
				effect_tooltip = {
					LIC = { add_opinion_modifier = { target = ROOT modifier = insult } }
				}
			}
		}

		ai_will_do = { base = 1 }
	}
}

SWI_referendums = {

	SWI_referendum_eu_accession = {

		icon = GFX_decision_generic_political_discourse

		days_re_enable = 1095

		cost = 50

		available = {
			has_war = no
			NOT = { has_idea = EU_member }
			NOT = { has_country_flag = SWI_eu_referendum_passed }
		}

		visible = {
			NOT = { has_country_flag = SWI_eu_referendum_passed }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision SWI_referendum_eu_accession"
			random_list = {
				50 = {
					modifier = {
						add = 20
						has_completed_focus = SWI_move_closer_to_the_eu
					}
					modifier = {
						add = 15
						has_completed_focus = SWI_modernize_direct_democracy
					}
					modifier = {
						add = 10
						has_stability > 0.6
					}
					set_country_flag = SWI_eu_referendum_passed
					set_temp_variable = { modify_europeanism = 0.10 }
					EU_potential_europeanism_change = yes
					add_stability = 0.05
					country_event = { id = switzerland.31 hours = 6 }
				}
				50 = {
					modifier = {
						factor = 0.4
						has_completed_focus = SWI_move_closer_to_the_eu
					}
					add_political_power = -50
					add_stability = -0.03
					country_event = { id = switzerland.32 hours = 6 }
				}
			}
		}

		ai_will_do = { base = 0 }
	}

	SWI_referendum_army_initiative = {

		icon = GFX_decision_generic_political_discourse

		days_re_enable = 730

		cost = 40

		available = {
			has_war = no
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision SWI_referendum_army_initiative"
			random_list = {
				50 = {
					modifier = {
						add = 20
						has_war_support > 0.5
					}
					modifier = {
						add = 15
						has_completed_focus = SWI_modernize_direct_democracy
					}
					add_war_support = 0.10
					add_stability = 0.03
					country_event = { id = switzerland.33 hours = 6 }
				}
				50 = {
					modifier = {
						factor = 0.5
						has_war_support > 0.5
					}
					add_political_power = -40
					add_war_support = -0.05
					country_event = { id = switzerland.34 hours = 6 }
				}
			}
		}

		ai_will_do = { base = 0 }
	}
}
