decision_category_internal_factions = {
	entrench_small_medium_business_owners_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = small_medium_business_owners
		}

		available = {
			small_medium_business_owners_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { small_medium_business_owners_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_small_medium_business_owners_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { small_medium_business_owners_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_small_medium_business_owners_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_international_bankers_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = international_bankers
		}

		available = {
			international_bankers_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { international_bankers_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_international_bankers_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { international_bankers_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_international_bankers_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_fossil_fuel_industry_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = fossil_fuel_industry
		}

		available = {
			fossil_fuel_industry_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { fossil_fuel_industry_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_fossil_fuel_industry_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { fossil_fuel_industry_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_fossil_fuel_industry_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_industrial_conglomerates_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = industrial_conglomerates
		}

		available = {
			industrial_conglomerates_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { industrial_conglomerates_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_industrial_conglomerates_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { industrial_conglomerates_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_industrial_conglomerates_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_oligarchs_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = oligarchs
		}

		available = {
			oligarchs_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { oligarchs_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_oligarchs_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { oligarchs_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_oligarchs_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_maritime_industry_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = maritime_industry
		}

		available = {
			maritime_industry_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { maritime_industry_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_maritime_industry_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { maritime_industry_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_maritime_industry_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_defense_industry_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = defense_industry
		}

		available = {
			defense_industry_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { defense_industry_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_defense_industry_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { defense_industry_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_defense_industry_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_the_military_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = the_military
		}

		available = {
			military_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { the_military_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_the_military_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { the_military_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_the_military_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_intelligence_community_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = intelligence_community
		}

		available = {
			intelligence_community_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { intelligence_community_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_intelligence_community_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { intelligence_community_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_intelligence_community_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_labour_unions_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = labour_unions
		}

		available = {
			labour_unions_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { labour_unions_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_labour_unions_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { labour_unions_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_labour_unions_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_landowners_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = landowners
		}

		available = {
			landowners_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { landowners_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_landowners_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { landowners_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_landowners_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_farmers_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = farmers
		}

		available = {
			farmers_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { farmers_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_farmers_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { farmers_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_farmers_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_communist_cadres_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = communist_cadres
		}

		available = {
			communist_cadres_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { communist_cadres_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_communist_cadres_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { communist_cadres_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_communist_cadres_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_the_priesthood_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = the_priesthood
		}

		available = {
			the_priesthood_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { the_priesthood_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_the_priesthood_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { the_priesthood_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_the_priesthood_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_the_ulema_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = the_ulema
		}

		available = {
			the_ulema_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { the_ulema_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_the_ulema_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { the_ulema_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_the_ulema_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_the_clergy_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = the_clergy
		}

		available = {
			the_clergy_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { the_clergy_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_the_clergy_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { the_clergy_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_the_clergy_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_wahabi_ulema_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = wahabi_ulema
		}

		available = {
			wahabi_ulema_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { wahabi_ulema_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_wahabi_ulema_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { wahabi_ulema_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_the_wahabi_ulema_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_saudi_royal_family_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = saudi_royal_family
		}

		available = {
			saudi_royal_family_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { saudi_royal_family_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_saudi_royal_family_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { saudi_royal_family_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_saudi_royal_family_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_iranian_quds_force_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = iranian_quds_force
		}

		available = {
			iranian_quds_force_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { iranian_quds_force_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_iranian_quds_force_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { iranian_quds_force_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_iranian_quds_force_opinion = yes
		}

		ai_will_do = { base = 0 }
	}

	entrench_foreign_jihadis_patronage_decision = {
		icon = GFX_decision_bancru_ask_button
		fire_only_once = yes

		visible = {
			has_idea = foreign_jihadis
		}

		available = {
			foreign_jihadis_at_least_enthusiastic_opinion = yes
			custom_trigger_tooltip = {
				tooltip = internal_faction_entrench_headroom_tt
				check_variable = { foreign_jihadis_opinion_min < 71 }
			}
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}

		cost = 300

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision entrench_foreign_jihadis_patronage_decision"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			custom_effect_tooltip = internal_faction_entrench_floor_tt
			add_to_variable = { foreign_jihadis_opinion_floor = 10 }
			set_temp_variable = { temp_opinion = 5 }
			change_foreign_jihadis_opinion = yes
		}

		ai_will_do = { base = 0 }
	}
}
