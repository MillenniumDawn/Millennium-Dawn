post_balkan_war_categories = {
	
	Support_western_outlook = {

		visible = {
			OR = {
				original_tag = ENG
				original_tag = ITA
				original_tag = GER
				original_tag = HOL
				original_tag = IRE
				original_tag = SPR
				original_tag = ROM
				original_tag = SWE
				original_tag = USA
				original_tag = TUR
				original_tag = DEN
				original_tag = CAN
			}
		}

		target_trigger = {
 			FROM = {
				OR = {
					original_tag = ALB
					original_tag = CRO
					original_tag = FYR
					original_tag = BOS
					original_tag = KOS
					original_tag = SER
				}
 				has_government = democratic
 				NOT = { has_war_with = ROOT }
 			}
 		}

		cost = 100
		icon = political_actions

		days_re_enable = 150
		fire_only_once = no

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			FROM = {
				set_temp_variable = { balkan_modify_stability = 5 }
				balkan_modify_stability = yes
				set_temp_variable = { percent_change = 1.5 }
				set_temp_variable = { tag_index = ROOT }
				set_temp_variable = { influence_target = FROM }
				change_influence_percentage = yes
				add_timed_idea = {
					idea = democratic_influence
					days = 100
				}
			}
		}

		ai_will_do = {
			factor = 100
		}
	}
	Support_east_outlook = {

		visible = {
			OR = {
				has_idea = CSTO_member
				original_tag = SOV
			}
		}

		target_trigger = {
 			FROM = {
				OR = {
					original_tag = ALB
					original_tag = CRO
					original_tag = FYR
					original_tag = BOS
					original_tag = KOS
					original_tag = SER
				}
				OR = {
					has_government = communism
					has_government = nationalist
				}
 				NOT = { has_war_with = ROOT }
 			}
 		}

		cost = 100
		icon = political_actions

		days_re_enable = 150
		fire_only_once = no

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			FROM = { 
				set_temp_variable = { balkan_modify_stability = -5 }
				balkan_modify_stability = yes
				set_temp_variable = { percent_change = 1.5 }
				set_temp_variable = { tag_index = ROOT }
				set_temp_variable = { influence_target = FROM }
				change_influence_percentage = yes
				add_timed_idea = {
					idea = large_far_right_movement
					days = 100
				}
			}
		}

		ai_will_do = {
			factor = 100
		}
	}
	Support_claims_east = {

		visible = {
			OR = {
				has_idea = CSTO_member
				original_tag = SOV
			}
		}

		target_trigger = {
 			FROM = {
				OR = {
					original_tag = ALB
					original_tag = CRO
					original_tag = FYR
					original_tag = BOS
					original_tag = KOS
					original_tag = SER

				}
				OR = {
					has_government = communism
					has_government = nationalist
				}
 				NOT = { has_war_with = ROOT }
 			}
 		}

		cost = 100
		icon = political_actions

		days_re_enable = 150
		fire_only_once = no

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			FROM = {
				set_temp_variable = { balkan_modify_stability = -5 }
				balkan_modify_stability = yes
				set_temp_variable = { percent_change = 1.5 }
				set_temp_variable = { tag_index = ROOT }
				set_temp_variable = { influence_target = FROM }
				change_influence_percentage = yes
			}
			
			ROOT = {
				send_equipment = {
 					target = FROM
 					type = mbt_hull_1
 					amount = 42
 				}
				send_equipment = {
 					target = FROM
 					type = APC_hull_1
 					amount = 42
 				}
			}
		}

		ai_will_do = {
			factor = 100
		}
	}
	Send_peacekeeper = {

		visible = {
			OR = {
				original_tag = ENG
				original_tag = ITA
				original_tag = GER
				original_tag = HOL
				original_tag = IRE
				original_tag = SPR
				original_tag = ROM
				original_tag = SWE
				original_tag = USA
				original_tag = TUR
				original_tag = DEN
				original_tag = CAN
			}
		}

		target_trigger = {
 			FROM = {
				OR = {
					original_tag = ALB
					original_tag = CRO
					original_tag = FYR
					original_tag = BOS
					original_tag = KOS
					original_tag = SER
				}
 				has_government = democratic
 				NOT = { has_war_with = ROOT }
 			}
 		}

		cost = 100
		icon = political_actions

		days_re_enable = 150
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			FROM = {
				set_temp_variable = { balkan_modify_stability = 5 }
				balkan_modify_stability = yes
				set_temp_variable = { percent_change = 1.5 }
				set_temp_variable = { tag_index = ROOT }
				set_temp_variable = { influence_target = FROM }
				change_influence_percentage = yes
			}
 			
 			ROOT = {
 				give_guarantee = FROM
 			}		
		}

		ai_will_do = {
			factor = 100
		}
	}
}