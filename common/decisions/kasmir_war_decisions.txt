RAJ_PAK_border_conflict_category = {

	# --- TRAINING ---

	prepare_army_training = {
		desc = prepare_army_training_desc
		available = {
			has_war = no
			has_equipment = { infantry_weapons_type > 30 }
			has_equipment = { cnc_equipment_type > 7 }
		}
		days_remove = 15
		days_re_enable = 20
		fixed_random_seed = no
		custom_cost_text = command_power_more_than_20
		custom_cost_trigger = { command_power > 20 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_army_training"
			add_command_power = -20
			custom_effect_tooltip = border_war_increase_readiness_TT
			add_equipment_to_stockpile = { type = infantry_weapons_type amount = -30 }
			add_equipment_to_stockpile = { type = cnc_equipment_type amount = -7 }
			hidden_effect = {
				set_temp_variable = { border_war_add_readiness_amount = 12 }
				border_war_add_readiness = yes
				random = {
					chance = 10
					country_event = { id = border_war_training_failure.1 days = 1 }
				}
			}
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 check_variable = { border_readiness > 80 } }
			modifier = { factor = 0 NOT = { border_war_ai_should_prepare = yes } }
		}
	}

	prepare_mech_army_training = {
		desc = prepare_mech_army_training_desc
		available = {
			has_war = no
			has_equipment = { medium_tank_amphibious_chassis > 5 }
			has_equipment = { medium_tank_chassis > 3 }
		}
		days_remove = 15
		days_re_enable = 20
		fixed_random_seed = no
		custom_cost_text = command_power_more_than_20
		custom_cost_trigger = { command_power > 20 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_mech_army_training"
			add_command_power = -20
			custom_effect_tooltip = border_war_increase_readiness_TT
			add_equipment_to_stockpile = { type = medium_tank_amphibious_chassis amount = -5 }
			add_equipment_to_stockpile = { type = medium_tank_chassis amount = -3 }
			hidden_effect = {
				set_temp_variable = { border_war_add_readiness_amount = 20 }
				border_war_add_readiness = yes
				random = {
					chance = 10
					country_event = { id = border_war_training_failure.2 days = 1 }
				}
			}
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 check_variable = { border_readiness > 80 } }
			modifier = { factor = 0 NOT = { border_war_ai_should_prepare = yes } }
		}
	}

	prepare_arty_army_training = {
		desc = prepare_arty_army_training_desc
		available = {
			has_war = no
			has_equipment = { artillery_equipment > 5 }
		}
		days_remove = 15
		days_re_enable = 20
		fixed_random_seed = no
		custom_cost_text = command_power_more_than_20
		custom_cost_trigger = { command_power > 20 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_arty_army_training"
			add_command_power = -20
			custom_effect_tooltip = border_war_increase_readiness_TT
			add_equipment_to_stockpile = { type = artillery_equipment amount = -5 }
			hidden_effect = {
				set_temp_variable = { border_war_add_readiness_amount = 10 }
				border_war_add_readiness = yes
			}
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 check_variable = { border_readiness > 80 } }
			modifier = { factor = 0 NOT = { border_war_ai_should_prepare = yes } }
		}
	}

	# --- TENSION ---

	prepare_attack = {
		desc = prepare_attack_desc
		available = {
			has_war = no
			NOT = { has_decision = prepare_shelling }
		}
		days_remove = 15
		days_re_enable = 20
		fixed_random_seed = no
		custom_cost_text = command_power_more_than_20
		custom_cost_trigger = { command_power > 20 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_attack"
			add_command_power = -20
			custom_effect_tooltip = border_war_increase_tension_TT
			hidden_effect = {
				set_temp_variable = { border_war_add_tension_amount = 18 }
				border_war_add_tension = yes
				random = {
					chance = 10
					country_event = { id = border_war_tension_failure.1 days = 1 }
				}
			}
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 check_variable = { global.border_tension > 80 } }
			modifier = { factor = 0 NOT = { border_war_ai_should_prepare = yes } }
		}
	}

	prepare_shelling = {
		desc = prepare_shelling_desc
		available = {
			has_war = no
			NOT = { has_decision = prepare_attack }
		}
		days_remove = 15
		days_re_enable = 20
		fixed_random_seed = no
		custom_cost_text = command_power_more_than_20
		custom_cost_trigger = { command_power > 20 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_shelling"
			add_command_power = -20
			custom_effect_tooltip = border_war_increase_tension_TT
			hidden_effect = {
				set_temp_variable = { border_war_add_tension_amount = 18 }
				border_war_add_tension = yes
				random = {
					chance = 10
					country_event = { id = border_war_tension_failure.1 days = 1 }
				}
			}
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 check_variable = { global.border_tension > 80 } }
			modifier = { factor = 0 NOT = { border_war_ai_should_prepare = yes } }
		}
	}

	# --- NEGOTIATE (de-escalation, content expansion) ---

	border_negotiate_pak_raj = {
		desc = border_negotiate_pak_raj_desc
		icon = GFX_decision_generic_political_discourse
		visible = {
			is_border_war_eligible = yes
			check_variable = { global.border_tension > 19 }
		}
		available = {
			has_war = no
			custom_trigger_tooltip = {
				tooltip = border_war_negotiate_opinion_TT
				RAJ = { has_opinion = { target = PAK value > 24 } }
				PAK = { has_opinion = { target = RAJ value > 24 } }
			}
		}
		cost = 75
		fire_only_once = no
		days_re_enable = 90

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision border_negotiate_pak_raj"
			custom_effect_tooltip = border_war_negotiate_tension_TT
			hidden_effect = {
				subtract_from_variable = { global.border_tension = 25 }
				clamp_variable = { var = global.border_tension min = 0 max = 100 }
			}
		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 5
				has_war = no
				has_stability < 0.50
			}
		}
	}

	# --- ATTACKS ---

	attack_427_from_420 = {
		desc = attack_427_from_420_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 427 } }

		visible = {
			controls_state = 420
			NOT = { controls_state = 427 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_427_from_420"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_427_from_420"
			if = {
				limit = {
					420 = { has_border_war = yes }
					427 = { has_border_war = yes }
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 420
					NOT = { controls_state = 427 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 420
						num_provinces = 3
						on_win = border_war_outcome.win
						on_lose = border_war_outcome.lose
						on_cancel = border_war_outcome.cancel
					}
					defender = {
						state = 427
						num_provinces = 3
						on_win = border_war_outcome.lose
						on_lose = border_war_outcome.win
						on_cancel = border_war_outcome.cancel
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 1
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	attack_420_from_427 = {
		desc = attack_420_from_427_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 420 } }

		visible = {
			controls_state = 427
			NOT = { controls_state = 420 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_420_from_427"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_420_from_427"
			if = {
				limit = {
					427 = { has_border_war = yes }
					420 = { has_border_war = yes }
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 427
					NOT = { controls_state = 420 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 427
						num_provinces = 3
						on_win = border_war_outcome.win
						on_lose = border_war_outcome.lose
						on_cancel = border_war_outcome.cancel
					}
					defender = {
						state = 420
						num_provinces = 3
						on_win = border_war_outcome.lose
						on_lose = border_war_outcome.win
						on_cancel = border_war_outcome.cancel
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 1
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	attack_429_from_563 = {
		desc = attack_429_from_563_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 429 } }

		visible = {
			controls_state = 563
			NOT = { controls_state = 429 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_429_from_563"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_429_from_563"
			if = {
				limit = {
					563 = { has_border_war = yes }
					429 = { has_border_war = yes }
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 563
					NOT = { controls_state = 429 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 563
						num_provinces = 3
						on_win = border_war_outcome.win
						on_lose = border_war_outcome.lose
						on_cancel = border_war_outcome.cancel
					}
					defender = {
						state = 429
						num_provinces = 3
						on_win = border_war_outcome.lose
						on_lose = border_war_outcome.win
						on_cancel = border_war_outcome.cancel
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 1
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	attack_563_from_429 = {
		desc = attack_563_from_429_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 563 } }

		visible = {
			controls_state = 429
			NOT = { controls_state = 563 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_563_from_429"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_563_from_429"
			if = {
				limit = {
					429 = { has_border_war = yes }
					563 = { has_border_war = yes }
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 429
					NOT = { controls_state = 563 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 429
						num_provinces = 3
						on_win = border_war_outcome.win
						on_lose = border_war_outcome.lose
						on_cancel = border_war_outcome.cancel
					}
					defender = {
						state = 563
						num_provinces = 3
						on_win = border_war_outcome.lose
						on_lose = border_war_outcome.win
						on_cancel = border_war_outcome.cancel
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 1
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	attack_573_from_984 = {
		desc = attack_573_from_984_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 573 } }

		visible = {
			controls_state = 984
			NOT = { controls_state = 573 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_573_from_984"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_573_from_984"
			if = {
				limit = {
					984 = { has_border_war = yes }
					573 = { has_border_war = yes }
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 984
					NOT = { controls_state = 573 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 984
						num_provinces = 3
						on_win = border_war_outcome.win
						on_lose = border_war_outcome.lose
						on_cancel = border_war_outcome.cancel
					}
					defender = {
						state = 573
						num_provinces = 3
						on_win = border_war_outcome.lose
						on_lose = border_war_outcome.win
						on_cancel = border_war_outcome.cancel
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 1
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	attack_984_from_573 = {
		desc = attack_984_from_573_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 984 } }

		visible = {
			controls_state = 573
			NOT = { controls_state = 984 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_984_from_573"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_984_from_573"
			if = {
				limit = {
					573 = { has_border_war = yes }
					984 = { has_border_war = yes }
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 573
					NOT = { controls_state = 984 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 573
						num_provinces = 3
						on_win = border_war_outcome.win
						on_lose = border_war_outcome.lose
						on_cancel = border_war_outcome.cancel
					}
					defender = {
						state = 984
						num_provinces = 3
						on_win = border_war_outcome.lose
						on_lose = border_war_outcome.win
						on_cancel = border_war_outcome.cancel
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 1
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	attack_426_from_984 = {
		desc = attack_426_from_984_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 426 } }

		visible = {
			controls_state = 984
			NOT = { controls_state = 426 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_426_from_984"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_426_from_984"
			if = {
				limit = {
					984 = { has_border_war = yes }
					426 = { has_border_war = yes }
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 984
					NOT = { controls_state = 426 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 984
						num_provinces = 3
						on_win = border_war_outcome.win
						on_lose = border_war_outcome.lose
						on_cancel = border_war_outcome.cancel
					}
					defender = {
						state = 426
						num_provinces = 3
						on_win = border_war_outcome.lose
						on_lose = border_war_outcome.win
						on_cancel = border_war_outcome.cancel
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 1
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	attack_984_from_426 = {
		desc = attack_984_from_426_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 984 } }

		visible = {
			controls_state = 426
			NOT = { controls_state = 984 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_984_from_426"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_984_from_426"
			if = {
				limit = {
					426 = { has_border_war = yes }
					984 = { has_border_war = yes }
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 426
					NOT = { controls_state = 984 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 426
						num_provinces = 3
						on_win = border_war_outcome.win
						on_lose = border_war_outcome.lose
						on_cancel = border_war_outcome.cancel
					}
					defender = {
						state = 984
						num_provinces = 3
						on_win = border_war_outcome.lose
						on_lose = border_war_outcome.win
						on_cancel = border_war_outcome.cancel
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 1
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	attack_426_from_987 = {
		desc = attack_426_from_987_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 426 } }

		visible = {
			controls_state = 987
			NOT = { controls_state = 426 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_426_from_987"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_426_from_987"
			if = {
				limit = {
					987 = { has_border_war = yes }
					426 = { has_border_war = yes }
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 987
					NOT = { controls_state = 426 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 987
						num_provinces = 3
						on_win = border_war_outcome.win
						on_lose = border_war_outcome.lose
						on_cancel = border_war_outcome.cancel
					}
					defender = {
						state = 426
						num_provinces = 3
						on_win = border_war_outcome.lose
						on_lose = border_war_outcome.win
						on_cancel = border_war_outcome.cancel
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 1
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	attack_987_from_426 = {
		desc = attack_987_from_426_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 987 } }

		visible = {
			controls_state = 426
			NOT = { controls_state = 987 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_987_from_426"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_987_from_426"
			if = {
				limit = {
					426 = { has_border_war = yes }
					987 = { has_border_war = yes }
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 426
					NOT = { controls_state = 987 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 426
						num_provinces = 3
						on_win = border_war_outcome.win
						on_lose = border_war_outcome.lose
						on_cancel = border_war_outcome.cancel
					}
					defender = {
						state = 987
						num_provinces = 3
						on_win = border_war_outcome.lose
						on_lose = border_war_outcome.win
						on_cancel = border_war_outcome.cancel
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 1
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	# --- GRABS ---

	grab_420 = {
		desc = grab_420_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 420 } }

		visible = { NOT = { controls_state = 420 } }

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_420"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_420"
			set_temp_variable = { border_war_grab_state = 420 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	grab_427 = {
		desc = grab_427_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 427 } }

		visible = { NOT = { controls_state = 427 } }

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_427"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_427"
			set_temp_variable = { border_war_grab_state = 427 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	grab_429 = {
		desc = grab_429_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 429 } }

		visible = { NOT = { controls_state = 429 } }

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_429"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_429"
			set_temp_variable = { border_war_grab_state = 429 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	grab_563 = {
		desc = grab_563_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 563 } }

		visible = { NOT = { controls_state = 563 } }

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_563"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_563"
			set_temp_variable = { border_war_grab_state = 563 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	grab_573 = {
		desc = grab_573_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 573 } }

		visible = { NOT = { controls_state = 573 } }

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_573"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_573"
			set_temp_variable = { border_war_grab_state = 573 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	grab_426 = {
		desc = grab_426_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 426 } }

		visible = { NOT = { controls_state = 426 } }

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_426"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_426"
			set_temp_variable = { border_war_grab_state = 426 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	grab_987 = {
		desc = grab_987_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 987 } }

		visible = { NOT = { controls_state = 987 } }

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_987"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_987"
			set_temp_variable = { border_war_grab_state = 987 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	grab_984 = {
		desc = grab_984_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 984 } }

		visible = { NOT = { controls_state = 984 } }

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_984"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_984"
			set_temp_variable = { border_war_grab_state = 984 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
		}
	}

	# --- EXTORT XP ---

	extort_xp = {
		desc = extort_xp_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		available = {
			has_country_flag = border_war_recently_active
			custom_trigger_tooltip = {
				tooltip = border_war_progress_50_TT
				check_variable = { border_war_progress > 49 }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision extort_xp"
			set_temp_variable = { border_war_subtract_progress_amount = 50 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove decision extort_xp"
			army_experience = 30
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { has_country_flag = border_war_recently_active } }
		}
	}
}
