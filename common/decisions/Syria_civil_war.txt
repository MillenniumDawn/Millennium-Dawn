####FREE SYRIAN ARMY####
FSA_vaps_revolt_category = {

	FSA_defection_to_extremist = {

		allowed = { tag = FSA }

		icon = GFX_decision_revolt

		available = {
			hidden_trigger = { always = no }
		}

		days_mission_timeout = FSA_days_var
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_completed_focus = NULL
					has_completed_focus = NULL
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		activation = {
			has_completed_focus = Need_a_start_trigger
			NOT = {
				has_government = fascism
				has_country_flag = fought_extremist_flag
			}
		}

		visible = {
			NOT = {
				has_government = fascism
				has_country_flag = fought_extremist_flag
			}
		}

		cancel_effect = {

		}

		timeout_effect = {
			#FSA_vaps_down_effect = yes
			activate_mission = FSA_defection_to_extremist
		}

		ai_will_do = {
			factor = 200
		}
	}

	FSA_capture_damascus = {

		allowed = { tag = FSA }

		icon = GFX_decision_oppression

		available = {
			OR = {
				has_country_flag = FSA_capture_damascus
			}
		}

		days_mission_timeout = FSA_capture_damascus_var
		is_good = yes
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		activation = {
			tag = FSA
			has_completed_focus = Need_a_start_trigger
			NOT = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		visible = {
			tag = FSA
			NOT = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		cancel_effect = {

		}

		timeout_effect = {
			set_country_flag = fought_extremist_flag
			if = {
				limit = { #Fallback in case the AI really sucks at handling this
					is_ai = yes
					OR = {
						AND = {
							is_historical_focus_on = yes
							has_game_rule = {
								rule = FSA_ai_behavior
								option = DEFAULT
							}
						}
						has_country_flag = FSA_AI_RANDOM_HISTORICAL
						has_game_rule = {
							rule = FSA_ai_behavior
							option = HISTORICAL
						}
					}
				}
				#Empty, we just want to remove the Vaps NS as we do below
			}
			else_if = {
				limit = {
					has_idea = FSA_vaps_idea_4
				}
				start_civil_war = {
					ideology = fascism
					size = 0.6
				}
			}
			else_if = {
				limit = {
					has_idea = FSA_vaps_idea_3
				}
				start_civil_war = {
					ideology = fascism
					size = 0.35
				}
			}
			else_if = {
				limit = {
					has_idea = FSA_vaps_idea_2
				}
				add_popularity = {
					ideology = fascism
					popularity = -0.1
				}
				add_stability = -0.1
			}
			else = {
				add_popularity = {
					ideology = fascism
					popularity = -0.2
				}
			}
			remove_vaps_effect = yes
		}

		ai_will_do = {
			factor = 200
		}
	}
}