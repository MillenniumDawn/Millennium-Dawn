USA_congress_chart = {
	show_law_change_decisions = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_laws_flag }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_laws_flag }
		}
		fire_only_once = no
		complete_effect = {
			set_country_flag = show_laws_flag
		}
	}

	hide_law_change_decisions = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			clr_country_flag = show_laws_flag
		}
	}

	# Bureaucracy Bills
	show_bureaucracy_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_bureau_bills_flag }
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_bureau_bills_flag }
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			set_country_flag = show_bureau_bills_flag
		}
	}

	hide_bureaucracy_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_bureau_bills_flag
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_bureau_bills_flag
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			clr_country_flag = show_bureau_bills_flag
		}
	}

	increase_bureaucracy_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_bureau_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = bureau_05 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_bureau_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = bureau_05 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = bureau_01 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = bureau_02 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = bureau_03 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = increase_bureau_spending_flag
		}
	}

	decrease_bureaucracy_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_bureau_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = bureau_01 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_bureau_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = bureau_01 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = bureau_05 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = bureau_04 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = bureau_03 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = decrease_bureau_spending_flag
		}
	}

	# Crime Fighting Bills
	show_police_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_police_bills_flag }
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_police_bills_flag }
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			set_country_flag = show_police_bills_flag
		}
	}

	hide_police_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_police_bills_flag
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_police_bills_flag
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			clr_country_flag = show_police_bills_flag
		}
	}

	increase_police_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_police_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = police_05 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_police_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = police_05 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = police_01 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = police_02 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = police_03 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = increase_police_spending_flag
		}
	}

	decrease_police_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_police_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = police_01 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_police_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = police_01 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = police_05 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = police_04 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = police_03 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = decrease_police_spending_flag
		}
	}


	# Education Bills
	show_education_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_education_bills_flag }
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_education_bills_flag }
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			set_country_flag = show_education_bills_flag
		}
	}

	hide_education_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_education_bills_flag
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_education_bills_flag
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			clr_country_flag = show_education_bills_flag
		}
	}

	increase_education_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_education_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = edu_05 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_education_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = edu_05 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = edu_01 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = edu_02 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = edu_03 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = increase_education_spending_flag
		}
	}

	decrease_education_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_education_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = edu_01 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_education_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = edu_01 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = edu_05 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = edu_04 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = {has_idea = edu_03 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = decrease_education_spending_flag
		}
	}

	# Healthcare Bills
	show_health_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_health_bills_flag }
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_health_bills_flag }
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			set_country_flag = show_health_bills_flag
		}
	}

	hide_health_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_health_bills_flag
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_health_bills_flag
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			clr_country_flag = show_health_bills_flag
		}
	}


	increase_health_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_health_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = health_06 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_health_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = health_06 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = health_01 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = health_02 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = health_03 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
			}
			else_if = {
				limit = { has_idea = health_04 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = increase_health_spending_flag
		}
	}

	decrease_health_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_health_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = health_01 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_health_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = health_01 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = health_06 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = health_05 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = health_04 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
			}
			else_if = {
				limit = { has_idea = health_03 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = decrease_health_spending_flag
		}
	}

	# Welfare Bills
	show_social_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_social_bills_flag }
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_social_bills_flag }
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			set_country_flag = show_social_bills_flag
		}
	}

	hide_social_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_social_bills_flag
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_social_bills_flag
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			clr_country_flag = show_social_bills_flag
		}
	}

	increase_social_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_social_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = social_06 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_social_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = social_06 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = social_01 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = social_02 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = social_03 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
			}
			else_if = {
				limit = { has_idea = social_04 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = increase_social_spending_flag
		}
	}

	decrease_social_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_social_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = social_01 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_social_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = social_01 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = social_06 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = social_05 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = social_04 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
			}
			else_if = {
				limit = { has_idea = social_03 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = decrease_social_spending_flag
		}
	}

	# Military Spending
	show_military_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_military_bills_flag }
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_military_bills_flag }
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			set_country_flag = show_military_bills_flag
		}
	}

	hide_military_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_military_bills_flag
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_military_bills_flag
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			clr_country_flag = show_military_bills_flag
		}
	}


	increase_military_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_military_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = defence_09 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_military_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = defence_09 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = defence_00 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = defence_01 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = defence_02 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = defence_03 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = defence_04 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
			}
			else_if = {
				limit = { has_idea = defence_05 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
			}
			else_if = {
				limit = { has_idea = defence_06 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = defence_07 }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = increase_military_spending_flag
		}
	}

	decrease_military_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_military_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = defence_00 }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_military_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = defence_00 }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = defence_09 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = defence_08 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = defence_07 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = defence_06 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = defence_05 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
			}
			else_if = {
				limit = { has_idea = defence_04 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
			}
			else_if = {
				limit = { has_idea = defence_03 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = defence_02 }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = decrease_military_spending_flag
		}
	}

	# Conscription
	show_conscription_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_conscription_bills_flag }
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_conscription_bills_flag }
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			set_country_flag = show_conscription_bills_flag
		}
	}

	hide_conscription_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_conscription_bills_flag
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_conscription_bills_flag
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			clr_country_flag = show_conscription_bills_flag
		}
	}

	increase_conscription_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_conscription_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = draft_army }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_conscription_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = draft_army }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = no_military }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = volunteer_army }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = increase_conscription_spending_flag
		}
	}

	decrease_conscription_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_conscription_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = no_military }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_conscription_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = no_military }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = draft_army }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else_if = {
				limit = { has_idea = partial_draft_army }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = decrease_conscription_spending_flag
		}
	}

	# Women in Military
	show_milwomen_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_milwomen_bills_flag }
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_milwomen_bills_flag }
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			set_country_flag = show_milwomen_bills_flag
		}
	}

	hide_milwomen_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_milwomen_bills_flag
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_milwomen_bills_flag
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			clr_country_flag = show_milwomen_bills_flag
		}
	}

	increase_milwomen_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_milwomen_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = drafted_women }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_milwomen_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = drafted_women }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = volunteer_women }
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				add_to_variable = { representative_bill_support = hor_demprog_seats }
				add_to_variable = { representative_bill_support = hor_demest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_demsoc_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = increase_milwomen_spending_flag
		}
	}

	decrease_milwomen_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_milwomen_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = no_women_in_military }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_milwomen_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = no_women_in_military }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = drafted_women }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			else = {
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_repconst_seats }
				clamp_temp_variable = { var = temp_factor max = hor_repconst_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = decrease_milwomen_spending_flag
		}
	}

	# Intervention
	show_intervention_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_intervention_bills_flag }
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			NOT = { has_country_flag = show_intervention_bills_flag }
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			set_country_flag = show_intervention_bills_flag
		}
	}

	hide_intervention_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_intervention_bills_flag
			has_country_flag = show_laws_flag
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_intervention_bills_flag
			has_country_flag = show_laws_flag
		}
		fire_only_once = no
		complete_effect = {
			clr_country_flag = show_intervention_bills_flag
		}
	}

	increase_conscription_bills = {
		available = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_conscription_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = draft_army }
		}
		visible = {
			NOT = { has_country_flag = bill_on_house_floor_flag }
			has_country_flag = show_conscription_bills_flag
			has_country_flag = show_laws_flag
			NOT = { has_idea = draft_army }
		}
		fire_only_once = no
		complete_effect = {
			set_variable = { representative_bill_support = 0 }
			if = {
				limit = { has_idea = no_military }
				add_to_variable = { representative_bill_support = hor_reprwp_seats }
				add_to_variable = { representative_bill_support = hor_repconst_seats }
				add_to_variable = { representative_bill_support = hor_repest_seats }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demprog_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demprog_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
				set_temp_variable = { temp_factor = random }
				multiply_temp_variable = { temp_factor = hor_demest_seats }
				clamp_temp_variable = { var = temp_factor max = hor_demest_seats min = 0 }
				add_to_variable = { representative_bill_support = temp_factor }
			}
			multiply_variable = { representative_bill_support = representatives_support }
			divide_variable = { representative_bill_support = 100 }
			round_variable = representative_bill_support
			set_country_flag = bill_on_house_floor_flag
			set_country_flag = increase_conscription_spending_flag
		}
	}

	# Upto 25  more Reps support the Bill
	ask_for_small_house_bill_support = {
		available = {
			has_country_flag = bill_on_house_floor_flag
		}
		visible = {
			has_country_flag = bill_on_house_floor_flag
		}
		fire_only_once = no
		cost = 50
		complete_effect = {
			custom_effect_tooltip = mtg_usa_house_small_support_tt
			set_temp_variable = { temp_factor = random }
			multiply_temp_variable = { temp_factor = 25 }
			clamp_temp_variable = { var = temp_factor max = 25 min = 10 }
			add_to_variable = { representative_bill_support = temp_factor }
			round_variable = representative_bill_support
			clamp_variable = { var = representative_bill_support max = representatives_total min = 0 }
		}
	}

	# Upto 50 more Reps support the bill
	ask_for_medium_house_support = {
		available = {
			has_country_flag = bill_on_house_floor_flag
		}
		visible = {
			has_country_flag = bill_on_house_floor_flag
		}
		fire_only_once = no
		cost = 100
		complete_effect = {
			custom_effect_tooltip = mtg_usa_house_medium_support_tt
			set_temp_variable = { temp_factor = random }
			multiply_temp_variable = { temp_factor = 50 }
			clamp_temp_variable = { var = temp_factor max = 50 min = 20 }
			add_to_variable = { representative_bill_support = temp_factor }
			round_variable = representative_bill_support
			clamp_variable = { var = representative_bill_support max = representatives_total min = 0 }
		}
	}

	# Upto 125 more Reps support the bill
	ask_for_large_house_bill_support = {
		available = {
			has_country_flag = bill_on_house_floor_flag
		}
		visible = {
			has_country_flag = bill_on_house_floor_flag
		}
		fire_only_once = no
		cost = 250
		complete_effect = {
			custom_effect_tooltip = mtg_usa_house_large_support_tt
			set_temp_variable = { temp_factor = random }
			multiply_temp_variable = { temp_factor = 125 }
			clamp_temp_variable = { var = temp_factor max = 125 min = 80 }
			add_to_variable = { representatives_support = temp_factor }
			round_variable = representatives_support
			clamp_variable = { var = representatives_support max = representatives_total min = 0 }
		}
	}

	#House starts voting on the bill
	house_debating_bill = {
		available = {
			check_variable = { representative_bill_support > 217 }
		}
		activation = {
			has_country_flag = bill_on_house_floor_flag
		}
		is_good = yes
		selectable_mission = no
		days_mission_timeout = 100
		timeout_effect = {
			USA_house_large_opposition = yes
			clr_country_flag = bill_on_house_floor_flag
			set_variable = { representative_bill_support = 0 }
			clr_country_flag = increase_bureau_spending_flag
			clr_country_flag = decrease_bureau_spending_flag
			clr_country_flag = increase_police_spending_flag
			clr_country_flag = decrease_police_spending_flag
			clr_country_flag = increase_education_spending_flag
			clr_country_flag = decrease_education_spending_flag
			clr_country_flag = increase_health_spending_flag
			clr_country_flag = decrease_health_spending_flag
			clr_country_flag = increase_social_spending_flag
			clr_country_flag = decrease_social_spending_flag
			clr_country_flag = increase_military_spending_flag
			clr_country_flag = decrease_military_spending_flag
			clr_country_flag = increase_conscription_spending_flag
			clr_country_flag = decrease_conscription_spending_flag
		}
		complete_effect = {
			USA_house_small_opposition = yes
			clr_country_flag = bill_on_house_floor_flag
			set_country_flag = bill_on_senate_floor_flag
		}
	}
}