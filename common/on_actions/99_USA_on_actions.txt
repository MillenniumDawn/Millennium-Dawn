on_actions = {
	on_monthly_USA = {
		effect = {
			# No need to constanty check values
			update_us_subideologies = yes
			# Determines who the swing states are first so the AI knows which states are a priority
			determine_swing_states = yes
			random_electoral_college_drift = yes
			# No need to do unnessary calculations if no one is going to see it
			if = {
				limit = { is_ai = no }
				update_electral_college_alignments = yes
			}
		}
	}
}