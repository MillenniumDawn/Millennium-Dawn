# Compute and electronics manufacturers.
# They build cnc_equipment_type, so their research categories stay on the
# command-and-control, computing and AI branches the equipment actually uses.

JAP_sony_corporation = {
	allowed = { original_tag = JAP }
	name = JAP_sony_corporation
	icon = GFX_idea_microchip_spirit

	task_capacity = 4

	equipment_type = { cnc_equipment_type }

	research_categories = {
		CAT_computing_tech
		CAT_cnc
	}

	initial_trait = {
		name = JAP_sony_corporation_trait

		equipment_bonus = { reliability = 0.04 }
		organization_modifier = { military_industrial_organization_research_bonus = 0.05 }
	}

	trait = {
		token = JAP_sony_corporation_cmos_sensors
		name = JAP_sony_corporation_cmos_sensors
		icon = GFX_generic_mio_trait_icon_cnc

		position = { x = 2 y = 0 }

		equipment_bonus = { reliability = 0.04 }

		on_complete = { expenditure_for_mio_upgrade = yes }

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	trait = {
		token = JAP_sony_corporation_optical_systems
		name = JAP_sony_corporation_optical_systems
		icon = GFX_generic_mio_trait_icon_cnc_defense

		position = { x = 5 y = 0 }

		equipment_bonus = { max_organisation = 0.03 }

		on_complete = { expenditure_for_mio_upgrade = yes }

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	trait = {
		token = JAP_sony_corporation_consumer_scale
		name = JAP_sony_corporation_consumer_scale
		icon = GFX_generic_mio_trait_icon_cnc_buildcost

		position = { x = 2 y = 1 }
		all_parents = { JAP_sony_corporation_cmos_sensors }

		equipment_bonus = { build_cost_ic = -0.05 }

		on_complete = { expenditure_for_mio_upgrade = yes }

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}

	trait = {
		token = JAP_sony_corporation_cell_architecture
		name = JAP_sony_corporation_cell_architecture
		icon = GFX_generic_mio_trait_icon_cnc_speed

		position = { x = 5 y = 1 }
		all_parents = { JAP_sony_corporation_optical_systems }

		equipment_bonus = { max_organisation = 0.03 }

		on_complete = { expenditure_for_mio_upgrade = yes }

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				check_variable = { FROM.interest_rate > 8 }
			}
		}
	}
}
