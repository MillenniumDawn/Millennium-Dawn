bankruptcy_decisions = {

	bankruptcy_cheap_loans_from_imf = {
	
		available = {
			is_full_state = yes
			has_added_tension_amount < 5
			NOT = {
				has_idea = paralyzing_corruption
				has_idea = crippling_corruption
				has_idea = rampant_corruption
			}
			custom_trigger_tooltip = {
				tooltip = has_higher_gdp_per_capita_than_5
				check_variable = { gdp_per_capita > 5 }
			}
			custom_trigger_tooltip = {
				tooltip = interest_rate_lower_than_15
				check_variable = { interest_rate < 15 }
			}
		}
		
		cost = 100
		
		days_remove = 360
		
		days_re_enable = 180
		
		complete_effect = {
			set_temp_variable = { debt_addition = debt }
			multiply_temp_variable = { debt_addition = 0.05 }
			round_temp_variable = debt_addition
			add_to_variable = { debt = debt_addition }
			custom_effect_tooltip = cheap_loans_from_imf_tt
			ingame_update_setup = yes
		}
	
	}
	
	bankruptcy_seek_bailout_from_biggest_influencer = {
	
		available = {
			custom_trigger_tooltip = {
				tooltip = "has_foreign_influence"
				check_variable = { influence_array^0 > 0 }
			}
			custom_trigger_tooltip = {
				tooltip = interest_rate_higher_than_10
				check_variable = { interest_rate > 10 }
			}
		}
		
		days_re_enable = 360
		
		cost = 100
		
		complete_effect = {
			set_temp_variable = { debt_bailout_temp = debt }
			multiply_temp_variable = { debt_bailout_temp = 0.20 }
			set_variable = { debt_bailout = debt_bailout_temp }
			round_variable = debt_bailout
			custom_effect_tooltip = bankruptcy_seek_bailout_from_biggest_influencer_tt
			var:FROM.influence_array^0 = { country_event = bankruptcy.1 days = 2 }
		}	
	
	}
	
	bankruptcy_seek_bailout_from_second_biggest_influencer = {
	
		available = {
			custom_trigger_tooltip = {
				tooltip = "has_foreign_influence"
				check_variable = { influence_array^1 > 0 }
			}
			custom_trigger_tooltip = {
				tooltip = interest_rate_higher_than_10
				check_variable = { interest_rate > 10 }
			}
		}
		
		days_re_enable = 360
		
		cost = 100
		
		complete_effect = {
			set_temp_variable = { debt_bailout_temp = debt }
			multiply_temp_variable = { debt_bailout_temp = 0.15 }
			set_variable = { debt_bailout = debt_bailout_temp }
			round_variable = debt_bailout
			custom_effect_tooltip = bankruptcy_seek_bailout_from_second_biggest_influencer_tt
			var:FROM.influence_array^1 = { country_event = bankruptcy.2 days = 2 }
		}	
	
	}

}