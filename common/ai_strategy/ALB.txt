ALB_KOS_War = {
	allowed = { original_tag = ALB }
	enable = {
		KOS = {
			has_war_with = SER
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = { type = send_volunteers_desire id = "KOS" value = 1000 }
	ai_strategy = { type = send_lend_lease_desire id = "KOS" value = 1000 }
	ai_strategy = { type = support id = "KOS" value = 1000 }
}
ALB_Friend_KOS = {
	allowed = { original_tag = ALB }
	enable = {
		country_exists = KOS
	}
	abort_when_not_enabled = yes
	ai_strategy = { type = befriend id = "KOS" value = 100 }
}

ALB_Friend_CHI = {
	allowed = { original_tag = ALB }
	enable = {
		country_exists = CHI
	}
	abort_when_not_enabled = yes
	ai_strategy = { type = befriend id = "CHI" value = 100 }
}

ALB_hate_SER = {
	allowed = { original_tag = ALB }
	enable = {
		country_exists = SER
	}
	abort_when_not_enabled = yes
	ai_strategy = { type = antagonize id = "SER" value = 100 }
}

ALB_alliance_with_usa = {
	allowed = { original_tag = ALB }
	enable = {
		ALB = {
			OR = {
				has_government = democratic
				has_government = neutrality
			}
		}
		NOT = { has_war_with = USA }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = befriend id = "USA" value = 200 }
	ai_strategy = { type = support id = "USA" value = 200 }
}
ALB_prepare_WAR = {
	allowed = { original_tag = ALB }
	enable = {
		has_war = no
		ALB = {
			country_exists = KOS
			OR = {
				strength_ratio = {
					tag = KOS
					ratio > 0.8
				}
				has_government = nationalist
			}
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = prepare_for_war
		id = KOS
		value = 60
	}
}
ALB_launch_war = {
	allowed = { original_tag = ALB }
	enable = {
		has_war = no
		ALB = {
			country_exists = KOS
			OR = {
				strength_ratio = {
					tag = KOS
					ratio < 0.8
				}
				has_government = nationalist
			}
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = declare_war
		id = KOS
		value = 40
	}
}

ALB_prepare_WAR_II = {
	allowed = { original_tag = ALB }
	enable = {
		has_war = no
		ALB = {
			country_exists = FYR
			OR = {
				strength_ratio = {
					tag = FYR
					ratio > 0.8
				}
				has_government = nationalist
			}
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = prepare_for_war
		id = FYR
		value = 60
	}
}
ALB_launch_war_II = {
	allowed = { original_tag = ALB }
	enable = {
		has_war = no
		ALB = {
			country_exists = FYR
			OR = {
				strength_ratio = {
					tag = FYR
					ratio < 0.8
				}
				has_government = nationalist
			}
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = declare_war
		id = FYR
		value = 40
	}
}
