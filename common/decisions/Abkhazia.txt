ABK_political_decisions = {
	ABK_need_russian_money = {
		fire_only_once = yes
		cost = 50
		days_remove = 90
		visible = {
			original_tag = ABK
			
		}
		available = {
			has_completed_focus = ABK_president_illness
		}
		remove_effect = {
			SOV = {
				set_temp_variable = { treasury_change = -3.5 }
				modify_treasury_effect = yes
				set_temp_variable = { int_investment_change = 3.5 }
				modify_international_investment_effect = yes
			}
			set_temp_variable = { treasury_change = 3.5 }
			modify_treasury_effect = yes
		}
		complete_effect = {
			
		}
	    }

    ABK_exersises_with_russia = {
		fire_only_once = yes
		cost = 50
		days_remove = 60
		visible = {
			original_tag = ABK
			
		}
		available = {
			has_completed_focus =  ABK_training_in_russia
		}
		remove_effect = {
			army_experience = 15
		    air_experience = 15
		}
		complete_effect = {
			
		}
    	}
    ABK_fight_with_mafia = {
		fire_only_once = yes
		cost = 50
		days_remove = 120
		visible = {
			original_tag = ABK
			
		}
		available = {
			has_completed_focus =  ABK_new_era
		}
        remove_effect = {
		if = {
			limit = {
					has_idea = ABK_mafia
				}
			swap_ideas = {
				remove_idea = ABK_mafia
				add_idea = ABK_mafia2
			}
		}
		}
		complete_effect = {
			increase_officer_training_level = yes
	 	}
	    }

    ABK_fight_narco = {
		fire_only_once = yes
		cost = 50
		days_remove = 100
		visible = {
			original_tag = ABK
		
		}
		available = {
			has_completed_focus =  ABK_new_era
		}
		remove_effect = {
			if = {
				limit = {
						has_idea = ABK_narcos
					}
				swap_ideas = {
					remove_idea = ABK_narcos
					add_idea = ABK_narcos2
				}
			}	
		}
		complete_effect = {
			increase_healthcare_budget = yes
		}
	}
}

