scripted_gui = {

	MD_GER_missions_Open_Main = {
		context_type = player_context
		parent_window_token = "MD_GER_missions_Open"
		window_name = "MD_GER_missions_menu_main"

		visible = {
			has_country_flag = GER_missions_gui_open
		}

		effects = {
			GER_button_close_click =
			{
				clr_country_flag = GER_missions_gui_open
			}
			GER_button_bosnia_click = {
				set_country_flag = GER_missions_bosnia_gui_open
			}
		}

	}

	MD_GER_missions_Open_Screen = {
		context_type = player_context
		parent_window_token = "MD_GER_missions_Open"
		window_name = "MD_GER_missions_screen"

		visible = {
			has_country_flag = GER_missions_bosnia_gui_open
		}

		effects = {
			GER_button_close1_click = {
				clr_country_flag = GER_missions_bosnia_gui_open
			}
		}

	}

}
