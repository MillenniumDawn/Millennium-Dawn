# By Zei33

# The Solomon Competition is a quest chain played out between Australia and China.
# The Solomon Islands will request assistance throughout the game and each country
# will have the opportunity to help and gain points. At around 2020, the country with
# the most points will gain some sort of control over the Solomon Islands.
# In the real world, China ends up signing a security pact with them, so something
# in that vain.
# The results of the competition will differ dramatically if Australia is moving
# towards China instead of the USA.
add_namespace = solomon_competition

country_event = { # Solomon Islands Civilian Industry Request - Initial choice
	id = solomon_competition.1
	title = solomon_competition.1.t
	desc = solomon_competition.1.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.1.o1 # We'll help out
		log = "[GetDateText]: [This.GetName]: solomon_competition.1.o1 executed"

		set_temp_variable = { comp_points = 4 }
		if = {
			limit = { original_tag = AST }
			SOL = { SOL_award_AST_points = yes }
		}
		else = {
			SOL = { SOL_award_CHI_points = yes }
		}

		# Build industry in Solomon Islands at cost
		set_temp_variable = { treasury_change = -12.5 }
		modify_treasury_effect = yes
		SOL = {
			random_owned_controlled_state = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			country_event = { id = solomon_competition.3 days = 1 } # Confirmation of investment
			set_country_flag = SOL_industry_construction_built
		}

		# Give influence to investor
		set_temp_variable = { percent_change = 2 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 1

			modifier = {
				check_variable = { treasury < 12.49 }
				factor = 0
			}
		}
	}

	option = {
		name = solomon_competition.1.o2 # We're not interested
		log = "[GetDateText]: [This.GetName]: solomon_competition.1.o2 executed"

		# Approach the other guys
		if = {
			limit = { original_tag = AST }
			CHI = {
				country_event = { id = solomon_competition.2 days = 1 }
			}
		}
		else = {
			AST = {
				country_event = { id = solomon_competition.2 days = 1 }
			}
		}

		# Reduce the influence of the rejector
		set_temp_variable = { percent_change = -3 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 1
		}
	}
}

country_event = { # Solomon Islands Civilian Industry Request - Ask the other guys
	id = solomon_competition.2
	title = solomon_competition.2.t
	desc = solomon_competition.2.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.2.o1 # We'll help out
		log = "[GetDateText]: [This.GetName]: solomon_competition.2.o1 executed"

		set_temp_variable = { comp_points = 4 }
		if = {
			limit = { original_tag = AST }
			SOL = { SOL_award_AST_points = yes }
		}
		else = {
			SOL = { SOL_award_CHI_points = yes }
		}

		# Build industry in Solomon Islands at cost
		set_temp_variable = { treasury_change = -12.5 }
		modify_treasury_effect = yes
		SOL = {
			random_owned_controlled_state = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			country_event = { id = solomon_competition.3 days = 1 }
			set_country_flag = SOL_industry_construction_built
		}

		set_temp_variable = { percent_change = 10 } # Extra grateful
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 2 # More likely to accept after rejection by opponent.

			modifier = {
				check_variable = { treasury < 12.5 }
				factor = 0
			}
		}
	}

	option = {
		name = solomon_competition.2.o2 # We're not interested
		log = "[GetDateText]: [This.GetName]: solomon_competition.2.o2 executed"

		SOL = {
			country_event = { id = solomon_competition.4 days = 1 }
		}

		# Reduce the influence of the rejector
		set_temp_variable = { percent_change = -5 } # Extra disappointed
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 1
		}
	}
}

country_event = { # Solomon Islands Civilian Industry Request - Someone agreed to help
	id = solomon_competition.3
	title = solomon_competition.3.t
	desc = solomon_competition.3.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.3.o1 # Great news!
		log = "[GetDateText]: [This.GetName]: solomon_competition.3.o1 executed"

		country_event = { id = solomon_competition.5 days = 250 random_days = 100 } # Make next decision available

		add_opinion_modifier = {
			target = ROOT
			modifier = recently_invest
		}
	}
}

country_event = { # Solomon Islands Civilian Industry Request - Nobody wants to help
	id = solomon_competition.4
	title = solomon_competition.4.t
	desc = solomon_competition.4.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.4.o1 # We've been abandoned...
		log = "[GetDateText]: [This.GetName]: solomon_competition.4.o1 executed"

		country_event = { id = solomon_competition.5 days = 250 random_days = 100 }
	}
}

country_event = { # The second set of decisions in the Solomon Competition becomes available.
	id = solomon_competition.5
	title = solomon_competition.5.t # Onwards to a better future!
	desc = solomon_competition.5.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.5.o1 # Onwards to a better future!
		log = "[GetDateText]: [This.GetName]: solomon_competition.5 executed"

		SOL_advance_comp_stage = yes
	}
}

country_event = { # Solomon Islands Infrastructure Request - Initial choice
	id = solomon_competition.6
	title = solomon_competition.6.t
	desc = solomon_competition.6.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.6.o1 # We'll help out
		log = "[GetDateText]: [This.GetName]: solomon_competition.6.o1 executed"

		set_temp_variable = { comp_points = 2 }
		if = {
			limit = { original_tag = AST }
			SOL = { SOL_award_AST_points = yes }
		}
		else = {
			SOL = { SOL_award_CHI_points = yes }
		}

		# Build infrastructure in Solomon Islands at cost
		set_temp_variable = { treasury_change = -5 }
		modify_treasury_effect = yes
		SOL = {
			random_owned_controlled_state = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
			country_event = { id = solomon_competition.8 days = 1 } # Confirmation of investment
			set_country_flag = SOL_infrastructure_construction_built
		}

		# Give influence to investor
		set_temp_variable = { percent_change = 2 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 1

			modifier = {
				check_variable = { treasury < 4.99 }
				factor = 0
			}
		}
	}

	option = {
		name = solomon_competition.6.o2 # We're not interested
		log = "[GetDateText]: [This.GetName]: solomon_competition.6.o2 executed"

		# Approach the other guys
		if = {
			limit = { original_tag = AST }
			CHI = {
				country_event = { id = solomon_competition.7 days = 1 }
			}
		}
		else = {
			AST = {
				country_event = { id = solomon_competition.7 days = 1 }
			}
		}

		# Reduce the influence of the rejector
		set_temp_variable = { percent_change = -3 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 1
		}
	}
}

country_event = { # Solomon Islands Infrastructure Request - Ask the other guys
	id = solomon_competition.7
	title = solomon_competition.7.t
	desc = solomon_competition.7.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.7.o1 # We'll help out
		log = "[GetDateText]: [This.GetName]: solomon_competition.7.o1 executed"

		set_temp_variable = { comp_points = 2 }
		if = {
			limit = { original_tag = AST }
			SOL = { SOL_award_AST_points = yes }
		}
		else = {
			SOL = { SOL_award_CHI_points = yes }
		}

		# Build infrastructure in Solomon Islands at cost
		set_temp_variable = { treasury_change = -5 }
		modify_treasury_effect = yes
		SOL = {
			random_owned_controlled_state = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
			country_event = { id = solomon_competition.8 days = 1 }
			set_country_flag = SOL_infrastructure_construction_built
		}

		set_temp_variable = { percent_change = 10 } # Extra grateful
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 2 # More likely to accept after rejection by opponent.

			modifier = {
				check_variable = { treasury < 4.99 }
				factor = 0
			}
		}
	}

	option = {
		name = solomon_competition.7.o2 # We're not interested
		log = "[GetDateText]: [This.GetName]: solomon_competition.7.o2 executed"

		SOL = {
			country_event = { id = solomon_competition.9 days = 1 }
		}

		# Reduce the influence of the rejector
		set_temp_variable = { percent_change = -5 } # Extra disappointed
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 1
		}
	}
}

country_event = { # Solomon Islands Infrastructure Request - Someone agreed to help
	id = solomon_competition.8
	title = solomon_competition.8.t
	desc = solomon_competition.8.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.8.o1 # Great news!
		log = "[GetDateText]: [This.GetName]: solomon_competition.8.o1 executed"

		country_event = { id = solomon_competition.10 days = 250 random_days = 100 } # Make next decision available

		add_opinion_modifier = {
			target = ROOT
			modifier = recently_invest
		}
	}
}

country_event = { # Solomon Islands Civilian Industry Request - Nobody wants to help
	id = solomon_competition.9
	title = solomon_competition.9.t
	desc = solomon_competition.9.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.9.o1 # We've been abandoned...
		log = "[GetDateText]: [This.GetName]: solomon_competition.9.o1 executed"

		country_event = { id = solomon_competition.10 days = 250 random_days = 100 }
	}
}

country_event = { # The third set of decisions in the Solomon Competition becomes available.
	id = solomon_competition.10
	title = solomon_competition.10.t # The Information Age
	desc = solomon_competition.10.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.10.o1 # Let's get connected!
		log = "[GetDateText]: [This.GetName]: solomon_competition.10.o1 executed"

		SOL_advance_comp_stage = yes
	}
}

country_event = { # Solomon unrest
	id = solomon_competition.25
	title = solomon_competition.25.t
	desc = solomon_competition.25.d
	picture = GFX_internal_conflict

	is_triggered_only = yes
	fire_only_once = yes

	immediate = {
		add_stability = -0.10
	}

	option = {
		name = solomon_competition.25.o1 # Maybe things will improve?
		log = "[GetDateText]: [This.GetName]: solomon_competition.25.o1 executed"

		country_event = { id = solomon_competition.26 days = 14 }
	}
}

country_event = { # Violence erupts
	id = solomon_competition.26
	title = solomon_competition.26.t
	desc = solomon_competition.26.d
	picture = GFX_2003_solomon_unrest

	is_triggered_only = yes
	fire_only_once = yes

	immediate = {
		add_stability = -0.20
	}

	option = {
		name = solomon_competition.26.o1 # We need international assistance
		log = "[GetDateText]: [This.GetName]: solomon_competition.26.o1 executed"

		SOL = {
			add_ideas = SOL_idea_rampant_ethnic_violence
			add_ideas = SOL_idea_humanitarian_disaster
		}

		AST = {
			country_event = { id = solomon_competition.27 days = 2 }
		}
	}
}

country_event = { # Operation RAMSI - Initial request
	id = solomon_competition.27
	title = solomon_competition.27.t
	desc = {
		text = solomon_competition.27.d2
		trigger = {
			original_tag = CHI
			NOT = { SOL = { has_country_flag = SOL_AST_declined_RAMSI } }
		}
	}
	desc = {
		text = solomon_competition.27.d3
		trigger = {
			original_tag = CHI
			SOL = { has_country_flag = SOL_AST_declined_RAMSI }
		}
	}
	desc = { text = solomon_competition.27.d1 }
	picture = GFX_2003_solomon_unrest

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.27.o1 # Send our forces
		log = "[GetDateText]: [This.GetName]: solomon_competition.27.o1 executed"

		if = {
			limit = { original_tag = AST }
			set_country_flag = SOL_AST_operation_RAMSI
			mark_focus_tree_layout_dirty = yes
			log = "[GetDateText]: Australia has committed to Operation RAMSI. Points follow from the intervention focuses."
		}
		else = {
			set_temp_variable = { comp_points = 4 }
			SOL = { SOL_award_CHI_points = yes }
			set_country_flag = SOL_CHI_operation_RAMSI
			log = "[GetDateText]: China has committed to Operation RAMSI."
		}

		SOL = {
			set_variable = { RAMSI_supporter = PREV }
			country_event = { id = solomon_competition.28 days = 1 }
		}

		log = "RAMSI_supporter is [?SOL.RAMSI_supporter.GetName]"

		ai_chance = {
			base = 3
		}
	}

	option = {
		name = solomon_competition.27.o2 # Let's see how this plays out
		log = "[GetDateText]: [This.GetName]: solomon_competition.27.o2 executed"

		if = {
			limit = { original_tag = AST }
			SOL = {
				set_country_flag = SOL_AST_declined_RAMSI
			}
			CHI = {
				country_event = { id = solomon_competition.27 days = 30 }
			}
		}
		else = {
			SOL = {
				country_event = { id = solomon_competition.29 days = 1 }
			}
		}

		ai_chance = {
			base = 1
		}
	}
}

country_event = { # Operation RAMSI - Accepted request
	id = solomon_competition.28
	title = solomon_competition.28.t
	desc = solomon_competition.28.d
	picture = GFX_2003_solomon_unrest

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.28.o1 # Thank goodness...
		log = "[GetDateText]: [This.GetName]: solomon_competition.28.o1 executed"

		SOL = {
			if = {
				limit = {
					check_variable = { RAMSI_supporter = AST }
				}
				add_opinion_modifier = {
					target = AST
					modifier = SOL_ramsi_support
				}
			}
			else_if = {
				limit = {
					check_variable = { RAMSI_supporter = CHI }
				}
				add_opinion_modifier = {
					target = CHI
					modifier = SOL_ramsi_support
				}
			}
		}
	}
}

country_event = { # Operation RAMSI - Rejected request
	id = solomon_competition.29
	title = solomon_competition.29.t
	desc = solomon_competition.29.d
	picture = GFX_2003_solomon_unrest

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.29.o1 # We're on our own...
		log = "[GetDateText]: [This.GetName]: solomon_competition.29.o1 executed"

		add_opinion_modifier = {
			target = AST
			modifier = SOL_abandoned_us
		}

		add_opinion_modifier = {
			target = CHI
			modifier = SOL_abandoned_us
		}
	}
}

country_event = {
	id = solomon_competition.30 # Operation RAMSI - Foreign Forces Arrive
	title = solomon_competition.30.t
	desc = solomon_competition.30.d
	picture = GFX_2021_solomon_intervention_police

	is_triggered_only = yes

	option = {
		name = solomon_competition.30.o1 # A promising start
		log = "[GetDateText]: [This.GetName]: solomon_competition.30.o1 executed"

		if = {
			limit = { tag = CHI }
			SOL = {
				remove_ideas = {
					SOL_idea_rampant_ethnic_violence
					SOL_idea_humanitarian_disaster
				}
				add_ideas = SOL_CHI_idea_contained_ethnic_violence
				country_event = { id = solomon_competition.31 days = 100 }
			}
			country_event = { id = solomon_competition.31 days = 100 }
		}
	}
}

country_event = {
	id = solomon_competition.31 # Operation RAMSI - Destroy the guns
	title = solomon_competition.31.t
	desc = solomon_competition.31.d
	picture = GFX_2003_solomon_gun_burning

	is_triggered_only = yes

	option = {
		name = solomon_competition.31.o1 # Destroy the guns
		log = "[GetDateText]: [This.GetName]: solomon_competition.31.o1 executed"
		trigger = {
			NOT = { tag = SOL }
		}
		if = {
			limit = { tag = CHI }
			SOL = {
				add_ideas = SOL_idea_disarmed_population
				set_temp_variable = { comp_points = 2 }
				SOL_award_CHI_points = yes
				country_event = { id = solomon_competition.32 days = 100 }
			}
			country_event = { id = solomon_competition.32 days = 100 }
		}
	}

	option = {
		name = solomon_competition.31.o2 # This was a good idea
		log = "[GetDateText]: [This.GetName]: solomon_competition.31.o2 executed"
		trigger = {
			tag = SOL
		}
	}
}

country_event = {
	id = solomon_competition.32 # Operation RAMSI - Restore the government
	title = solomon_competition.32.t
	desc = {
		text = solomon_competition.32.d2 # China does it (Emerging shift)
		trigger = {
			CHI = { has_country_flag = SOL_CHI_operation_RAMSI }
		}
	}
	desc = { text = solomon_competition.32.d1 } # Australia does it (Western shift)
	picture = GFX_political_deal

	is_triggered_only = yes

	option = {
		name = solomon_competition.32.o1 # Almost there
		log = "[GetDateText]: [This.GetName]: solomon_competition.32.o1 executed"
		trigger = {
			tag = SOL.RAMSI_supporter
		}

		if = {
			limit = { tag = CHI }
			country_event = { id = solomon_competition.33 days = 100 }
			SOL = {
				set_temp_variable = { comp_points = 2 }
				SOL_award_CHI_points = yes
				add_ideas = SOL_CHI_idea_reformed_government
				remove_ideas = SOL_CHI_idea_contained_ethnic_violence
				country_event = { id = solomon_competition.33 days = 100 }
			}
		}
	}

	option = {
		name = solomon_competition.32.o2 # Good luck to them
		log = "[GetDateText]: [This.GetName]: solomon_competition.32.o2 executed"
		trigger = {
			tag = CHI
			NOT = { tag = SOL.RAMSI_supporter }
		}
	}

	option = {
		name = solomon_competition.32.o3 # Uh oh...
		log = "[GetDateText]: [This.GetName]: solomon_competition.32.o3 executed"
		trigger = {
			tag = AST
			NOT = { tag = SOL.RAMSI_supporter }
		}
	}

	option = {
		name = solomon_competition.32.o4 # Stability returns
		log = "[GetDateText]: [This.GetName]: solomon_competition.32.o4 executed"
		trigger = {
			tag = SOL
		}
	}
}

country_event = {
	id = solomon_competition.33 # Operation RAMSI - The Solomon Islands are saved
	title = solomon_competition.33.t
	desc = solomon_competition.33.d
	picture = GFX_corvette

	is_triggered_only = yes

	option = {
		name = solomon_competition.33.o1 # Mission accomplished
		log = "[GetDateText]: [This.GetName]: solomon_competition.33.o1 executed"

		add_manpower = 2000
	}
}

country_event = { # Solomon Islands Connectivity Request - Initial choice
	id = solomon_competition.11
	title = solomon_competition.11.t
	desc = solomon_competition.11.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.11.o1 # We'll help out
		log = "[GetDateText]: [This.GetName]: solomon_competition.11.o1 executed"

		set_temp_variable = { comp_points = 3 }
		if = {
			limit = { original_tag = AST }
			SOL = { SOL_award_AST_points = yes }
		}
		else = {
			SOL = { SOL_award_CHI_points = yes }
		}

		# Build a radar station in Solomon Islands at cost
		set_temp_variable = { treasury_change = -8 }
		modify_treasury_effect = yes
		SOL = {
			random_owned_controlled_state = {
				add_building_construction = {
					type = radar_station
					level = 1
					instant_build = yes
				}
			}
			country_event = { id = solomon_competition.13 days = 1 }
			set_country_flag = SOL_connectivity_construction_built
		}

		# Give influence to investor
		set_temp_variable = { percent_change = 2 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 1

			modifier = {
				check_variable = { treasury < 7.99 }
				factor = 0
			}
		}
	}

	option = {
		name = solomon_competition.11.o2 # We're not interested
		log = "[GetDateText]: [This.GetName]: solomon_competition.11.o2 executed"

		if = {
			limit = { original_tag = AST }
			CHI = {
				country_event = { id = solomon_competition.12 days = 1 }
			}
		}
		else = {
			AST = {
				country_event = { id = solomon_competition.12 days = 1 }
			}
		}

		set_temp_variable = { percent_change = -3 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 1
		}
	}
}

country_event = { # Solomon Islands Connectivity Request - Ask the other guys
	id = solomon_competition.12
	title = solomon_competition.12.t
	desc = solomon_competition.12.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.12.o1 # We'll help out
		log = "[GetDateText]: [This.GetName]: solomon_competition.12.o1 executed"

		set_temp_variable = { comp_points = 3 }
		if = {
			limit = { original_tag = AST }
			SOL = { SOL_award_AST_points = yes }
		}
		else = {
			SOL = { SOL_award_CHI_points = yes }
		}

		set_temp_variable = { treasury_change = -8 }
		modify_treasury_effect = yes
		SOL = {
			random_owned_controlled_state = {
				add_building_construction = {
					type = radar_station
					level = 1
					instant_build = yes
				}
			}
			country_event = { id = solomon_competition.13 days = 1 }
			set_country_flag = SOL_connectivity_construction_built
		}

		set_temp_variable = { percent_change = 10 } # Extra grateful
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 2

			modifier = {
				check_variable = { treasury < 8 }
				factor = 0
			}
		}
	}

	option = {
		name = solomon_competition.12.o2 # We're not interested
		log = "[GetDateText]: [This.GetName]: solomon_competition.12.o2 executed"

		SOL = {
			country_event = { id = solomon_competition.14 days = 1 }
		}

		set_temp_variable = { percent_change = -5 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 1
		}
	}
}

country_event = { # Solomon Islands Connectivity Request - Someone agreed to help
	id = solomon_competition.13
	title = solomon_competition.13.t
	desc = solomon_competition.13.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.13.o1 # Great news!
		log = "[GetDateText]: [This.GetName]: solomon_competition.13.o1 executed"

		country_event = { id = solomon_competition.15 days = 250 random_days = 100 }

		add_opinion_modifier = {
			target = ROOT
			modifier = recently_invest
		}
	}
}

country_event = { # Solomon Islands Connectivity Request - Nobody wants to help
	id = solomon_competition.14
	title = solomon_competition.14.t
	desc = solomon_competition.14.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.14.o1 # We'll need to make our own way
		log = "[GetDateText]: [This.GetName]: solomon_competition.14.o1 executed"

		country_event = { id = solomon_competition.15 days = 250 random_days = 100 }
	}
}

country_event = { # The connectivity stage of the Solomon Competition concludes.
	id = solomon_competition.15
	title = solomon_competition.15.t
	desc = solomon_competition.15.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.15.o1 # The future is bright
		log = "[GetDateText]: [This.GetName]: solomon_competition.15 executed"

		# The three construction stages are done, but the contest is not. The
		# maritime security stage runs from here through the 2010s and stays
		# open until the competition is settled.
		SOL_advance_comp_stage = yes
	}
}

country_event = { # Solomon Islands Maritime Security Request - Initial choice
	id = solomon_competition.16
	title = solomon_competition.16.t
	desc = solomon_competition.16.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.16.o1 # We'll fund the patrol fleet
		log = "[GetDateText]: [This.GetName]: solomon_competition.16.o1 executed"

		set_temp_variable = { treasury_change = -20 }
		modify_treasury_effect = yes

		set_temp_variable = { comp_points = 5 }
		if = {
			limit = { original_tag = AST }
			SOL = { SOL_award_AST_points = yes }
		}
		else_if = {
			limit = { original_tag = USA }
			SOL = { SOL_award_USA_points = yes }
		}
		else = {
			SOL = { SOL_award_CHI_points = yes }
		}

		SOL = {
			add_ideas = SOL_idea_maritime_surveillance_programme
			country_event = { id = solomon_competition.18 days = 1 }
			set_country_flag = SOL_maritime_programme_built
		}

		set_temp_variable = { percent_change = 4 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 2

			modifier = {
				check_variable = { treasury < 20 }
				factor = 0
			}
			modifier = {
				add = 2
				SOL_competition_caller_leads = yes
			}
		}
	}

	option = {
		name = solomon_competition.16.o2 # We're not interested
		log = "[GetDateText]: [This.GetName]: solomon_competition.16.o2 executed"

		# Turned down, Honiara walks the request across to the other side of
		# whatever contest it is currently running. Who that is depends on the
		# configuration rather than on any fixed pairing: in mode 3 Beijing is
		# sitting on Canberra's side of the table and Washington is the one
		# across it, so a refusal from either half of that bloc goes to the
		# Americans rather than back to the partner who is already funding it.
		if = {
			limit = {
				OR = {
					original_tag = AST
					original_tag = CHI
				}
			}
			if = {
				limit = {
					SOL_competition_is_usa_rival = yes
					country_exists = USA
				}
				USA = { country_event = { id = solomon_competition.17 days = 1 } }
			}
			else_if = {
				limit = {
					original_tag = AST
					country_exists = CHI
				}
				CHI = { country_event = { id = solomon_competition.17 days = 1 } }
			}
			else_if = {
				limit = {
					original_tag = CHI
					country_exists = AST
				}
				AST = { country_event = { id = solomon_competition.17 days = 1 } }
			}
			else = {
				SOL = { country_event = { id = solomon_competition.19 days = 1 } }
			}
		}
		# Washington only ever holds this file itself in mode 3, where Canberra
		# is the rival it is bidding against.
		else = {
			if = {
				limit = { country_exists = AST }
				AST = { country_event = { id = solomon_competition.17 days = 1 } }
			}
			else = {
				SOL = { country_event = { id = solomon_competition.19 days = 1 } }
			}
		}

		set_temp_variable = { percent_change = -3 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = { base = 1 }
	}
}

country_event = { # Solomon Islands Maritime Security Request - Ask the other guys
	id = solomon_competition.17
	title = solomon_competition.17.t
	desc = solomon_competition.17.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.17.o1 # We'll fund the patrol fleet
		log = "[GetDateText]: [This.GetName]: solomon_competition.17.o1 executed"

		set_temp_variable = { treasury_change = -20 }
		modify_treasury_effect = yes

		set_temp_variable = { comp_points = 5 }
		if = {
			limit = { original_tag = AST }
			SOL = { SOL_award_AST_points = yes }
		}
		else_if = {
			limit = { original_tag = USA }
			SOL = { SOL_award_USA_points = yes }
		}
		else = {
			SOL = { SOL_award_CHI_points = yes }
		}

		SOL = {
			add_ideas = SOL_idea_maritime_surveillance_programme
			country_event = { id = solomon_competition.18 days = 1 }
			set_country_flag = SOL_maritime_programme_built
		}

		set_temp_variable = { percent_change = 10 } # Extra grateful
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 3 # More likely to accept after a rejection by the opponent.

			modifier = {
				check_variable = { treasury < 20 }
				factor = 0
			}
		}
	}

	option = {
		name = solomon_competition.17.o2 # We're not interested
		log = "[GetDateText]: [This.GetName]: solomon_competition.17.o2 executed"

		SOL = {
			country_event = { id = solomon_competition.19 days = 1 }
		}

		set_temp_variable = { percent_change = -5 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = { base = 1 }
	}
}

country_event = { # Solomon Islands Maritime Security Request - Someone agreed to help
	id = solomon_competition.18
	title = solomon_competition.18.t
	desc = solomon_competition.18.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.18.o1 # Our waters are ours again
		log = "[GetDateText]: [This.GetName]: solomon_competition.18.o1 executed"

		add_opinion_modifier = {
			target = ROOT
			modifier = recently_invest
		}
	}
}

country_event = { # Solomon Islands Maritime Security Request - Nobody wants to help
	id = solomon_competition.19
	title = solomon_competition.19.t
	desc = solomon_competition.19.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.19.o1 # We patrol alone
		log = "[GetDateText]: [This.GetName]: solomon_competition.19.o1 executed"

		add_stability = -0.02
	}
}

country_event = { # A suitor pulls ahead in the Solomons
	id = solomon_competition.20
	title = solomon_competition.20.t
	desc = solomon_competition.20.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes

	option = {
		name = solomon_competition.20.o1 # Noted in Honiara
		log = "[GetDateText]: [This.GetName]: Solomon competition reached tier [?SOL_comp_tier]."
	}
}

country_event = { # Solomon Competition - Resolution gate
	# The yearly trigger fires this rather than the resolution itself. A
	# Compact of Free Association is five beats of decision timers - 120 + 180
	# + 180 + 150 days at best, and another 180 + 365 for every rejected
	# strategic denial clause - and it cannot even open until Australia has
	# gone over to Beijing. Settling the contest on a fixed date in 2022 would
	# close it out from under a negotiation already in progress and put the
	# ratification, the signed Compact and everything downstream of it out of
	# reach. So while Washington is actively negotiating, resolution stands
	# itself down for half a year and asks again, up to a hard cap of ten
	# deferrals so nothing can loop forever.
	id = solomon_competition.39
	hidden = yes

	is_triggered_only = yes

	immediate = {
		if = {
			limit = { NOT = { has_country_flag = SOL_finished_solomon_competition } }

			if = {
				limit = {
					SOL_competition_resolution_blocked = yes
					check_variable = { SOL_comp_resolution_defers < 10 }
				}
				add_to_variable = { SOL_comp_resolution_defers = 1 }
				set_country_flag = SOL_comp_resolution_deferred
				log = "[GetDateText]: Solomon Competition resolution deferred - the American Compact arc is still being negotiated (deferral [?SOL_comp_resolution_defers] of 10)."
				country_event = { id = solomon_competition.39 days = 180 }
			}
			else = {
				clr_country_flag = SOL_comp_resolution_deferred
				country_event = { id = solomon_competition.40 days = 1 }
			}
		}
	}
}

country_event = { # Solomon Competition - Resolution
	id = solomon_competition.40
	title = solomon_competition.40.t
	desc = solomon_competition.40.d
	picture = GFX_political_deal

	is_triggered_only = yes
	fire_only_once = yes

	immediate = {
		SOL_refresh_comp_tier = yes
		log = "[GetDateText]: Solomon Competition resolving. Mode [?AST_comp_mode], tier [?SOL_comp_tier], AST [?AST_comp_scores^0], CHI [?AST_comp_scores^1], USA [?AST_comp_scores^2]."
	}

	option = {
		name = solomon_competition.40.o1 # Acknowledge
		log = "[GetDateText]: [This.GetName]: solomon_competition.40.o1 executed"

		# Nobody has built enough standing in Honiara to be called a patron.
		if = {
			limit = { check_variable = { SOL_comp_tier = 0 } }
			SOL_apply_competition_outcome = yes
			if = { limit = { country_exists = AST } AST = { country_event = { id = solomon_competition.43 days = 1 } } }
			if = { limit = { country_exists = CHI } CHI = { country_event = { id = solomon_competition.43 days = 1 } } }
			if = { limit = { country_exists = USA } USA = { country_event = { id = solomon_competition.43 days = 1 } } }
		}
		# The reversed contest: Australia and China together, against Washington.
		else_if = {
			limit = { check_variable = { AST_comp_mode = 3 } }
			SOL_apply_competition_outcome = yes
			if = {
				limit = { check_variable = { AST_comp_status = 1 } }
				if = { limit = { country_exists = AST } AST = { country_event = { id = solomon_competition.54 days = 1 } } }
				if = { limit = { country_exists = CHI } CHI = { country_event = { id = solomon_competition.54 days = 1 } } }
				if = { limit = { country_exists = USA } USA = { country_event = { id = solomon_competition.54 days = 1 } } }
				news_event = { id = usa_pacific_news.1 days = 3 }
			}
			else_if = {
				limit = { check_variable = { AST_comp_status = 2 } }
				if = { limit = { country_exists = USA } USA = { country_event = { id = solomon_competition.55 days = 1 } } }
				if = { limit = { country_exists = AST } AST = { country_event = { id = solomon_competition.55 days = 1 } } }
				if = { limit = { country_exists = CHI } CHI = { country_event = { id = solomon_competition.55 days = 1 } } }
				news_event = { id = usa_pacific_news.2 days = 3 }
			}
			# Level on points. Nobody carried the reversed contest either, so
			# nobody gets to claim it - the same stalemate the default contest
			# already knows how to end in.
			else = {
				if = { limit = { country_exists = AST } AST = { country_event = { id = solomon_competition.43 days = 1 } } }
				if = { limit = { country_exists = CHI } CHI = { country_event = { id = solomon_competition.43 days = 1 } } }
				if = { limit = { country_exists = USA } USA = { country_event = { id = solomon_competition.43 days = 1 } } }
			}
		}
		# The default contest: Australia, with or without American backing,
		# against China.
		else = {
			SOL_apply_competition_outcome = yes
			if = {
				limit = { check_variable = { AST_comp_status = 1 } }
				if = { limit = { country_exists = AST } AST = { country_event = { id = solomon_competition.41 days = 1 } } }
				if = { limit = { country_exists = CHI } CHI = { country_event = { id = solomon_competition.41 days = 1 } } }
				if = { limit = { country_exists = USA } USA = { country_event = { id = solomon_competition.41 days = 1 } } }
			}
			else_if = {
				limit = { check_variable = { AST_comp_status = 2 } }
				if = { limit = { country_exists = CHI } CHI = { country_event = { id = solomon_competition.42 days = 1 } } }
				if = { limit = { country_exists = AST } AST = { country_event = { id = solomon_competition.42 days = 1 } } }
				if = { limit = { country_exists = USA } USA = { country_event = { id = solomon_competition.42 days = 1 } } }
			}
			else = {
				if = { limit = { country_exists = AST } AST = { country_event = { id = solomon_competition.43 days = 1 } } }
				if = { limit = { country_exists = CHI } CHI = { country_event = { id = solomon_competition.43 days = 1 } } }
				if = { limit = { country_exists = USA } USA = { country_event = { id = solomon_competition.43 days = 1 } } }
			}
		}
	}
}

country_event = { # Solomon Competition - Australia wins
	id = solomon_competition.41
	title = solomon_competition.41.t
	desc = {
		text = solomon_competition.41.d1
		trigger = { original_tag = AST }
	}
	desc = {
		text = solomon_competition.41.d2
		trigger = { original_tag = CHI }
	}
	desc = {
		text = solomon_competition.41.d3
		trigger = { original_tag = USA }
	}
	picture = GFX_political_deal

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.41.o1 # A partner secured
		log = "[GetDateText]: [This.GetName]: solomon_competition.41.o1 executed"
		trigger = { original_tag = AST }

		# Only a patron-tier result puts Honiara inside Canberra's orbit.
		# Anything less is a partnership between equals.
		SOL_grant_patron_standing = yes
		if = {
			limit = { SOL = { check_variable = { SOL_comp_tier > 2 } } }
			add_ideas = SOL_AST_pacific_security_partner
		}
		else = {
			add_ideas = SOL_AST_pacific_partnership_accord
		}

		add_opinion_modifier = {
			target = SOL
			modifier = SOL_pacific_partner
		}
		SOL = {
			add_opinion_modifier = {
				target = AST
				modifier = SOL_pacific_partner
			}
			add_opinion_modifier = {
				target = CHI
				modifier = SOL_abandoned_us
			}
		}
		# Washington bankrolled this result and gets the credit for it.
		if = {
			limit = {
				country_exists = USA
				SOL = { check_variable = { AST_comp_mode = 2 } }
			}
			USA = {
				add_opinion_modifier = {
					target = AST
					modifier = SOL_pacific_partner
				}
			}
			add_opinion_modifier = {
				target = USA
				modifier = SOL_pacific_partner
			}
		}
	}

	option = {
		name = solomon_competition.41.o2 # A diplomatic loss
		log = "[GetDateText]: [This.GetName]: solomon_competition.41.o2 executed"
		trigger = { original_tag = CHI }

		add_opinion_modifier = {
			target = SOL
			modifier = SOL_abandoned_us
		}
	}

	option = {
		name = solomon_competition.41.o3 # Our partner held the line
		log = "[GetDateText]: [This.GetName]: solomon_competition.41.o3 executed"
		trigger = { original_tag = USA }

		add_political_power = 25
		add_opinion_modifier = {
			target = SOL
			modifier = SOL_pacific_partner
		}
	}
}

country_event = { # Solomon Competition - China wins
	id = solomon_competition.42
	title = solomon_competition.42.t
	desc = {
		text = solomon_competition.42.d1
		trigger = { original_tag = CHI }
	}
	desc = {
		text = solomon_competition.42.d2
		trigger = { original_tag = AST }
	}
	desc = {
		text = solomon_competition.42.d3
		trigger = { original_tag = USA }
	}
	picture = GFX_political_deal

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.42.o1 # A partner secured
		log = "[GetDateText]: [This.GetName]: solomon_competition.42.o1 executed"
		trigger = { original_tag = CHI }

		# Only a patron-tier result puts Honiara inside Beijing's orbit.
		# Anything less is a partnership between equals - but it is still a
		# win, and it still buys something.
		SOL_grant_patron_standing = yes
		if = {
			limit = { SOL = { check_variable = { SOL_comp_tier > 2 } } }
			add_ideas = SOL_CHI_pacific_security_partner
		}
		else = {
			add_ideas = SOL_CHI_pacific_partnership_accord
		}

		add_opinion_modifier = {
			target = SOL
			modifier = SOL_pacific_partner
		}
		SOL = {
			add_opinion_modifier = {
				target = CHI
				modifier = SOL_pacific_partner
			}
			add_opinion_modifier = {
				target = AST
				modifier = SOL_abandoned_us
			}
		}
	}

	option = {
		name = solomon_competition.42.o2 # We have lost our own near region
		log = "[GetDateText]: [This.GetName]: solomon_competition.42.o2 executed"
		trigger = { original_tag = AST }

		add_opinion_modifier = {
			target = SOL
			modifier = SOL_abandoned_us
		}
		add_timed_idea = { idea = SOL_AST_lost_the_near_region days = 1825 }
		add_political_power = -50
		add_stability = -0.02
	}

	option = {
		name = solomon_competition.42.o3 # A gap in the second island chain
		log = "[GetDateText]: [This.GetName]: solomon_competition.42.o3 executed"
		trigger = { original_tag = USA }

		add_political_power = -30
		add_opinion_modifier = {
			target = SOL
			modifier = SOL_abandoned_us
		}
	}
}

country_event = { # Solomon Competition - Neither wins
	id = solomon_competition.43
	title = solomon_competition.43.t
	desc = solomon_competition.43.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.43.o1 # The moment passed
		log = "[GetDateText]: [This.GetName]: solomon_competition.43.o1 executed"

		# Nobody bought Honiara, so Honiara keeps its own counsel and the
		# courting simply carries on at a lower intensity.
		if = {
			limit = { country_exists = SOL }
			SOL = { set_country_flag = SOL_non_aligned_pacific_state }
		}
		add_political_power = -15
	}
}

########################################################################
# The reversal - Australia realigns towards Beijing
########################################################################

country_event = { # Australia's realignment reaches the Solomons
	id = solomon_competition.50
	title = solomon_competition.50.t
	desc = solomon_competition.50.d
	picture = GFX_political_deal

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.50.o1 # The contest has changed shape
		log = "[GetDateText]: [This.GetName]: solomon_competition.50.o1 executed"

		add_political_power = 25
		custom_effect_tooltip = solomon_competition_flip_tt
	}
}

country_event = { # Washington must court Honiara itself
	id = solomon_competition.51
	title = solomon_competition.51.t
	desc = solomon_competition.51.d
	picture = GFX_political_deal

	is_triggered_only = yes
	fire_only_once = yes

	trigger = { original_tag = USA }

	option = {
		name = solomon_competition.51.o1 # We will contest it ourselves
		log = "[GetDateText]: [This.GetName]: solomon_competition.51.o1 executed"

		set_country_flag = USA_SOL_competition_engaged
		add_political_power = -25
		set_temp_variable = { comp_points = 3 }
		SOL = { SOL_award_USA_points = yes }
		unlock_decision_tooltip = USA_SOL_pacific_partnership_aid
		ai_chance = { base = 8 }
	}

	option = {
		name = solomon_competition.51.o2 # The Pacific is not worth the effort
		log = "[GetDateText]: [This.GetName]: solomon_competition.51.o2 executed"

		add_political_power = 25
		set_temp_variable = { comp_points = 2 }
		SOL = { SOL_award_AST_points = yes }
		ai_chance = {
			base = 2
			modifier = {
				factor = 0
				OR = {
					western_conservatism_are_in_power = yes
					western_liberals_are_in_power = yes
				}
			}
		}
	}
}

country_event = { # Beijing welcomes an Australian partner in the Pacific
	id = solomon_competition.52
	title = solomon_competition.52.t
	desc = solomon_competition.52.d
	picture = GFX_political_deal

	is_triggered_only = yes
	fire_only_once = yes

	trigger = { original_tag = CHI }

	option = {
		name = solomon_competition.52.o1 # Work through Canberra
		log = "[GetDateText]: [This.GetName]: solomon_competition.52.o1 executed"

		set_temp_variable = { treasury_change = -40 }
		modify_treasury_effect = yes
		set_temp_variable = { comp_points = 5 }
		SOL = { SOL_award_CHI_points = yes }
		add_opinion_modifier = {
			target = AST
			modifier = SOL_pacific_partner
		}
		ai_chance = { base = 8 }
	}

	option = {
		name = solomon_competition.52.o2 # Beijing prefers to deal directly
		log = "[GetDateText]: [This.GetName]: solomon_competition.52.o2 executed"

		add_political_power = 20
		ai_chance = { base = 3 }
	}
}

country_event = { # Honiara weighs a very different set of suitors
	id = solomon_competition.53
	title = solomon_competition.53.t
	desc = solomon_competition.53.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.53.o1 # The old certainties are gone
		log = "[GetDateText]: [This.GetName]: solomon_competition.53.o1 executed"
	}
}

country_event = { # Reversed competition - Australia and China prevail
	id = solomon_competition.54
	title = solomon_competition.54.t
	desc = {
		text = solomon_competition.54.d1
		trigger = { original_tag = AST }
	}
	desc = {
		text = solomon_competition.54.d2
		trigger = { original_tag = CHI }
	}
	desc = {
		text = solomon_competition.54.d3
		trigger = { original_tag = USA }
	}
	picture = GFX_political_deal

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.54.o1 # The Pacific on our own terms
		log = "[GetDateText]: [This.GetName]: solomon_competition.54.o1 executed"
		trigger = { original_tag = AST }

		SOL_grant_patron_standing = yes
		if = {
			limit = { SOL = { check_variable = { SOL_comp_tier > 2 } } }
			add_ideas = SOL_AST_pacific_security_partner
		}
		else = {
			add_ideas = SOL_AST_pacific_partnership_accord
		}

		add_opinion_modifier = {
			target = SOL
			modifier = SOL_pacific_partner
		}
		SOL = {
			add_opinion_modifier = {
				target = AST
				modifier = SOL_pacific_partner
			}
			add_opinion_modifier = {
				target = USA
				modifier = SOL_abandoned_us
			}
		}
	}

	option = {
		name = solomon_competition.54.o2 # The chain is closed without a shot
		log = "[GetDateText]: [This.GetName]: solomon_competition.54.o2 executed"
		trigger = { original_tag = CHI }

		add_political_power = 40
		add_to_variable = { CHI_bri_pacific_engagement = 5 tooltip = CHI_bri_pacific_engagement_tt }
		add_opinion_modifier = {
			target = AST
			modifier = SOL_pacific_partner
		}
	}

	option = {
		name = solomon_competition.54.o3 # We have been shut out of the Pacific
		log = "[GetDateText]: [This.GetName]: solomon_competition.54.o3 executed"
		trigger = { original_tag = USA }

		add_political_power = -60
		add_stability = -0.03
		add_opinion_modifier = {
			target = AST
			modifier = SOL_abandoned_us
		}
	}
}

country_event = { # Reversed competition - Washington holds the Solomons
	id = solomon_competition.55
	title = solomon_competition.55.t
	desc = {
		text = solomon_competition.55.d1
		trigger = { original_tag = USA }
	}
	desc = {
		text = solomon_competition.55.d2
		trigger = { original_tag = AST }
	}
	desc = {
		text = solomon_competition.55.d3
		trigger = { original_tag = CHI }
	}
	picture = GFX_political_deal

	is_triggered_only = yes
	fire_only_once = yes

	option = {
		name = solomon_competition.55.o1 # The compact holds
		log = "[GetDateText]: [This.GetName]: solomon_competition.55.o1 executed"
		trigger = { original_tag = USA }

		# A patron-tier result gets the full security partnership; anything less
		# holds the ground without the architecture that goes on top of it.
		SOL_grant_patron_standing = yes
		if = {
			limit = { SOL = { check_variable = { SOL_comp_tier > 2 } } }
			add_ideas = SOL_USA_pacific_security_partner
		}
		else = {
			add_ideas = SOL_USA_pacific_partnership_accord
		}

		add_opinion_modifier = {
			target = SOL
			modifier = SOL_pacific_partner
		}
		SOL = {
			add_opinion_modifier = {
				target = USA
				modifier = SOL_pacific_partner
			}
			add_opinion_modifier = {
				target = AST
				modifier = SOL_abandoned_us
			}
		}
	}

	option = {
		name = solomon_competition.55.o2 # Locked out of our own near region
		log = "[GetDateText]: [This.GetName]: solomon_competition.55.o2 executed"
		trigger = { original_tag = AST }

		add_timed_idea = { idea = SOL_AST_lost_the_near_region days = 1825 }
		add_political_power = -50
		add_stability = -0.03
		add_opinion_modifier = {
			target = USA
			modifier = SOL_abandoned_us
		}
	}

	option = {
		name = solomon_competition.55.o3 # Our partner could not deliver
		log = "[GetDateText]: [This.GetName]: solomon_competition.55.o3 executed"
		trigger = { original_tag = CHI }

		add_political_power = -25
	}
}

########################################################################
# After the settlement - holding what was won
########################################################################

country_event = { # The patron's standing in Honiara is tested
	id = solomon_competition.60
	title = solomon_competition.60.t
	desc = solomon_competition.60.d
	picture = GFX_report_event_generic_1

	is_triggered_only = yes

	immediate = {
		clr_country_flag = SOL_patron_test_pending
	}

	option = {
		name = solomon_competition.60.o1 # Ask our patron for help
		log = "[GetDateText]: [This.GetName]: solomon_competition.60.o1 executed"
		# A contest that ended level, or one nobody funded past the first tier,
		# left Honiara without a patron to call. There is nobody on the other
		# end of this line, so the option is not offered.
		trigger = { has_country_flag = SOL_comp_has_patron }

		if = {
			limit = {
				country_exists = AST
				check_variable = { SOL_comp_patron = AST.id }
			}
			AST = { country_event = { id = solomon_competition.61 days = 3 } }
		}
		else_if = {
			limit = {
				country_exists = CHI
				check_variable = { SOL_comp_patron = CHI.id }
			}
			CHI = { country_event = { id = solomon_competition.61 days = 3 } }
		}
		else_if = {
			limit = {
				country_exists = USA
				check_variable = { SOL_comp_patron = USA.id }
			}
			USA = { country_event = { id = solomon_competition.61 days = 3 } }
		}
		ai_chance = { base = 8 }
	}

	option = {
		name = solomon_competition.60.o2 # Handle it ourselves
		log = "[GetDateText]: [This.GetName]: solomon_competition.60.o2 executed"

		add_political_power = -20
		add_stability = -0.02
		ai_chance = { base = 3 }
	}
}

country_event = { # Honiara calls in the relationship
	id = solomon_competition.61
	title = solomon_competition.61.t
	desc = solomon_competition.61.d
	picture = GFX_political_deal

	is_triggered_only = yes

	option = {
		name = solomon_competition.61.o1 # Stand by our partner
		log = "[GetDateText]: [This.GetName]: solomon_competition.61.o1 executed"

		set_temp_variable = { treasury_change = -25 }
		modify_treasury_effect = yes
		add_political_power = -20
		SOL = {
			add_stability = 0.04
			add_opinion_modifier = {
				target = ROOT
				modifier = SOL_pacific_partner
			}
		}
		set_temp_variable = { percent_change = 4 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = {
			base = 8
			modifier = {
				factor = 0
				check_variable = { treasury < 25 }
			}
		}
	}

	option = {
		name = solomon_competition.61.o2 # We have other priorities
		log = "[GetDateText]: [This.GetName]: solomon_competition.61.o2 executed"

		add_political_power = 15
		SOL = {
			add_stability = -0.03
			add_opinion_modifier = {
				target = ROOT
				modifier = SOL_abandoned_us
			}
		}
		set_temp_variable = { percent_change = -6 }
		set_temp_variable = { tag_index = ROOT.id }
		set_temp_variable = { influence_target = SOL.id }
		change_influence_percentage = yes

		ai_chance = { base = 2 }
	}
}