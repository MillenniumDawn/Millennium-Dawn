scripted_gui = {
	companies_gui = {
		window_name = "companies_gui"
		context_type = player_context
		parent_window_token = top_bar

		visible = {
			has_country_flag = Company_open_gui
		}

		effects = {
			close_button_click = {
				clr_country_flag = Company_open_gui }

			trait1_button_click = {
				clr_country_flag = Company_open_gui }
			}
		}

		triggers = {
		}

		dynamic_lists = {
		}

		properties = {
		}

		properties = {
		}

		ai_enabled = {
			always = no
		}

		ai_test_interval = 290
		ai_test_variance = 0.5

		ai_check = {
		}

		ai_check_scope = {
		}

		ai_weights = {
		}
	}

	company_view = {
        window_name = "companies_open_gui"
        context_type = player_context
        parent_window_token = top_bar
        dirty = ROOT.refresh_AGRI

        visible = { always = yes }

        effects = {
            companies_open_gui_button_click = {
                if = { limit = { NOT = { has_country_flag = Company_open_gui } } set_country_flag = Company_open_gui }
                else = { clr_country_flag = Company_open_gui }
            }
        }
    }
}