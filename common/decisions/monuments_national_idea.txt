national_idea_category = {

	ger_nationalism = {

		allowed = {
 			original_tag = GER
			is_in_array = { ruling_party = 21 }
			has_idea = national_idea_GER_nationalism
		}

		available = {
			always = no
		}

		visible = {
			always = no
		}

		activation = {
 			original_tag = GER
			is_in_array = { ruling_party = 21 }
			has_idea = national_idea_GER_nationalism
		}

		days_mission_timeout = 0
		selectable_mission = yes
		complete_effect = {
		}
		timeout_effect = {
				remove_ideas = national_idea_GER
				add_ideas = national_idea_GER_nationalism
			}
		}

	ger_no_nationalism = {

		allowed = {
 			original_tag = GER
			NOT = { is_in_array = { ruling_party = 21 } }
			has_idea = national_idea_GER_nationalism
		}

		available = {
			always = no
		}

		visible = {
			always = no
		}

		activation = {
 			original_tag = GER
			NOT = { is_in_array = { ruling_party = 21 } }
			has_idea = national_idea_GER_nationalism
		}

		days_mission_timeout = 0
		selectable_mission = yes
		complete_effect = {
		}
		timeout_effect = {
				remove_ideas = national_idea_GER_nationalism
				add_ideas = national_idea_GER
			}
		}

	spr_communism = {

		allowed = {
 			original_tag = SPR
			has_government = communism
			has_idea = national_idea_SPR
		}

		available = {
			always = no
		}

		visible = {
			always = no
		}

		activation = {
 			original_tag = SPR
			has_government = communism
			has_idea = national_idea_SPR
		}

		days_mission_timeout = 0
		selectable_mission = yes
		complete_effect = {
		}
		timeout_effect = {
				remove_ideas = national_idea_SPR
				add_ideas = national_idea_SPR_communism
			}
		}

	spr_no_communism = {

		allowed = {
 			original_tag = SPR
			NOT = { has_government = communism }
			has_idea = national_idea_SPR_communism
		}

		available = {
			always = no
		}

		visible = {
			always = no
		}

		activation = {
 			original_tag = SPR
			NOT = { has_government = communism }
			has_idea = national_idea_SPR_communism
		}

		days_mission_timeout = 0
		selectable_mission = yes
		complete_effect = {
		}
		timeout_effect = {
				remove_ideas = national_idea_SPR_communism
				add_ideas = national_idea_SPR
			}
		}

	prc_no_communism = {

		allowed = {
 			original_tag = CHI
			NOT = { has_idea = law_socialist_state }
			has_idea = national_idea_PRC
		}

		available = {
			always = no
		}

		visible = {
			always = no
		}

		activation = {
 			original_tag = CHI
			NOT = { has_idea = law_socialist_state }
			has_idea = national_idea_PRC
		}

		days_mission_timeout = 0
		selectable_mission = yes
		complete_effect = {
		}
		timeout_effect = {
				remove_ideas = national_idea_PRC
			}
		}

	chi_nationalism = {

		allowed = {
			OR = {
 				original_tag = CHI
 				original_tag = TAI
			}
			has_idea = law_absolute_monarchy
			OR = {
 				has_idea = national_idea_PRC
 				has_idea = national_idea_CHI
			}
		}

		available = {
			always = no
		}

		visible = {
			always = no
		}

		activation = {
			OR = {
 				original_tag = CHI
 				original_tag = TAI
			}
			has_idea = law_absolute_monarchy
			OR = {
 				has_idea = national_idea_PRC
 				has_idea = national_idea_CHI
			}
		}

		days_mission_timeout = 0
		selectable_mission = yes
		complete_effect = {
		}
		timeout_effect = {
				remove_ideas = national_idea_PRC
				remove_ideas = national_idea_CHI
				add_ideas = national_idea_CHI_nationalist
			}
		}

	chi_no_nationalism_prc = {

		allowed = {
 			original_tag = CHI
			NOT = { has_idea = law_absolute_monarchy }
 			has_idea = national_idea_CHI_nationalist
		}

		available = {
			always = no
		}

		visible = {
			always = no
		}

		activation = {
 			original_tag = CHI
			NOT = { has_idea = law_absolute_monarchy }
 			has_idea = national_idea_CHI_nationalist
		}

		days_mission_timeout = 0
		selectable_mission = yes
		complete_effect = {
		}
		timeout_effect = {
				remove_ideas = national_idea_CHI_nationalist
				add_ideas = national_idea_PRC
			}
		}

	chi_no_nationalism_chi = {

		allowed = {
 			original_tag = TAI
			NOT = { has_idea = law_absolute_monarchy }
 			has_idea = national_idea_CHI_nationalist
		}

		available = {
			always = no
		}

		visible = {
			always = no
		}

		activation = {
 			original_tag = TAI
			NOT = { has_idea = law_absolute_monarchy }
 			has_idea = national_idea_CHI_nationalist
		}

		days_mission_timeout = 0
		selectable_mission = yes
		complete_effect = {
		}
		timeout_effect = {
				remove_ideas = national_idea_CHI_nationalist
				add_ideas = national_idea_CHI
			}
		}

}