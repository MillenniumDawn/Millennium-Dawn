MER_mercosur_diplomacy = {
	MER_invite_CHL_associate = {
		icon = GFX_decision_generic_diplomacy

		cost = 75
		days_remove = 30

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_associate_invites_unlocked
			CHL = {
				exists = yes
				NOT = { tag = ROOT }
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			CHL = {
				exists = yes
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.01 days = 0 }
			CHL = { country_event = { id = mercosur.02 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_CHL_full_member = {

		cost = 100
		days_remove = 45

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_full_member_invites_unlocked
			CHL = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			CHL = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.05 days = 0 }
			CHL = { country_event = { id = mercosur.06 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_PRU_associate = {
		icon = GFX_decision_generic_diplomacy

		cost = 75
		days_remove = 30

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_associate_invites_unlocked
			PRU = {
				exists = yes
				NOT = { tag = ROOT }
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			PRU = {
				exists = yes
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.09 days = 0 }
			PRU = { country_event = { id = mercosur.02 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_PRU_full_member = {

		cost = 100
		days_remove = 45

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_full_member_invites_unlocked
			PRU = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			PRU = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.18 days = 0 }
			PRU = { country_event = { id = mercosur.06 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_COL_associate = {
		icon = GFX_decision_generic_diplomacy

		cost = 75
		days_remove = 30

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_associate_invites_unlocked
			COL = {
				exists = yes
				NOT = { tag = ROOT }
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			COL = {
				exists = yes
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.10 days = 0 }
			COL = { country_event = { id = mercosur.02 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_COL_full_member = {

		cost = 100
		days_remove = 45

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_full_member_invites_unlocked
			COL = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			COL = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.19 days = 0 }
			COL = { country_event = { id = mercosur.06 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_ECU_associate = {
		icon = GFX_decision_generic_diplomacy

		cost = 75
		days_remove = 30

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_associate_invites_unlocked
			ECU = {
				exists = yes
				NOT = { tag = ROOT }
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			ECU = {
				exists = yes
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.11 days = 0 }
			ECU = { country_event = { id = mercosur.02 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_ECU_full_member = {

		cost = 100
		days_remove = 45

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_full_member_invites_unlocked
			ECU = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			ECU = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.20 days = 0 }
			ECU = { country_event = { id = mercosur.06 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_VEN_associate = {
		icon = GFX_decision_generic_diplomacy

		cost = 75
		days_remove = 30

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_associate_invites_unlocked
			VEN = {
				exists = yes
				NOT = { tag = ROOT }
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			VEN = {
				exists = yes
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.12 days = 0 }
			VEN = { country_event = { id = mercosur.02 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_VEN_full_member = {

		cost = 100
		days_remove = 45

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_full_member_invites_unlocked
			VEN = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			VEN = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.21 days = 0 }
			VEN = { country_event = { id = mercosur.06 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_BOL_associate = {
		icon = GFX_decision_generic_diplomacy

		cost = 75
		days_remove = 30

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_associate_invites_unlocked
			BOL = {
				exists = yes
				NOT = { tag = ROOT }
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			BOL = {
				exists = yes
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.13 days = 0 }
			BOL = { country_event = { id = mercosur.02 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_BOL_full_member = {

		cost = 100
		days_remove = 45

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_full_member_invites_unlocked
			BOL = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			BOL = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.22 days = 0 }
			BOL = { country_event = { id = mercosur.06 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_GUY_associate = {
		icon = GFX_decision_generic_diplomacy

		cost = 75
		days_remove = 30

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_associate_invites_unlocked
			GUY = {
				exists = yes
				NOT = { tag = ROOT }
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			GUY = {
				exists = yes
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.14 days = 0 }
			GUY = { country_event = { id = mercosur.02 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_GUY_full_member = {

		cost = 100
		days_remove = 45

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_full_member_invites_unlocked
			GUY = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			GUY = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.23 days = 0 }
			GUY = { country_event = { id = mercosur.06 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_SUR_associate = {
		icon = GFX_decision_generic_diplomacy

		cost = 75
		days_remove = 30

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_associate_invites_unlocked
			SUR = {
				exists = yes
				NOT = { tag = ROOT }
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			SUR = {
				exists = yes
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.15 days = 0 }
			SUR = { country_event = { id = mercosur.02 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_SUR_full_member = {

		cost = 100
		days_remove = 45

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_full_member_invites_unlocked
			SUR = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			SUR = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.24 days = 0 }
			SUR = { country_event = { id = mercosur.06 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_MEX_associate = {
		icon = GFX_decision_generic_diplomacy

		cost = 75
		days_remove = 30

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_global_flag = mercosur_latin_america_invites_unlocked
			MEX = {
				exists = yes
				NOT = { tag = ROOT }
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_global_flag = mercosur_latin_america_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			MEX = {
				exists = yes
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.16 days = 0 }
			MEX = { country_event = { id = mercosur.02 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_MEX_full_member = {

		cost = 100
		days_remove = 45

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_global_flag = mercosur_latin_america_invites_unlocked
			MEX = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_global_flag = mercosur_latin_america_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			MEX = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.25 days = 0 }
			MEX = { country_event = { id = mercosur.06 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_PAN_associate = {
		icon = GFX_decision_generic_diplomacy

		cost = 75
		days_remove = 30

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_global_flag = mercosur_latin_america_invites_unlocked
			PAN = {
				exists = yes
				NOT = { tag = ROOT }
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_associate_invites_unlocked
			has_global_flag = mercosur_latin_america_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			PAN = {
				exists = yes
				NOT = { has_idea = BRA_idea_associate_states }
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.17 days = 0 }
			PAN = { country_event = { id = mercosur.02 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}

	MER_invite_PAN_full_member = {

		cost = 100
		days_remove = 45

		days_re_enable = 180

		visible = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_global_flag = mercosur_latin_america_invites_unlocked
			PAN = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
			}
		}

		available = {
			has_global_flag = mercosur_full_member_invites_unlocked
			has_global_flag = mercosur_latin_america_invites_unlocked
			has_country_flag = mercosur_joint_tree_member
			is_subject = no
			has_at_least_regional_power_status = yes
			PAN = {
				exists = yes
				has_idea = BRA_idea_associate_states
				NOT = {
					OR = {
						has_idea = BRA_idea_mercosur_member_state
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_before_education
						has_idea = BRA_idea_mercosur_member_state_trade_agreement_after_education
					}
				}
				is_subject = no
			}
		}

		remove_effect = {
			country_event = { id = mercosur.26 days = 0 }
			PAN = { country_event = { id = mercosur.06 days = 1 } }
		}

		ai_will_do = {
			base = 1
		}
	}
}
