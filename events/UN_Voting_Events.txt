add_namespace = UN
add_namespace = UNSC

#1-100 series events - events for voting
#200 series events - events for swaying nations during

#event to scope SC votes
country_event = {
	id = UNSC.100

	is_triggered_only = yes
	hidden = yes

	immediate = {
		random_scope_in_array = {
			array = global.security_council_members
			set_global_flag = current_ongoing_sc_vote
			set_country_flag = has_sc_mission
			activate_mission = UN_SC_voting_mission
		}
		for_each_loop = {
			array = global.security_council_members
			value = v

			var:v = {
				meta_effect = {
					text = {
						if = {
							limit = {
								NOT = { has_country_flag = auto_accept_all_sc_votes }
							}
							country_event = UNSC.[ID]
						} else = {
							add_to_array = { global.security_council_vote_yes = THIS }
						}
					}
					ID = "[?global.current_sc_vote_type]"
				}
				if = {
					limit ={
						NOT = { has_country_flag = has_sc_mission }
					}
					activate_mission = UN_SC_voting_mission_result_timer
				}
			}
		}
	}
}
#event for SC votes
#Pressure to end war
country_event = {
	id = UNSC.1
	title = UNSC.1.t
	desc = UNSC.1.d
	picture = GFX_UN_gen_assembly

	is_triggered_only = yes
	timeout_days = 4

	#Abstain
	option = {
		name = UNSC.1.c
		add_to_array = { global.security_council_vote_abstain = THIS }
		log = "[GetDateText]: [THIS.GetName] has abstained from voting"

		missile_ui_update = yes
		ai_chance = {
			base = 0
			modifier = {
				add = 5
				has_opinion = {
					target = FROM
					value < 5
				}
				has_opinion = {
					target = FROM
					value > -5
				}
			}
			modifier = {
				add = 1000
				has_war_with = FROM
			}
		}
	}
	#Yes
	option = {
		name = UNSC.1.a
		log = "[GetDateText]: [THIS.GetName] has voted yes"
		add_to_array = { global.security_council_vote_yes = THIS }

		missile_ui_update = yes

		ai_chance = {
			base = 0
			modifier = {
				add = 20
				has_opinion = {
					target = FROM
					value < -5
				}
			}
			modifier = {
				add = 20
				is_embargoed_by = FROM
			}
			modifier = {
				add = 30
				is_neighbor_of = FROM
			}
			modifier = {
				add = 50
				is_in_array = { potential_and_current_enemies = FROM }
			}
		}
	}
	#No
	option = {
		name = UNSC.1.b
		add_to_array = { global.security_council_vote_no = THIS }
		log = "[GetDateText]: [THIS.GetName] has voted no"
		if = {
			limit = {
				is_in_array = { global.p5_sc_members = THIS }
			}
			set_global_flag = security_council_veto
		}

		missile_ui_update = yes

		ai_chance = {
			base = 0

			modifier = {
				add = 10
				is_ally_with = FROM
			}

			modifier = {
				add = 10
				is_in_faction_with = FROM
			}

			modifier = {
				add = 10
				FROM = {
					has_same_ideology = yes
				}
			}

			modifier = {
				add = 20
				has_opinion = {
					target = FROM
					value > 5
				}
			}

			modifier = {
				add = 20
				has_war = yes
			}

			modifier = {
				add = 200
				is_puppet_of = FROM
			}

			modifier = {
				add = 20
				is_guaranteed_by = FROM
			}

			modifier = {
				add = 10
				has_non_aggression_pact_with = FROM
			}

			modifier = {
				add = 10
				has_guaranteed = FROM
			}
		}
	}
}
#Sanction for tension
country_event = {
	id = UNSC.2
	title = UNSC.2.t
	desc = UNSC.2.d
	picture = GFX_UN_gen_assembly

	is_triggered_only = yes
	timeout_days = 4

	#Abstain
	option = {
		name = UNSC.2.c
		add_to_array = { global.security_council_vote_abstain = THIS }
		log = "[GetDateText]: [THIS.GetName] has abstained from voting"

		missile_ui_update = yes
		ai_chance = {
			base = 0
			modifier = {
				add = 5
				has_opinion = {
					target = FROM
					value < 20
				}
				has_opinion = {
					target = FROM
					value > -20
				}
			}
			modifier = {
				add = 1000
				has_war_with = FROM
			}
		}
	}
	#Yes
	option = {
		name = UNSC.2.a
		log = "[GetDateText]: [THIS.GetName] has voted yes"
		add_to_array = { global.security_council_vote_yes = THIS }

		missile_ui_update = yes

		ai_chance = {
			base = 0
			modifier = {
				add = 20
				has_opinion = {
					target = FROM
					value < -20
				}
			}
			modifier = {
				add = 20
				is_embargoed_by = FROM
			}
			modifier = {
				add = 30
				is_neighbor_of = FROM
			}
			modifier = {
				add = 50
				is_in_array = { potential_and_current_enemies = FROM }
			}
		}
	}
	#No
	option = {
		name = UNSC.2.b
		add_to_array = { global.security_council_vote_no = THIS }
		log = "[GetDateText]: [THIS.GetName] has voted no"
		if = {
			limit = {
				is_in_array = { global.p5_sc_members = THIS }
			}
			set_global_flag = security_council_veto
		}

		missile_ui_update = yes

		ai_chance = {
			base = 0

			modifier = {
				add = 10
				is_ally_with = FROM
			}

			modifier = {
				add = 10
				is_in_faction_with = FROM
			}

			modifier = {
				add = 10
				FROM = {
					has_same_ideology = yes
				}
			}

			modifier = {
				add = 20
				has_opinion = {
					target = FROM
					value > 20
				}
			}

			modifier = {
				add = 20
				has_war = yes
			}

			modifier = {
				add = 200
				is_puppet_of = FROM
			}

			modifier = {
				add = 20
				is_guaranteed_by = FROM
			}

			modifier = {
				add = 10
				has_non_aggression_pact_with = FROM
			}

			modifier = {
				add = 10
				has_guaranteed = FROM
			}
		}
	}
}
#Regulate volunteers
country_event = {
	id = UNSC.3
	title = UNSC.3.t
	desc = UNSC.3.d
	picture = GFX_UN_gen_assembly

	is_triggered_only = yes
	timeout_days = 4

	#Abstain
	option = {
		name = UNSC.3.c
		add_to_array = { global.security_council_vote_abstain = THIS }
		log = "[GetDateText]: [THIS.GetName] has abstained from voting"

		missile_ui_update = yes
		ai_chance = {
			base = 0
			modifier = {
				add = 5
				has_opinion = {
					target = FROM
					value < 20
				}
				has_opinion = {
					target = FROM
					value > -20
				}
			}
			modifier = {
				add = 1000
				has_war_with = FROM
			}
		}
	}
	#Yes
	option = {
		name = UNSC.3.a
		log = "[GetDateText]: [THIS.GetName] has voted yes"
		add_to_array = { global.security_council_vote_yes = THIS }

		missile_ui_update = yes

		ai_chance = {
			base = 0
			modifier = {
				add = 20
				has_opinion = {
					target = FROM
					value < -20
				}
			}
			modifier = {
				add = 20
				is_embargoed_by = FROM
			}
			modifier = {
				add = 30
				is_neighbor_of = FROM
			}
			modifier = {
				add = 50
				is_in_array = { potential_and_current_enemies = FROM }
			}
		}
	}
	#No
	option = {
		name = UNSC.3.b
		add_to_array = { global.security_council_vote_no = THIS }
		log = "[GetDateText]: [THIS.GetName] has voted no"
		if = {
			limit = {
				is_in_array = { global.p5_sc_members = THIS }
			}
			set_global_flag = security_council_veto
		}

		missile_ui_update = yes

		ai_chance = {
			base = 0

			modifier = {
				add = 10
				is_ally_with = FROM
			}

			modifier = {
				add = 10
				is_in_faction_with = FROM
			}

			modifier = {
				add = 10
				FROM = {
					has_same_ideology = yes
				}
			}

			modifier = {
				add = 20
				has_opinion = {
					target = FROM
					value > 20
				}
			}

			modifier = {
				add = 20
				has_war = yes
			}

			modifier = {
				add = 200
				is_puppet_of = FROM
			}

			modifier = {
				add = 20
				is_guaranteed_by = FROM
			}

			modifier = {
				add = 10
				has_non_aggression_pact_with = FROM
			}

			modifier = {
				add = 10
				has_guaranteed = FROM
			}
		}
	}
}
#Arms embargo
country_event = {
	id = UNSC.4
	title = UNSC.4.t
	desc = UNSC.4.d
	picture = GFX_UN_gen_assembly
	timeout_days = 4

	is_triggered_only = yes

	#Abstain
	option = {
		name = UNSC.4.c
		add_to_array = { global.security_council_vote_abstain = THIS }
		log = "[GetDateText]: [THIS.GetName] has abstained from voting"

		missile_ui_update = yes
		ai_chance = {
			base = 0
			modifier = {
				add = 5
				has_opinion = {
					target = FROM
					value < 20
				}
				has_opinion = {
					target = FROM
					value > -20
				}
			}
			modifier = {
				add = 1000
				has_war_with = FROM
			}
		}
	}
	#Yes
	option = {
		name = UNSC.4.a
		log = "[GetDateText]: [THIS.GetName] has voted yes"
		add_to_array = { global.security_council_vote_yes = THIS }

		missile_ui_update = yes

		ai_chance = {
			base = 0
			modifier = {
				add = 20
				has_opinion = {
					target = FROM
					value < -20
				}
			}
			modifier = {
				add = 20
				is_embargoed_by = FROM
			}
			modifier = {
				add = 30
				is_neighbor_of = FROM
			}
			modifier = {
				add = 50
				is_in_array = { potential_and_current_enemies = FROM }
			}
		}
	}
	#No
	option = {
		name = UNSC.4.b
		add_to_array = { global.security_council_vote_no = THIS }
		log = "[GetDateText]: [THIS.GetName] has voted no"
		if = {
			limit = {
				is_in_array = { global.p5_sc_members = THIS }
			}
			set_global_flag = security_council_veto
		}

		missile_ui_update = yes

		ai_chance = {
			base = 0

			modifier = {
				add = 10
				is_ally_with = FROM
			}

			modifier = {
				add = 10
				is_in_faction_with = FROM
			}

			modifier = {
				add = 10
				FROM = {
					has_same_ideology = yes
				}
			}

			modifier = {
				add = 20
				has_opinion = {
					target = FROM
					value > 20
				}
			}

			modifier = {
				add = 20
				has_war = yes
			}

			modifier = {
				add = 200
				is_puppet_of = FROM
			}

			modifier = {
				add = 20
				is_guaranteed_by = FROM
			}

			modifier = {
				add = 10
				has_non_aggression_pact_with = FROM
			}

			modifier = {
				add = 10
				has_guaranteed = FROM
			}
		}
	}
}

#event to scope GA votes
country_event = {
	id = UN.100

	is_triggered_only = yes
	hidden = yes

	immediate = {
		random_scope_in_array = {
			array = global.UN_general_assembly
			set_global_flag = current_ongoing_ga_vote
			set_country_flag = has_ga_mission
			activate_mission = UN_GA_voting_mission
		}
		for_each_loop = {
			array = global.UN_general_assembly
			value = v

			var:v = {
				if = {
					limit = {
						NOT = { has_country_flag = auto_accept_all_ga_votes has_country_flag = auto_reject_all_ga_votes }
					}
					meta_effect = {
						text = {
							country_event = UN.[ID]
						}
						ID = "[?global.current_ga_vote_type]"
					}
				}else = {
					if = {
						limit = {
							has_country_flag = auto_accept_all_ga_votes
						}
						add_to_array = { global.general_assembly_vote_yes = THIS }
					}else_if = {
						limit = {
							has_country_flag = auto_reject_all_ga_votes
						}
						add_to_array = { global.general_assembly_vote_no = THIS }
					}
				}
				if = {
					limit ={
						NOT = { has_country_flag = has_ga_mission }
					}
					activate_mission = UN_GA_voting_mission_result_timer
				}
			}
		}
	}
}
#events for GA votes
#security council seat confirmation event
country_event = {
	id = UN.6

	is_triggered_only = yes

	title = UN.6.t
	desc = UN.6.d
	picture = GFX_UN_gen_assembly

	is_triggered_only = yes
	timeout_days = 4

	#Abstain
	option = {
		name = UN.6.c
		add_to_array = { global.general_assembly_vote_abstain = THIS }
		log = "[GetDateText]: [THIS.GetName] has abstained from voting"

		missile_ui_update = yes
		ai_chance = {
			base = 0
			modifier = {
				add = 1000
				tag = FROM
			}
		}
	}
	#Yes
	option = {
		name = UN.6.a
		log = "[GetDateText]: [THIS.GetName] has voted yes"
		add_to_array = { global.general_assembly_vote_yes = THIS }

		missile_ui_update = yes

		ai_chance = {
			base = 1
			modifier = {
				add = 200
				is_puppet_of = FROM
			}
			modifier = {
				add = 10
				is_ally_with = FROM
			}
			modifier = {
				add = 10
				is_in_faction_with = FROM
			}
			modifier = {
				add = 20
				is_guaranteed_by = FROM
			}
			modifier = {
				add = 10
				has_opinion = {
					target = FROM
					value > -1
				}
			}
			modifier = {
				add = 10
				is_on_same_continent_as = FROM
				has_opinion = {
					target = FROM
					value > -1
				}
			}
		}
	}
	#No
	option = {
		name = UN.6.b
		add_to_array = { global.general_assembly_vote_no = THIS } #testing, vote no after test
		log = "[GetDateText]: [THIS.GetName] has voted no"

		missile_ui_update = yes

		ai_chance = {
			base = 0
			modifier = {
				add = -10
				has_non_aggression_pact_with = FROM
			}
			modifier = {
				add = 50
				is_in_array = { potential_and_current_enemies = FROM }
			}
			modifier = {
				add = 50
				FROM = {
					has_war = yes
				}
			}
			modifier = {
				add = 10
				has_opinion = {
					target = FROM
					value < 0
				}
				FROM = {
					has_same_ideology = yes
				}
			}
			modifier = {
				add = 10
				is_on_same_continent_as = FROM
				has_opinion = {
					target = FROM
					value < 0
				}
			}
			modifier = {
				add = 20
				any_of_scopes = {
					array = allies
					has_opinion = {
						target = FROM
						value < -10
					}
				}
			}
			modifier = {
				add = 10
				has_opinion = {
					target = FROM
					value < -11
				}
			}
			modifier = {
				add = 100
				has_war_with = FROM
			}
		}
	}
}
#raid non-binding sanctions event
country_event = {
	id = UN.7

	is_triggered_only = yes

	title = UN.7.t
	desc = UN.7.d
	picture = GFX_UN_gen_assembly

	is_triggered_only = yes
	timeout_days = 4

	#Abstain
	option = {
		name = UN.7.c
		add_to_array = { global.general_assembly_vote_abstain = THIS }
		log = "[GetDateText]: [THIS.GetName] has abstained from voting"

		missile_ui_update = yes
		ai_chance = {
			base = 0
		}
	}
	#Yes
	option = {
		name = UN.7.a
		log = "[GetDateText]: [THIS.GetName] has voted yes"
		add_to_array = { global.general_assembly_vote_yes = THIS }

		missile_ui_update = yes

		ai_chance = {
			base = 0
			modifier = {
				add = 500
				has_war_with = FROM
			}
			modifier = {
				add = 10
				is_embargoed_by = FROM
			}
			modifier = {
				add = 10
				has_opinion = {
					target = FROM
					value < 0
				}
				FROM = {
					has_same_ideology = yes
				}
			}
			modifier = {
				add = 10
				has_opinion = {
					target = FROM
					value < -10
				}
			}
		}
	}
	#No
	option = {
		name = UN.7.b
		add_to_array = { global.general_assembly_vote_no = THIS } #testing, vote no after test
		log = "[GetDateText]: [THIS.GetName] has voted no"

		missile_ui_update = yes

		ai_chance = {
			base = 0
			modifier = {
				add = 500
				is_puppet_of = FROM
			}
			modifier = {
				add = 100
				is_guaranteed_by = FROM
			}
			modifier = {
				add = 10
				has_war = yes
			}
			modifier = {
				add = 10
				has_opinion = {
					target = FROM
					value > -1
				}
			}
		}
	}
}

#Sway events
#UNSC sway scoping event
#Offer shit in here to get people to do things
#We need a list: money, power, ect?
country_event = {
	id = UNSC.200
	is_triggered_only = yes

	title = UNSC.200.t
	desc = UNSC.200.d

	option = {
		name = UNSC.200.a
		var:sway_target = {
			country_event = UNSC.201
			set_variable = { current_sway_direction = ROOT.current_vote_sway }
		}
		custom_effect_tooltip = un_change_vote_too_offer_1
		clear_variable = sway_target
		clear_variable = current_vote_sway
		ai_chance = {
			base = 0
		}
	}
	option = {
		name = UNSC.200.b
		var:sway_target = {
			country_event = UNSC.202
			set_variable = { current_sway_direction = ROOT.current_vote_sway }
		}
		custom_effect_tooltip = un_change_vote_too_offer_2
		clear_variable = sway_target
		clear_variable = current_vote_sway
		ai_chance = {
			base = 0
		}
	}
	option = {
		name = UNSC.200.c
		var:sway_target = {
			country_event = UNSC.203
			set_variable = { current_sway_direction = ROOT.current_vote_sway }
		}
		custom_effect_tooltip = un_change_vote_too_offer_3
		clear_variable = sway_target
		clear_variable = current_vote_sway
		ai_chance = {
			base = 0
		}
	}
	option = {
		name = UNSC.200.e

		trigger = {
			NOT = { has_idea = un_vote_sway_cost_4 }
		}
		custom_effect_tooltip = un_change_vote_too_offer_4

		var:sway_target = {
			country_event = UNSC.204
			set_variable = { current_sway_direction = ROOT.current_vote_sway }
			custom_effect_tooltip = nation_will_receive_tt
			effect_tooltip = {
				add_ideas = un_vote_sway_assistance
			}
		}
		custom_effect_tooltip = nation_will_receive_tt
		effect_tooltip = {
			add_ideas = un_vote_sway_Cost
		}
		clear_variable = sway_target
		clear_variable = current_vote_sway
		ai_chance = {
			base = 1000
		}
	}
}
#Sway Event
#Nations should be more willing to abstain, rather than fully swap their vote
#IE, if you want a full swap, make a much stronger offer
#The more nations don't like one another, the harder the swaying should get
country_event = {
	id = UNSC.201
	is_triggered_only = yes

	title = UNSC.201.t
	desc = UNSC.201.d

	timeout_days = 2

	option = {
		name = UNSC.201.b

		clear_variable = current_sway_direction

		FROM = {
			add_opinion_modifier = {
				target = PREV
				modifier = rejected_our_vote_request
			}
		}

		if = {
			limit = {
				sender_influence_higher_50 = yes
			}
			add_political_power = -250
		}

		ai_chance = {
			base = 0
			modifier = {
				add = 5
				OR = {
					check_variable = { current_sway_direction = 1 }
					check_variable = { current_sway_direction = 2 }
				}
			}
			modifier = {
				add = 5
				sender_influence_higher_50 = no
			}
			modifier = {
				add = 5
				has_country_flag = trade_agreement@FROM
			}
			modifier = {
				add = 5
				has_country_flag = mutual_investment_treaty_@FROM
			}
			modifier = {
				add = 5
				NOT = { is_embargoed_by = FROM }
			}
			modifier = {
				add = 5
				has_opinion = {
					target = FROM
					value < 0
				}
			}
		}
	}

	option = {
		name = UNSC.201.a

		set_country_flag = has_had_current_un_sc_vote_swayed

		if = {
			limit = { is_in_array = { global.security_council_vote_yes = THIS } }
			remove_from_array = { global.security_council_vote_yes = THIS }
		}else_if = {
			limit = { is_in_array = { global.security_council_vote_no = THIS } }
			remove_from_array = { global.security_council_vote_no = THIS }
		}else = {
			remove_from_array = { global.security_council_vote_abstain = THIS }
		}

		if = {
			limit = { check_variable = { current_sway_direction = 1 } }
			add_to_array = { global.security_council_vote_yes = THIS }
		}else_if = {
			limit = { check_variable = { current_sway_direction = 2 } }
			add_to_array = { global.security_council_vote_no = THIS }
		}else = {
			add_to_array = { global.security_council_vote_abstain = THIS }
		}

		if = {
			limit = {
				has_dlc = "By Blood Alone"
				is_embargoed_by = FROM
			}
			FROM = {
				diplomatic_relation = {
					country = PREV
					relation = embargo
					active = no
				}
			}
		}

		if = {
			limit = {
				NOT = { has_country_flag = trade_agreement@FROM }
			}
			FROM = {
				set_country_flag = trade_agreement@PREV

				set_temp_variable = { percent_change = 1 }
				set_temp_variable = { tag_index = PREV.id }
				set_temp_variable = { influence_target = THIS.id }
				change_influence_percentage = yes
				add_to_variable = { signed_trade_agreements = 1 }
			}
			set_country_flag = trade_agreement@FROM

			add_opinion_modifier = { target = FROM modifier = mutual_trade_agreement }
			add_opinion_modifier = { target = FROM modifier = mutual_trade_opinion }
			reverse_add_opinion_modifier = { target = FROM modifier = mutual_trade_opinion }
			reverse_add_opinion_modifier = { target = FROM modifier = mutual_trade_agreement }
			set_temp_variable = { percent_change = 1 }
			set_temp_variable = { tag_index = FROM.id }
			set_temp_variable = { influence_target = THIS.id }
			change_influence_percentage = yes
			add_to_variable = { signed_trade_agreements = 1 }
		}

		if = {
			limit = {
				NOT = { has_country_flag = mutual_investment_treaty_@FROM }
			}
			FROM = {
				set_country_flag = mutual_investment_treaty_@PREV

				set_temp_variable = { percent_change = 1 }
				set_temp_variable = { tag_index = PREV.id }
				set_temp_variable = { influence_target = THIS.id }
				change_influence_percentage = yes

				# Permanent Investment Targets
				set_temp_variable = { target_nation = THIS.id }
				set_temp_variable = { adding_nation = PREV.id }
				change_permanent_investment_target = yes
			}

			set_country_flag = mutual_investment_treaty_@FROM

			add_opinion_modifier = { target = FROM modifier = mutual_investment_treaty_opinion }
			add_opinion_modifier = { target = FROM modifier = mutual_investment_treaty_trade_opinion }
			reverse_add_opinion_modifier = { target = FROM modifier = mutual_investment_treaty_opinion }
			reverse_add_opinion_modifier = { target = FROM modifier = mutual_investment_treaty_trade_opinion }
			set_temp_variable = { percent_change = 1 }
			set_temp_variable = { tag_index = FROM.id }
			set_temp_variable = { influence_target = THIS.id }
			change_influence_percentage = yes

			# Permanent Investment Targets
			set_temp_variable = { target_nation = THIS.id }
			set_temp_variable = { adding_nation = FROM.id }
			change_permanent_investment_target = yes
		}

		clear_variable = current_sway_direction

		missile_ui_update = yes

		#Apply the agreed upon deal here
		ai_chance = {
			base = 0
			modifier = {
				add = 5
				check_variable = { current_sway_direction = 3 }
			}
			modifier = {
				add = 15
				sender_influence_higher_50 = yes
			}
			modifier = {
				add = 5
				NOT = { has_country_flag = trade_agreement@FROM }
			}
			modifier = {
				add = 5
				NOT = { has_country_flag = mutual_investment_treaty_@FROM }
			}
			modifier = {
				add = 5
				is_embargoed_by = FROM
			}
			modifier = {
				add = 5
				has_opinion = {
					target = FROM
					value > 0
				}
			}
		}
	}
}
country_event = {
	id = UNSC.202
	is_triggered_only = yes

	title = UNSC.202.t
	desc = UNSC.202.d

	timeout_days = 2

	option = {
		name = UNSC.202.b

		clear_variable = current_sway_direction

		FROM = {
			add_opinion_modifier = {
				target = PREV
				modifier = rejected_our_vote_request
			}
		}

		if = {
			limit = {
				sender_influence_higher_50 = yes
			}
			add_political_power = -250
		}

		ai_chance = {
			base = 0
			modifier = {
				add = 5
				OR = {
					check_variable = { current_sway_direction = 1 }
					check_variable = { current_sway_direction = 2 }
				}
			}
			modifier = {
				add = 5
				sender_influence_higher_50 = no
			}
		}
	}

	option = {
		name = UNSC.202.a

		set_country_flag = has_had_current_un_sc_vote_swayed

		if = {
			limit = { is_in_array = { global.security_council_vote_yes = THIS } }
			remove_from_array = { global.security_council_vote_yes = THIS }
		}else_if = {
			limit = { is_in_array = { global.security_council_vote_no = THIS } }
			remove_from_array = { global.security_council_vote_no = THIS }
		}else = {
			remove_from_array = { global.security_council_vote_abstain = THIS }
		}

		if = {
			limit = { check_variable = { current_sway_direction = 1 } }
			add_to_array = { global.security_council_vote_yes = THIS }
		}else_if = {
			limit = { check_variable = { current_sway_direction = 2 } }
			add_to_array = { global.security_council_vote_no = THIS }
		}else = {
			add_to_array = { global.security_council_vote_abstain = THIS }
		}

		set_temp_variable = { gdp_value_tracker = gdp_total }
		multiply_temp_variable = { gdp_value_tracker = 0.0025 }
		set_temp_variable = { treasury_change = gdp_value_tracker }
		modify_treasury_effect = yes

		multiply_temp_variable = { treasury_change = -1 }

		FROM = {
			modify_treasury_effect = yes
		}

		clear_variable = current_sway_direction

		missile_ui_update = yes

		#Apply the agreed upon deal here
		ai_chance = {
			base = 0
			modifier = {
				add = 5
				check_variable = { current_sway_direction = 3 }
			}
			modifier = {
				add = 15
				sender_influence_higher_50 = yes
			}
		}
	}
}
country_event = {
	id = UNSC.203
	is_triggered_only = yes

	title = UNSC.203.t
	desc = UNSC.203.d

	timeout_days = 2

	option = {
		name = UNSC.203.b

		clear_variable = current_sway_direction

		FROM = {
			add_opinion_modifier = {
				target = PREV
				modifier = rejected_our_vote_request
			}
		}

		if = {
			limit = {
				sender_influence_higher_50 = yes
			}
			add_political_power = -250
		}

		ai_chance = {
			base = 0
			modifier = {
				add = 5
				OR = {
					check_variable = { current_sway_direction = 1 }
					check_variable = { current_sway_direction = 2 }
				}
			}
		}
	}

	option = {
		name = UNSC.203.a

		set_country_flag = has_had_current_un_sc_vote_swayed

		if = {
			limit = { is_in_array = { global.security_council_vote_yes = THIS } }
			remove_from_array = { global.security_council_vote_yes = THIS }
		}else_if = {
			limit = { is_in_array = { global.security_council_vote_no = THIS } }
			remove_from_array = { global.security_council_vote_no = THIS }
		}else = {
			remove_from_array = { global.security_council_vote_abstain = THIS }
		}

		if = {
			limit = { check_variable = { current_sway_direction = 1 } }
			add_to_array = { global.security_council_vote_yes = THIS }
		}else_if = {
			limit = { check_variable = { current_sway_direction = 2 } }
			add_to_array = { global.security_council_vote_no = THIS }
		}else = {
			add_to_array = { global.security_council_vote_abstain = THIS }
		}

		clear_variable = current_sway_direction

		missile_ui_update = yes

		if = {
			limit = {
				NOT = {
					has_military_access_to = FROM
				}
			}
			FROM = {
				give_military_access = PREV
			}
		}

		FROM = {
			diplomatic_relation = {
				country = PREV
				relation = OFFER_AIR_BASE_ACCESS
				active = yes
			}
		}

		full_satellite_system_access = yes

		#Apply the agreed upon deal here
		ai_chance = {
			base = 0
			modifier = {
				add = 5
				check_variable = { current_sway_direction = 3 }
			}
		}
	}
}
country_event = {
	id = UNSC.204
	is_triggered_only = yes

	title = UNSC.204.t
	desc = UNSC.204.d

	timeout_days = 2

	option = {
		name = UNSC.204.b

		clear_variable = current_sway_direction

		FROM = {
			add_opinion_modifier = {
				target = PREV
				modifier = rejected_our_vote_request
			}
		}

		if = {
			limit = {
				sender_influence_higher_50 = yes
			}
			add_political_power = -250
		}

		ai_chance = {
			base = 0
			modifier = {
				add = 5
				OR = {
					check_variable = { current_sway_direction = 1 }
					check_variable = { current_sway_direction = 2 }
				}
			}
		}
	}

	option = {
		name = UNSC.204.a

		set_country_flag = has_had_current_un_sc_vote_swayed

		if = {
			limit = { is_in_array = { global.security_council_vote_yes = THIS } }
			remove_from_array = { global.security_council_vote_yes = THIS }
		}else_if = {
			limit = { is_in_array = { global.security_council_vote_no = THIS } }
			remove_from_array = { global.security_council_vote_no = THIS }
		}else = {
			remove_from_array = { global.security_council_vote_abstain = THIS }
		}

		if = {
			limit = { check_variable = { current_sway_direction = 1 } }
			add_to_array = { global.security_council_vote_yes = THIS }
		}else_if = {
			limit = { check_variable = { current_sway_direction = 2 } }
			add_to_array = { global.security_council_vote_no = THIS }
		}else = {
			add_to_array = { global.security_council_vote_abstain = THIS }
		}

		clear_variable = current_sway_direction

		add_timed_idea = {
			idea = un_vote_sway_assistance
			days = 365
		}

		FROM = {
			if = {
				limit = {
					has_idea = un_vote_sway_cost
				}
				modify_timed_idea = {
					idea = un_vote_sway_cost
					days = 365
				}
			}else = {
				add_timed_idea = {
					idea = un_vote_sway_cost
					days = 365
				}
			}
		}

		missile_ui_update = yes

		#Apply the agreed upon deal here
		ai_chance = {
			base = 0
			modifier = {
				add = 5
				check_variable = { current_sway_direction = 3 }
			}
		}
	}
}
#UNGA sway scoping event
country_event = {
	id = UN.200

	is_triggered_only = yes

	title = UN.200.t
	desc = UN.200.d

	option = {
		name = UN.200.a
		var:sway_target = {
			country_event = UN.201
		}
	}
	option = {
		name = UN.200.b
		var:sway_target = {
			country_event = UN.201
		}
	}
	option = {
		name = UN.200.c
		var:sway_target = {
			country_event = UN.201
		}
	}
	option = {
		name = UN.200.d
		var:sway_target = {
			country_event = UN.201
		}
	}

	#Make offer here
}
#UNGA Sway Event
country_event = {
	id = UN.201
	is_triggered_only = yes

	title = UN.201.t
	desc = UN.201.d

	timeout_days = 2

	option = {
		name = UN.201.b

		#Apply penalties for refusing here

		ai_chance = {
			base = 0
			modifier = {
				add = 5
				FROM = {
					OR = {
						check_variable = { current_vote_sway = 1 }
						check_variable = { current_vote_sway = 2 }
					}
				}
			}
		}
	}

	option = {
		name = UN.201.a

		set_country_flag = has_had_current_un_sc_vote_swayed

		if = {
			limit = { is_in_array = { global.general_assembly_vote_yes = THIS } }
			remove_from_array = { global.general_assembly_vote_yes = THIS }
		}else_if = {
			limit = { is_in_array = { global.general_assembly_vote_no = THIS } }
			remove_from_array = { global.general_assembly_vote_no = THIS }
		}else = {
			remove_from_array = { global.general_assembly_vote_abstain = THIS }
		}

		if = {
			limit = { FROM = { check_variable = { current_vote_sway = 1 } } }
			add_to_array = { global.general_assembly_vote_yes = THIS }
		}else_if = {
			limit = { FROM = { check_variable = { current_vote_sway = 2 } } }
			add_to_array = { global.general_assembly_vote_no = THIS }
		}else = {
			add_to_array = { global.general_assembly_vote_abstain = THIS }
		}

		missile_ui_update = yes

		#Apply the agreed upon deal here
		ai_chance = {
			base = 0
			modifier = {
				add = 5
				FROM = {
					check_variable = { current_vote_sway = 3 }
				}
			}
		}
	}
}