# Author(s): Angriest Bird

# Information:
# The following set of modifiers for each internal faction are general purpose modifiers for interacting with the internal faction system
# for national content.
# -----
# Examples:
# if_<faction>_monthly_opinion - Improves a factions opinion per month by x amount (only works if the decay is enabled/disabled)
# if_<faction>_minimum_opinion - Changes the minimum amount of opinion a nation can have from the decay mechanic as well as the minimum opinion the faction can go to
# if_<faction>_maximum_opinion - Changes the maximum opinion for a faction

# Farmers
if_farmers_monthly_opinion = {
    color_type = good
	value_type = number
	precision = 2
	category = country
}
if_farmers_minimum_opinion = {
	color_type = good
	value_type = number
	precision = 2
	category = country
}
if_farmers_maximum_opinion = {
	color_type = good
	value_type = number
	precision = 2
	category = country
}