RAJ_PAK_border_conflict_category = {

	# --- TRAINING ---

	prepare_army_training = {
		desc = prepare_army_training_desc
		available = {
			NOT = { has_war_with = RAJ }
			NOT = { has_war_with = PAK }
			has_equipment = { infantry_weapons_type > 30 }
			has_equipment = { cnc_equipment_type > 7 }
		}
		days_remove = 15
		days_re_enable = 20
		fixed_random_seed = no
		custom_cost_text = command_power_more_than_20
		custom_cost_trigger = { command_power > 20 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_army_training"
			add_command_power = -20
			custom_effect_tooltip = border_war_increase_readiness_TT
			add_equipment_to_stockpile = { type = infantry_weapons_type amount = -30 }
			add_equipment_to_stockpile = { type = cnc_equipment_type amount = -7 }
			hidden_effect = {
				set_temp_variable = { border_war_add_readiness_amount = 12 }
				border_war_add_readiness = yes
				random = {
					chance = 10
					country_event = { id = border_war_training_failure.1 days = 1 }
				}
			}
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 check_variable = { border_readiness > 80 } }
			modifier = { factor = 0 NOT = { border_war_ai_should_prepare = yes } }
		}
	}

	prepare_mech_army_training = {
		desc = prepare_mech_army_training_desc
		available = {
			NOT = { has_war_with = RAJ }
			NOT = { has_war_with = PAK }
			has_equipment = { medium_tank_amphibious_chassis > 5 }
			has_equipment = { medium_tank_chassis > 3 }
		}
		days_remove = 15
		days_re_enable = 20
		fixed_random_seed = no
		custom_cost_text = command_power_more_than_20
		custom_cost_trigger = { command_power > 20 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_mech_army_training"
			add_command_power = -20
			custom_effect_tooltip = border_war_increase_readiness_TT
			add_equipment_to_stockpile = { type = medium_tank_amphibious_chassis amount = -5 }
			add_equipment_to_stockpile = { type = medium_tank_chassis amount = -3 }
			hidden_effect = {
				set_temp_variable = { border_war_add_readiness_amount = 20 }
				border_war_add_readiness = yes
				random = {
					chance = 10
					country_event = { id = border_war_training_failure.2 days = 1 }
				}
			}
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 check_variable = { border_readiness > 80 } }
			modifier = { factor = 0 NOT = { border_war_ai_should_prepare = yes } }
		}
	}

	prepare_arty_army_training = {
		desc = prepare_arty_army_training_desc
		available = {
			NOT = { has_war_with = RAJ }
			NOT = { has_war_with = PAK }
			has_equipment = { artillery_equipment > 5 }
		}
		days_remove = 15
		days_re_enable = 20
		fixed_random_seed = no
		custom_cost_text = command_power_more_than_20
		custom_cost_trigger = { command_power > 20 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_arty_army_training"
			add_command_power = -20
			custom_effect_tooltip = border_war_increase_readiness_TT
			add_equipment_to_stockpile = { type = artillery_equipment amount = -5 }
			hidden_effect = {
				set_temp_variable = { border_war_add_readiness_amount = 10 }
				border_war_add_readiness = yes
			}
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 check_variable = { border_readiness > 80 } }
			modifier = { factor = 0 NOT = { border_war_ai_should_prepare = yes } }
		}
	}

	# --- TENSION ---

	prepare_attack = {
		desc = prepare_attack_desc
		available = {
			NOT = { has_war_with = RAJ }
			NOT = { has_war_with = PAK }
			NOT = { has_decision = prepare_shelling }
			NOT = { has_decision = prepare_shelling_china }
		}
		days_remove = 15
		days_re_enable = 20
		fixed_random_seed = no
		custom_cost_text = command_power_more_than_20
		custom_cost_trigger = { command_power > 20 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_attack"
			add_command_power = -20
			custom_effect_tooltip = border_war_increase_tension_TT
			hidden_effect = {
				set_temp_variable = { border_war_add_tension_amount = 18 }
				border_war_add_tension = yes
				random = {
					chance = 10
					country_event = { id = border_war_tension_failure.1 days = 1 }
				}
			}
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 check_variable = { global.border_tension > 80 } }
			modifier = { factor = 0 NOT = { border_war_ai_should_prepare = yes } }
		}
	}

	prepare_shelling = {
		desc = prepare_shelling_desc
		available = {
			NOT = { has_war_with = RAJ }
			NOT = { has_war_with = PAK }
			NOT = { has_decision = prepare_attack }
			NOT = { has_decision = prepare_attack_china }
		}
		days_remove = 15
		days_re_enable = 20
		fixed_random_seed = no
		custom_cost_text = command_power_more_than_20
		custom_cost_trigger = { command_power > 20 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision prepare_shelling"
			add_command_power = -20
			custom_effect_tooltip = border_war_increase_tension_TT
			hidden_effect = {
				set_temp_variable = { border_war_add_tension_amount = 18 }
				border_war_add_tension = yes
				random = {
					chance = 10
					country_event = { id = border_war_tension_failure.1 days = 1 }
				}
			}
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 check_variable = { global.border_tension > 80 } }
			modifier = { factor = 0 NOT = { border_war_ai_should_prepare = yes } }
		}
	}

	# --- NEGOTIATE (de-escalation, content expansion) ---

	border_negotiate_pak_raj = {
		desc = border_negotiate_pak_raj_desc
		icon = GFX_decision_generic_political_discourse
		visible = {
			is_border_war_eligible = yes
			check_variable = { global.border_tension > 19 }
		}
		available = {
			has_war = no
			custom_trigger_tooltip = {
				tooltip = border_war_negotiate_opinion_TT
				RAJ = { has_opinion = { target = PAK value > 24 } }
				PAK = { has_opinion = { target = RAJ value > 24 } }
			}
		}
		cost = 75
		fire_only_once = no
		days_re_enable = 90

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision border_negotiate_pak_raj"
			custom_effect_tooltip = border_war_negotiate_tension_TT
			hidden_effect = {
				subtract_from_variable = { global.border_tension = 25 }
				clamp_variable = { var = global.border_tension min = 0 max = 100 }
			}
			if = {
				limit = {
					has_global_flag = border_war_crisis_resolved_peacefully
					NOT = { has_global_flag = PAK_RAJ_border_regulated }
					check_variable = { global.border_tension < 21 }
					RAJ = { has_opinion = { target = PAK value > 49 } }
					PAK = { has_opinion = { target = RAJ value > 49 } }
				}
				custom_effect_tooltip = border_war_settlement_hint_TT
				set_global_flag = PAK_RAJ_border_regulated
			}
		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 5
				has_war = no
				has_stability < 0.50
			}
		}
	}

	# --- CRISIS ---

	border_war_general_mobilization = {
		desc = border_war_general_mobilization_desc
		visible = {
			has_global_flag = border_war_crisis_PAK_RAJ
			has_country_flag = border_war_crisis_escalator
		}
		available = {
			NOT = { has_idea = border_war_mobilization_idea }
		}
		cost = 100
		fire_only_once = no
		days_re_enable = 180

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision border_war_general_mobilization"
			add_timed_idea = {
				idea = border_war_mobilization_idea
				days = 180
			}
			custom_effect_tooltip = border_war_mobilization_unlocks_TT
			hidden_effect = {
				set_temp_variable = { border_war_add_tension_amount = 10 }
				border_war_add_tension = yes
			}
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 has_idea = border_war_mobilization_idea }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
		}
	}

	border_war_issue_ultimatum = {
		desc = border_war_issue_ultimatum_desc
		visible = {
			has_global_flag = border_war_crisis_PAK_RAJ
			has_country_flag = border_war_crisis_escalator
		}
		available = {
			NOT = { has_country_flag = border_war_ultimatum_pending }
			custom_trigger_tooltip = {
				tooltip = border_war_mobilization_unlocks_TT
				has_idea = border_war_mobilization_idea
			}
			custom_trigger_tooltip = {
				tooltip = border_war_tension_90_TT
				check_variable = { global.border_tension > 89 }
			}
		}
		cost = 50
		fire_only_once = no
		days_re_enable = 60

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision border_war_issue_ultimatum"
			set_country_flag = { flag = border_war_ultimatum_pending value = 1 days = 60 }
			if = {
				limit = { original_tag = RAJ }
				PAK = { country_event = { id = border_war_crisis.3 days = 1 } }
			}
			else_if = {
				limit = { original_tag = PAK }
				RAJ = { country_event = { id = border_war_crisis.3 days = 1 } }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				subtract_from_variable = { global.border_tension = 40 }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
		}

		ai_will_do = {
			base = 3
			modifier = { factor = 0 NOT = { has_idea = border_war_mobilization_idea } }
			modifier = { factor = 0.1 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 0.25 has_global_flag = border_war_nuclear_standoff }
			modifier = { factor = 0 original_tag = RAJ strength_ratio = { tag = PAK ratio < 1.2 } }
			modifier = { factor = 0 original_tag = PAK strength_ratio = { tag = RAJ ratio < 1.2 } }
		}
	}

	border_war_stand_down = {
		desc = border_war_stand_down_desc
		icon = GFX_decision_generic_political_discourse
		visible = {
			has_global_flag = border_war_crisis_PAK_RAJ
		}
		available = {
			has_war = no
		}
		cost = 50
		fire_only_once = no
		days_re_enable = 30

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision border_war_stand_down"
			add_stability = 0.05
			add_war_support = -0.05
			custom_effect_tooltip = border_war_stand_down_TT
			hidden_effect = {
				subtract_from_variable = { global.border_tension = 30 }
				clamp_variable = { var = global.border_tension min = 0 max = 100 }
				border_war_crisis_resolve_pak_raj = yes
				set_global_flag = border_war_crisis_resolved_peacefully
				if = {
					limit = { original_tag = RAJ }
					PAK = { country_event = { id = border_war_crisis.9 days = 1 } }
				}
				else_if = {
					limit = { original_tag = PAK }
					RAJ = { country_event = { id = border_war_crisis.9 days = 1 } }
				}
			}
		}

		ai_will_do = {
			base = 2
			modifier = { factor = 5 has_global_flag = border_war_nuclear_standoff is_historical_focus_on = yes }
			modifier = { factor = 3 has_stability < 0.40 }
			modifier = { factor = 0.2 has_global_flag = border_war_ai_aggressive }
		}
	}

	# --- ATTACKS ---

	attack_427_from_420 = {
		desc = attack_427_from_420_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 427 } }

		visible = {
			controls_state = 420
			NOT = { controls_state = 427 }
			427 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_427_from_420"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_427_from_420"
			if = {
				limit = {
					OR = {
						420 = { has_border_war = yes }
						427 = { has_border_war = yes }
					}
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 420
					NOT = { controls_state = 427 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 420
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
					defender = {
						state = 427
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	attack_420_from_427 = {
		desc = attack_420_from_427_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 420 } }

		visible = {
			controls_state = 427
			NOT = { controls_state = 420 }
			420 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_420_from_427"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_420_from_427"
			if = {
				limit = {
					OR = {
						427 = { has_border_war = yes }
						420 = { has_border_war = yes }
					}
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 427
					NOT = { controls_state = 420 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 427
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
					defender = {
						state = 420
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	attack_429_from_563 = {
		desc = attack_429_from_563_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 429 } }

		visible = {
			controls_state = 563
			NOT = { controls_state = 429 }
			429 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_429_from_563"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_429_from_563"
			if = {
				limit = {
					OR = {
						563 = { has_border_war = yes }
						429 = { has_border_war = yes }
					}
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 563
					NOT = { controls_state = 429 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 563
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
					defender = {
						state = 429
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	attack_563_from_429 = {
		desc = attack_563_from_429_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 563 } }

		visible = {
			controls_state = 429
			NOT = { controls_state = 563 }
			563 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_563_from_429"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_563_from_429"
			if = {
				limit = {
					OR = {
						429 = { has_border_war = yes }
						563 = { has_border_war = yes }
					}
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 429
					NOT = { controls_state = 563 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 429
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
					defender = {
						state = 563
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	attack_573_from_984 = {
		desc = attack_573_from_984_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 573 } }

		visible = {
			controls_state = 984
			NOT = { controls_state = 573 }
			573 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_573_from_984"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_573_from_984"
			if = {
				limit = {
					OR = {
						984 = { has_border_war = yes }
						573 = { has_border_war = yes }
					}
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 984
					NOT = { controls_state = 573 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 984
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
					defender = {
						state = 573
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	attack_984_from_573 = {
		desc = attack_984_from_573_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 984 } }

		visible = {
			controls_state = 573
			NOT = { controls_state = 984 }
			984 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_984_from_573"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_984_from_573"
			if = {
				limit = {
					OR = {
						573 = { has_border_war = yes }
						984 = { has_border_war = yes }
					}
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 573
					NOT = { controls_state = 984 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 573
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
					defender = {
						state = 984
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	attack_426_from_984 = {
		desc = attack_426_from_984_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 426 } }

		visible = {
			controls_state = 984
			NOT = { controls_state = 426 }
			426 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_426_from_984"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_426_from_984"
			if = {
				limit = {
					OR = {
						984 = { has_border_war = yes }
						426 = { has_border_war = yes }
					}
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 984
					NOT = { controls_state = 426 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 984
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
					defender = {
						state = 426
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	attack_984_from_426 = {
		desc = attack_984_from_426_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 984 } }

		visible = {
			controls_state = 426
			NOT = { controls_state = 984 }
			984 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_984_from_426"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_984_from_426"
			if = {
				limit = {
					OR = {
						426 = { has_border_war = yes }
						984 = { has_border_war = yes }
					}
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 426
					NOT = { controls_state = 984 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 426
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
					defender = {
						state = 984
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	attack_426_from_987 = {
		desc = attack_426_from_987_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 426 } }

		visible = {
			controls_state = 987
			NOT = { controls_state = 426 }
			426 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_426_from_987"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_426_from_987"
			if = {
				limit = {
					OR = {
						987 = { has_border_war = yes }
						426 = { has_border_war = yes }
					}
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 987
					NOT = { controls_state = 426 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 987
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
					defender = {
						state = 426
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	attack_987_from_426 = {
		desc = attack_987_from_426_desc
		days_remove = 10
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 987 } }

		visible = {
			controls_state = 426
			NOT = { controls_state = 987 }
			987 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision attack_987_from_426"
			border_war_set_attacking_pak_raj = yes
		}

		available = { border_war_attack_available = yes }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove attack_987_from_426"
			if = {
				limit = {
					OR = {
						426 = { has_border_war = yes }
						987 = { has_border_war = yes }
					}
				}
				country_event = border_war_attack_blocked.1
				border_war_clear_attacking_pak_raj = yes
			}
			else_if = {
				limit = {
					controls_state = 426
					NOT = { controls_state = 987 }
				}
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 426
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
					defender = {
						state = 987
						num_provinces = 3
						on_win = border_war_outcome.1
						on_lose = border_war_outcome.2
						on_cancel = border_war_outcome.3
					}
				}
			}
			else = { border_war_clear_attacking_pak_raj = yes }
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	# --- GRABS ---

	grab_420 = {
		desc = grab_420_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 420 } }

		visible = {
			NOT = { controls_state = 420 }
			420 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_420"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_420"
			set_temp_variable = { border_war_grab_state_id = 420 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	grab_427 = {
		desc = grab_427_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 427 } }

		visible = {
			NOT = { controls_state = 427 }
			427 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_427"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_427"
			set_temp_variable = { border_war_grab_state_id = 427 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	grab_429 = {
		desc = grab_429_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 429 } }

		visible = {
			NOT = { controls_state = 429 }
			429 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_429"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_429"
			set_temp_variable = { border_war_grab_state_id = 429 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	grab_563 = {
		desc = grab_563_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 563 } }

		visible = {
			NOT = { controls_state = 563 }
			563 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_563"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_563"
			set_temp_variable = { border_war_grab_state_id = 563 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	grab_573 = {
		desc = grab_573_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 573 } }

		visible = {
			NOT = { controls_state = 573 }
			573 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_573"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_573"
			set_temp_variable = { border_war_grab_state_id = 573 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	grab_426 = {
		desc = grab_426_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 426 } }

		visible = {
			NOT = { controls_state = 426 }
			426 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_426"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_426"
			set_temp_variable = { border_war_grab_state_id = 426 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	grab_987 = {
		desc = grab_987_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 987 } }

		visible = {
			NOT = { controls_state = 987 }
			987 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_987"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_987"
			set_temp_variable = { border_war_grab_state_id = 987 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	grab_984 = {
		desc = grab_984_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		highlight_states = { highlight_states_trigger = { state = 984 } }

		visible = {
			NOT = { controls_state = 984 }
			984 = { OR = { is_controlled_by = RAJ is_controlled_by = PAK } }
		}

		available = { border_war_grab_available = yes }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision grab_984"
			set_temp_variable = { border_war_subtract_progress_amount = 80 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove grab_984"
			set_temp_variable = { border_war_grab_state_id = 984 }
			border_war_grab_state = yes
		}

		ai_will_do = {
			base = 10
			modifier = { factor = 0 NOT = { border_war_ai_should_attack = yes } }
			modifier = { factor = 0.3 is_historical_focus_on = yes NOT = { has_global_flag = border_war_ai_aggressive } }
			modifier = { factor = 2 has_global_flag = border_war_ai_aggressive }
		}
	}

	# --- EXTORT XP ---

	extort_xp = {
		desc = extort_xp_desc
		days_remove = 30
		days_re_enable = 100
		fire_only_once = no

		available = {
			has_country_flag = border_war_recently_active
			custom_trigger_tooltip = {
				tooltip = border_war_progress_50_TT
				check_variable = { border_war_progress > 49 }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision extort_xp"
			set_temp_variable = { border_war_subtract_progress_amount = 50 }
			border_war_subtract_progress = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Remove decision extort_xp"
			army_experience = 30
		}

		ai_will_do = {
			base = 5
			modifier = { factor = 0 NOT = { has_country_flag = border_war_recently_active } }
		}
	}
}
