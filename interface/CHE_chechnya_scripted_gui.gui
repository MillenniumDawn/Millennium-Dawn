guiTypes = {
	containerWindowType = {
		name = "show_supply_yield_meter"
		position = { x = 50%% y = 5%% }
		size = { width = 640 height = 140 }
		clipping = no
		moveable = no

		# ── Title ─────────────────────────────────────────────────────
		instantTextboxType = {
			name = "supply_yield_title_text"
			text = "SUPPLY_YIELD_METER_TITLE"
			position = { x = 50%% y = 0 }
			size = { width = 600 height = 26 }
			format = centre
			font = "hoi_20b"
		}

		instantTextboxType = {
			name = "supply_yield_subtitle_text"
			text = "SUPPLY_YIELD_METER_SUBTITLE"
			position = { x = 50%% y = 26 }
			size = { width = 600 height = 18 }
			format = centre
			font = "hoi_20b"
		}

		# ── The bar. Property name "supply_yield_bar" must match the
		# scripted_gui properties block exactly. ──────────────────────
		iconType = {
			name = "supply_yield_bar"
			spriteType = "GFX_ichkeria_supply_yield_bar"
			position = { x = 150 y = 10 }
		}

		# ── Plain numeric readout, no drift line. ───────────────────
		instantTextboxType = {
			name = "supply_yield_pct_text"
			text = "SUPPLY_YIELD_PCT_READOUT"
			position = { x = 50%% y = 88 }
			size = { width = 300 height = 18 }
			format = centre
			font = "hoi_20b"
		}
	}
}
