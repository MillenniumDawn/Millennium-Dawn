# International Investments System
# Building types: 1=CIC, 2=MIC, 4=Dockyard, 3=Infra, 5=Offices, 6=SAM, 7=Radar,
#                 8=Airbase, 9=Fuel, 10=Internet, 11=Renewable, 12=Fossil, 13=Nuclear, 14=Agri

# Initialize global building cost arrays at game start.
# Called once in on_startup - do not trigger more than once.
init_investment_system = {
	# Building cost arrays (for array lookup - index = building type 1..14)
	set_variable = { global.build_cost_array^0 = 12 }     # CIC
	set_variable = { global.build_cost_array^1 = 12.50 }  # MIC
	set_variable = { global.build_cost_array^2 = 12 }     # NIC
	set_variable = { global.build_cost_array^3 = 5.5 }    # INFRA
	set_variable = { global.build_cost_array^4 = 20 }     # OFFICES
	set_variable = { global.build_cost_array^5 = 5.50 }   # AA
	set_variable = { global.build_cost_array^6 = 4.50 }   # RADAR
	set_variable = { global.build_cost_array^7 = 5.25 }   # AIRBASE
	set_variable = { global.build_cost_array^8 = 5.25 }   # FUEL_SILO
	set_variable = { global.build_cost_array^9 = 4.65 }   # INTERNET
	set_variable = { global.build_cost_array^10 = 13.60 } # REF
	set_variable = { global.build_cost_array^11 = 5.2 }   # FOSSIL
	set_variable = { global.build_cost_array^12 = 14.4 }  # NUCLEAR
	set_variable = { global.build_cost_array^13 = 5.8 }   # AGRICULTURE

	# Building const cost arrays (for construction speed calculation)
	set_variable = { global.build_const_cost_array^0 = 47500 }  # CIC
	set_variable = { global.build_const_cost_array^1 = 52500 }  # MIC
	set_variable = { global.build_const_cost_array^2 = 47500 }  # NIC
	set_variable = { global.build_const_cost_array^3 = 17000 }  # INFRA
	set_variable = { global.build_const_cost_array^4 = 50000 }  # OFFICES
	set_variable = { global.build_const_cost_array^5 = 15000 }  # AA
	set_variable = { global.build_const_cost_array^6 = 8000 }   # RADAR
	set_variable = { global.build_const_cost_array^7 = 5000 }   # AIRBASE
	set_variable = { global.build_const_cost_array^8 = 18000 }  # FUEL_SILO
	set_variable = { global.build_const_cost_array^9 = 10000 }  # INTERNET
	set_variable = { global.build_const_cost_array^10 = 50000 } # REF
	set_variable = { global.build_const_cost_array^11 = 16500 } # FOSSIL
	set_variable = { global.build_const_cost_array^12 = 62500 } # NUCLEAR
	set_variable = { global.build_const_cost_array^13 = 22500 } # AGRICULTURE

    # Building return procentage ( 0.0 - 1.0 )
	set_variable = { global.build_const_return_array^0 = 1 }  # CIC
	set_variable = { global.build_const_return_array^1 = 1 }  # MIC
	set_variable = { global.build_const_return_array^2 = 1 }  # NIC
	set_variable = { global.build_const_return_array^3 = 1 }  # INFRA
	set_variable = { global.build_const_return_array^4 = 1 }  # OFFICES
	set_variable = { global.build_const_return_array^5 = 1 }  # AA
	set_variable = { global.build_const_return_array^6 = 1 }  # RADAR
	set_variable = { global.build_const_return_array^7 = 1 }  # AIRBASE
	set_variable = { global.build_const_return_array^8 = 1 }  # FUEL_SILO
	set_variable = { global.build_const_return_array^9 = 1 }  # INTERNET
	set_variable = { global.build_const_return_array^10 = 1 } # REF
	set_variable = { global.build_const_return_array^11 = 1 } # FOSSIL
	set_variable = { global.build_const_return_array^12 = 1 } # NUCLEAR
	set_variable = { global.build_const_return_array^13 = 1 } # AGRICULTURE

    # Building icon frame
	set_variable = { global.build_const_icon_array^0 = 1 }   # CIC
	set_variable = { global.build_const_icon_array^1 = 2 }   # MIC
	set_variable = { global.build_const_icon_array^2 = 4 }   # NIC
	set_variable = { global.build_const_icon_array^3 = 3 }   # INFRA
	set_variable = { global.build_const_icon_array^4 = 35 }  # OFFICES
	set_variable = { global.build_const_icon_array^5 = 9 }   # AA
	set_variable = { global.build_const_icon_array^6 = 11 }  # RADAR
	set_variable = { global.build_const_icon_array^7 = 5 }   # AIRBASE
	set_variable = { global.build_const_icon_array^8 = 14 }  # FUEL_SILO
	set_variable = { global.build_const_icon_array^9 = 32 }  # INTERNET
	set_variable = { global.build_const_icon_array^10 = 10 } # REF
	set_variable = { global.build_const_icon_array^11 = 34 } # FOSSIL
	set_variable = { global.build_const_icon_array^12 = 27 } # NUCLEAR
	set_variable = { global.build_const_icon_array^13 = 33 } # AGRICULTURE
}

# Called in the spawning of a new nation in-game
# Purely used to update the variables of a nation if you are trying to play as a releasable
ingame_init_investment_system = {
    update_investments_max_projects = yes
	# set_variable = { MD_investments_max_projects_base = 15 }
}

create_investment_project = {
    set_temp_variable = { added = 0 }

    for_each_loop = {
        array = ROOT.investments_projects_state
        index = i
        value = v

        if = {
            limit = {
                check_variable = { v = THIS }
                check_variable = { ROOT.investments_projects_building^i = building_index }
            }
            
            add_to_variable = { ROOT.investments_projects_building_amount^i = amount }

            set_temp_variable = { added = 1 }

            calculate_investment_project_cost = yes
			ROOT = {
				set_temp_variable = { treasury_change = investment_project_cost }
				multiply_temp_variable = { treasury_change = amount }
				multiply_temp_variable = { treasury_change = -1 }
				modify_treasury_effect = yes
			}
        }
    }

    if = {
        limit = {
            check_variable = { added = 0 }
        }

        add_to_array = { ROOT.investments_projects = investments_projects^num }
        add_to_array = { ROOT.investments_projects_state = THIS }

        add_to_array = { ROOT.investments_projects_building = building_index }
        add_to_array = { ROOT.investments_projects_building_amount = amount }

        calculate_investment_project_time = yes
        add_to_array = { ROOT.investments_projects_building_time = project_time }
        add_to_array = { ROOT.investments_projects_building_time_max = project_time }

        calculate_investment_project_cost = yes
        add_to_array = { ROOT.investments_projects_building_cost = investment_project_cost }

		ROOT = {
			set_temp_variable = { treasury_change = investment_project_cost }
			multiply_temp_variable = { treasury_change = amount }
			multiply_temp_variable = { treasury_change = -1 }
			modify_treasury_effect = yes
		}
    }

	set_variable = { ROOT.MD_active_investments_projects = ROOT.investments_projects^num }
	clamp_variable = { var = ROOT.MD_active_investments_projects min = 0 max = ROOT.MD_investments_max_projects }

	ROOT = {
		update_dirty_investments = yes
		update_dirty_investments_mapicon = yes
	}

	custom_effect_tooltip = create_investment_project_tt
}

remove_investment_project = {
    for_each_loop = {
        array = ROOT.investments_projects_state
        index = i
        value = v
        break = break

        if = {
            limit = {
                check_variable = { v = THIS }
                check_variable = { ROOT.investments_projects_building^i = building_index }
            }
            
            set_temp_variable = { index = i }
            ROOT = { remove_investment_project_by_index = yes }
            set_temp_variable = { break = 1 }
        }
    }
}

remove_investment_project_by_index = {
	subtract_from_variable = { investments_projects_building_amount^index = amount }

	if = {
		limit = { check_variable = { investments_projects_building_time^index > 0 } }

		set_temp_variable = { remove_return_procentage = investments_projects_building_time^index }
		divide_temp_variable = { remove_return_procentage = investments_projects_building_time_max^index }
	
		set_temp_variable = { treasury_change = investments_projects_building_cost^index }
		multiply_temp_variable = { treasury_change = remove_return_procentage }
		multiply_temp_variable = { treasury_change = amount }
		modify_treasury_effect = yes
	}

	calculate_investment_project_cost = yes
	set_variable = { investments_projects_building_cost^index = investment_project_cost }
	
	calculate_investment_project_time = yes
	set_variable = { investments_projects_building_time^index = project_time }
	set_variable = { investments_projects_building_time_max^index = project_time }

	if = {
		limit = {
			check_variable = { investments_projects_building_amount^index < 1 }
		}

		remove_from_array = { 
			array = investments_projects
			value = index
		}

		for_each_loop = {
			array = investments_projects
			index = i
			value = pointer

			if = {
				limit = { check_variable = { pointer > index } }

				subtract_from_variable = { investments_projects^i = 1 }
			}
		}

		remove_from_array = { 
			array = investments_projects_building
			index = index
		}
		remove_from_array = { 
			array = investments_projects_building_amount
			index = index
		}
		remove_from_array = { 
			array = investments_projects_building_cost
			index = index
		}

		remove_from_array = { 
			array = investments_projects_building_time
			index = index
		}
		remove_from_array = { 
			array = investments_projects_building_time_max
			index = index
		}

		remove_from_array = { 
			array = investments_projects_state
			index = index
		}
	}

	set_variable = { MD_active_investments_projects = investments_projects^num }
	clamp_variable = { var = MD_active_investments_projects min = 0 max = MD_investments_max_projects }

	update_dirty_investments = yes
	update_dirty_investments_mapicon = yes

	custom_effect_tooltip = remove_investment_project_tt
}

# TODO: to implement new system in future?
add_investment = {
    set_temp_variable = { added = 0 }

    for_each_loop = {
        array = foreign_investments_countries
        index = i
        value = v

        if = {
            limit = {
                check_variable = { v = country }
            }
            
            add_to_variable = { foreign_investments^i = amount }

            set_temp_variable = { added = 1 }
        }
    }

    if = {
        limit = {
            check_variable = { added = 0 }
        }

        add_to_array = { foreign_investments_countries = country }
        add_to_array = { foreign_investments = amount }
    }

    add_to_variable = { int_foreign_investments = amount }

	var:country = {
		update_dirty_investments = yes
	}

	var:country_from = {
        add_to_variable = { int_investments = amount }
		update_dirty_investments = yes
    }

	custom_effect_tooltip = add_investment_tt
}

update_dirty_investments = {
	if = {
		limit = {
			check_variable = { MD_constructionview_tab_selected = 1 }
		}

		if = { limit = { check_variable = { THIS.investments_dirty < 100000 } }
			add_to_variable = { THIS.investments_dirty = 1 }
		}
		else = {
			set_variable = { THIS.investments_dirty = 0 }
		}
	}
}

update_dirty_investments_mapicon = {
	if = { limit = { check_variable = { THIS.investments_mapicon_dirty < 100000 } }
		add_to_variable = { THIS.investments_mapicon_dirty = 1 }
	}
	else = {
		set_variable = { THIS.investments_mapicon_dirty = 0 }
	}
}

# Add completed building to state (called by complete_project).
add_finished_building = {
	# Buildings to Add
	if = { limit = { check_variable = { building_index = 1 } }
		add_building_construction = { type = industrial_complex level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 2 } }
		add_building_construction = { type = arms_factory level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 3 } }
		add_building_construction = { type = dockyard level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 4 } }
		add_building_construction = { type = infrastructure level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 5 } }
		add_building_construction = { type = offices level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 6 } }
		add_building_construction = { type = anti_air_building level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 7 } }
		add_building_construction = { type = radar_station level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 8 } }
		add_building_construction = { type = air_base level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 9 } }
		add_building_construction = { type = fuel_silo level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 10 } }
		add_building_construction = { type = internet_station level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 11 } }
		add_building_construction = { type = renewable_energy_infra level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 12 } }
		add_building_construction = { type = fossil_powerplant level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 13 } }
		add_building_construction = { type = nuclear_reactor level = 1 instant_build = yes }
	}
	else_if = { limit = { check_variable = { building_index = 14 } }
		add_building_construction = { type = agriculture_district level = 1 instant_build = yes }
	}

	add_to_variable = { ROOT.project_monetary_cost^ROOT.project = var_cost }
}

# Get construction speed for project_type (1..14). Cost from array; modifiers type-specific.
get_building_construction_values = {
	set_temp_variable = { construction_speed = 1 }
	# Building cost from array (project_type = 1..14 indexes build_const_cost_array)
	set_temp_variable = { building_cost = global.build_const_cost_array^project_type }

	# Type-specific construction speed modifiers (each building type has unique modifier)
	if = { limit = { check_variable = { project_type = 1 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_industrial_complex_factor }
	}
	else_if = { limit = { check_variable = { project_type = 2 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_arms_factory_factor }
	}
	else_if = { limit = { check_variable = { project_type = 3 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_dockyard_factor }
	}
	else_if = { limit = { check_variable = { project_type = 4 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_infrastructure_factor }
	}
	else_if = { limit = { check_variable = { project_type = 5 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_offices_factor }
	}
	else_if = { limit = { check_variable = { project_type = 6 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_anti_air_building_factor }
	}
	else_if = { limit = { check_variable = { project_type = 7 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_radar_station_factor }
	}
	else_if = { limit = { check_variable = { project_type = 8 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_air_base_factor }
	}
	else_if = { limit = { check_variable = { project_type = 9 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_fuel_silo_factor }
	}
	else_if = { limit = { check_variable = { project_type = 10 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_internet_station_factor }
	}
	else_if = { limit = { check_variable = { project_type = 11 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_renewable_energy_infra_factor }
	}
	else_if = { limit = { check_variable = { project_type = 12 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_fossil_powerplant_factor }
	}
	else_if = { limit = { check_variable = { project_type = 13 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_nuclear_reactor_factor }
	}
	else_if = { limit = { check_variable = { project_type = 14 } }
		add_to_temp_variable = { var = construction_speed value = modifier@production_speed_agriculture_district_factor }
	}

	add_to_temp_variable = { var = construction_speed value = -0.40 }
	add_to_temp_variable = { var = construction_speed value = modifier@production_speed_buildings_factor }
	clamp_temp_variable = { var = construction_speed min = 0 }
}

# Precompute state-level construction data. Must be called in STATE scope with
# PREV = the investing country. Writes temp variables: local_state_modifier and
# receiving_investment_duration_temp, consumed by calculate_project_duration.
state_construction_data = {
	# State-local construction speed bonus (infra + internet + state-flag effects)
	set_temp_variable = { local_state_modifier = 0 }

	set_temp_variable = { tmp_speed = building_level@infrastructure }
	multiply_temp_variable = { tmp_speed = 0.15 }
	add_to_temp_variable = { local_state_modifier = tmp_speed }

	set_temp_variable = { tmp_speed = building_level@internet_station }
	multiply_temp_variable = { tmp_speed = 0.05 }
	add_to_temp_variable = { local_state_modifier = tmp_speed }

	if = { limit = { has_state_flag = currently_encouraged_investments }
		add_to_temp_variable = { local_state_modifier = 0.15 }
	}
	if = { limit = { has_state_flag = HOL_autonomy_economic_zone }
		add_to_temp_variable = { local_state_modifier = 0.15 }
	}

	# Net duration modifier from target controller.
	set_temp_variable = { receiving_investment_duration_temp = 0 }
	CONTROLLER = {
		if = {
			limit = {
				PREV.PREV = {
					OR = {
						has_idea = full_isr_boycotte
						has_idea = econ_isr_boycotte
					}
				}
			}
			add_to_temp_variable = { receiving_investment_duration_temp = 1 }
		}
		if = {
			limit = {
				is_in_array = { global.EU_member = THIS.id }
				PREV.PREV = { has_idea = ECB_president }
			}
			add_to_temp_variable = { receiving_investment_duration_temp = -0.15 }
		}
		if = {
			limit = {
				is_in_array = { global.EU_member = THIS.id }
				PREV.PREV = { has_idea = EU_finance_minister }
			}
			add_to_temp_variable = { receiving_investment_duration_temp = -0.05 }
		}
		if = {
			limit = {
				has_idea = FRA_idea_development_investment
				PREV.PREV = { original_tag = FRA }
			}
			add_to_temp_variable = { receiving_investment_duration_temp = -0.05 }
		}
		if = {
			limit = {
				PREV.PREV = {
					has_country_flag = mutual_investment_treaty_@PREV
				}
			}
			add_to_temp_variable = { receiving_investment_duration_temp = -0.05 }
		}
		add_to_temp_variable = { receiving_investment_duration_temp = THIS.modifier@receiving_investment_duration_modifier }
	}
}

# Calculate duration for a single investment project entry (new array-based system).
# Expects local vars: state, building_index, amount (set by create_investment_project).
# Reads: local_state_modifier, receiving_investment_duration_temp (precompute_state_construction_data).
# Writes: temp_variable project_time (consumed by create_investment_project).
calculate_investment_project_time = {
	set_temp_variable = { project_type = building_index }
	get_building_construction_values = yes

	state_construction_data = yes
	add_to_temp_variable = { construction_speed = local_state_modifier }
	add_to_temp_variable = { receiving_investment_duration_temp = modifier@investment_duration_modifier }
	clamp_temp_variable = { var = receiving_investment_duration_temp min = -1 }
	if = { limit = { check_variable = { receiving_investment_duration_temp > 0 } }
		add_to_temp_variable = { construction_speed = receiving_investment_duration_temp }
	}
	else = {
		subtract_from_temp_variable = { construction_speed = receiving_investment_duration_temp }
	}

	set_variable = { displayable_construction_modifier = construction_speed }

	# Assume 5 × 15 Civilian Industry devoted to a single construction line
	multiply_temp_variable = { construction_speed = 75 }
	if = { limit = { check_variable = { construction_speed > 0 } }
		divide_temp_variable = { building_cost = construction_speed }
	}
	else = {
		# construction_speed <= 0 -> clamp to 1 day to avoid instant completion
		set_temp_variable = { building_cost = 1 }
	}

	set_temp_variable = { project_time = building_cost }
	round_temp_variable = project_time
}
# Calculate treasury cost for a single investment project entry (new array-based system).
# Expects local vars: state, building_index, amount (set by create_investment_project).
# Scope: PREV = state, ROOT = investing country (unchanged from old system).
# Writes/returns: temp_variable investment_project_cost (the monetary cost).
calculate_investment_project_cost = {
	set_temp_variable = { investment_project_cost = global.build_cost_array^building_index }

	# AC Build Cost Modifiers
	set_temp_variable = { investments_cost_modifier = 1 }
	PREV = {
		CONTROLLER = {
			add_to_temp_variable = { investments_cost_modifier = THIS.modifier@receiving_investment_cost_modifier }
			if = {
				limit = {
					ROOT = {
						OR = {
							has_idea = full_isr_boycotte
							has_idea = econ_isr_boycotte
						}
					}
				}
				add_to_temp_variable = { investments_cost_modifier = 0.5 }
			}
			if = {
				limit = {
					is_in_array = { global.EU_member = THIS.id }
					PREV.PREV = { has_idea = ECB_president }
				}
				add_to_temp_variable = { investments_cost_modifier = -0.15 }
			}
			if = {
				limit = {
					is_in_array = { global.EU_member = THIS.id }
					PREV.PREV = { has_idea = EU_finance_minister }
				}
				add_to_temp_variable = { investments_cost_modifier = -0.05 }
			}
			if = {
				limit = {
					has_idea = FRA_idea_development_investment
					PREV.PREV = { original_tag = FRA }
				}
				add_to_temp_variable = { investments_cost_modifier = -0.05 }
			}
			if = {
				limit = {
					PREV.PREV = {
						has_country_flag = mutual_investment_treaty_@ROOT
					}
				}
				add_to_temp_variable = { investments_cost_modifier = -0.05 }
			}
		}
	}
	add_to_temp_variable = { investments_cost_modifier = ROOT.modifier@investment_cost_modifier }

	# Calculates modified Build Cost
	set_variable = { displayable_monetary_cost_modifier = investments_cost_modifier }
	multiply_temp_variable = { var = investment_project_cost value = investments_cost_modifier }
	multiply_temp_variable = { var = investment_project_cost value = amount }
}

update_investments_max_projects = {
	set_temp_variable = { modifier = 1 }
	add_to_temp_variable = { modifier = modifier@max_investment_projects_modifier }

	set_variable = { MD_investments_max_projects = modifier@max_investment_projects_base_factor }
	multiply_variable = { MD_investments_max_projects = modifier }
	round_variable = MD_investments_max_projects
}

investments_on_daily = {
	update_investments_max_projects = yes
            
	for_loop_effect = {
		start = 1
		end = MD_investments_max_projects
		value = v

		if = {
			limit = {
				check_variable = { v < investments_projects^num }
			}

			subtract_from_variable = { investments_projects_building_time^v = 1 }

			if = {
				limit = {
					check_variable = { investments_projects_building_time^v < 0 }
				}

				set_temp_variable = { index = v }
				var:investments_projects_state^v = {
					set_temp_variable = { building_index = investments_projects_building^v }
					add_finished_building = yes

					owner = {
						set_temp_variable = { country = ROOT }
						set_temp_variable = { country_from = FROM }
						set_temp_variable = { amount = investments_projects_building_cost^v }
						multiply_temp_variable = { amount = global.build_const_return_array^vnvestments_projects_building^v }
						add_investment = yes
					}
				}

				set_temp_variable = { amount = 1 }
				remove_investment_project_by_index = yes
			}
			else = {
				update_dirty_investments = yes
			}
		}
	}
}