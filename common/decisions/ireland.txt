IRE_debug_decisions = { # Name of category which decision is assigned to 
	IRE_execute_chosen_path = { # ID of decision
		icon = GFX_decision_icon_default			
		available = {
			is_debug = yes
		}
		fire_only_once = no
		visible = {
			is_debug = yes
		}
		cost = 1
		complete_effect = { # Effects on this decision
			every_country = {
				limit = {
					has_country_flag = IRE_support_fine_gaell
				}
				add_political_power = 100
			}
		}
		ai_will_do = {
			factor = 0
		}
		
	}
}

IRE_debug_decisions = {
	IRE_fine_gael_path = {
		icon = GFX_decision_icon_default
		available = {
			is_debug = yes
		}
		fire_only_once = no
		visible = {
			is_debug = yes
		}
		cost = 1
		complete_effect = {
			set_country_flag = IRE_support_fine_gaell
		}
		ai_will_do = {
			factor = 0
		}
	}
}


# Testing array-way-of-method storing the party indexes and affecting them, instead of checking for flag
# Lookup more of loops 
IRE_debug_decisions = { 
	IRE_array_testing_stores_parties_in_array = {
		icon = GFX_decision_icon_default
		available = {
			is_debug = yes
		}
		fire_only_once = no
		visible = {
			is_debug = yes
		}
		cost = 1
		complete_effect = {
			custom_effect_tooltip = "Testing array-way-of-method storing the party indexes and affecting them, instead of checking for flag"
			add_to_array = {
				array = IRE.IRE_party_support # IRE. is scope of array, only for Ireland
				value = 2
				index = 0
			}
			add_to_array = {
				array = IRE.IRE_party_support
				value = 23
				index = 1
			}
			# add popularity to OUTLOOK too
		}
	}
}

IRE_debug_decisions = { 
	IRE_boosting_parties_in_array = {
		icon = GFX_decision_icon_default
		available = {
			is_debug = yes
		}
		fire_only_once = no
		visible = {
			is_debug = yes
		}
		cost = 1
		complete_effect = {
			custom_effect_tooltip = "Give all the given subideologies stored in the array a boosted party popularity"
			for_each_loop = {
				array = IRE.IRE_party_support
				value = v
				index = i
				
				set_temp_variable = { party_index = v }
				set_temp_variable = { party_popularity_increase = 0.10 }	
				set_temp_variable = { temp_outlook_increase = 0.10 } -- OPTIONAL 
				add_relative_party_popularity = yes
			}
		}
	}
}

