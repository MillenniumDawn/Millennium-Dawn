name: Docs Quality (Astro)

on:
  pull_request:
    branches: [main, master]
    paths:
      - "docs/**"
      - "tools/docs_checks/**"
      - ".github/workflows/docs-quality.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  docs-quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 1
          sparse-checkout: |
            docs
            tools/docs_checks
          sparse-checkout-cone-mode: true

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: "24"

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: "1.3.14"

      - name: Setup Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install docs dependencies
        working-directory: docs
        run: bun install

      # Source checks run before the build so malformed content fails fast.
      - name: Link-syntax checker self-test
        run: python3 tools/docs_checks/check_link_syntax.py --self-test

      - name: Markdown link syntax
        run: python3 tools/docs_checks/check_link_syntax.py

      - name: Raw HTML and duplicate titles
        run: python3 tools/docs_checks/check_content_html.py

      - name: Country flag images
        run: python3 tools/docs_checks/check_flag_images.py

      - name: Astro checks
        working-directory: docs
        run: |
          bun run lint:md
          bun run check
          bun run build

      - name: Validate Open Graph metadata images
        run: python3 tools/docs_checks/check_og_images.py --site-dir docs/dist --baseurl /Millennium-Dawn

      - name: Docs hygiene checks
        run: python3 tools/docs_checks/check_docs_hygiene.py --repo-root . --docs-dir docs

      - name: Validate internal links and local assets
        run: python3 tools/docs_checks/check_site_links.py --site-dir docs/dist --baseurl /Millennium-Dawn

      - name: Accessibility checker self-test
        run: python3 tools/docs_checks/check_accessibility_basics.py --self-test

      - name: Accessibility baseline checks
        run: python3 tools/docs_checks/check_accessibility_basics.py --site-dir docs/dist

      - name: Performance budget checks
        run: python3 tools/docs_checks/check_perf_budgets.py --site-dir docs/dist
