post_balkan_war_categories = {

	get_active_in_balkans = {

		visible = {
			country_exists = KOS
			OR = {
				original_tag = ENG
				original_tag = ITA
				original_tag = GER
				original_tag = HOL
				original_tag = IRE
				original_tag = SPR
				original_tag = ROM
				original_tag = SWE
				original_tag = USA
				original_tag = TUR
				original_tag = DEN
				original_tag = CAN
			}
			NOT = {
				has_country_flag = balkan_activity
			}
		}

		cost = 250
		icon = political_actions

		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = {
				state = 113
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			set_country_flag = balkan_activity
		}

		ai_will_do = {
			factor = 100
		}
	}

	retreat_from_balkans = {

		visible = {
			country_exists = KOS
			has_country_flag = balkan_activity	
		}

		available = {
			check_variable = { balkan_Forces_status = 1000 }
		}

		cost = 250
		icon = political_actions

		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = {
				state = 113
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			clr_country_flag = balkan_activity
			clr_country_flag = balkan_major
			clear_variable = balkan_Forces_status
			add_manpower = 1000
		}

		ai_will_do = {
			factor = 100
		}
	}

	increase_deployment = {

		visible = {
			country_exists = KOS
			has_country_flag = balkan_activity
			has_country_flag = balkan_major
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = KFOR_units_TT
				KOS = {
					NOT = {
						check_variable = { balkan_Forces_status > 44999 }
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = PER_presidential_decisions_only_one_is_available_TT
				NOT = {
					#has_decision = increase_deployment
					has_decision = decrease_deployment
					has_decision = guarantee_country
					has_decision = western_influence
					has_decision = increase_deployment_small
					has_decision = decrease_deployment_small
					has_decision = welcome_refugees
					has_decision = start_rebuild
				}
			}
		}

		days_remove = 120

		days_re_enable = 250
		icon = political_actions

		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = {
				state = 113
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			ROOT = {
				set_temp_variable = { modify_balkan_Forces_status = 1000 }
				balkan_Forces_status = yes
				add_manpower = -1000
			}
			hidden_effect = {
				KOS = {
					set_temp_variable = { modify_balkan_Forces_status = 1000 }
					balkan_Forces_status = yes
				}
			}
			
		}

		ai_will_do = {
			factor = 100
		}
	}

	decrease_deployment = {

		visible = {
			country_exists = KOS
			has_country_flag = balkan_activity
			has_country_flag = balkan_major
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = KFOR_units2_TT
				ROOT = {
					check_variable = { balkan_Forces_status > 999 }
				}
			}
			custom_trigger_tooltip = {
				tooltip = PER_presidential_decisions_only_one_is_available_TT
				NOT = {
					has_decision = increase_deployment
					#has_decision = decrease_deployment
					has_decision = guarantee_country
					has_decision = western_influence
					has_decision = increase_deployment_small
					has_decision = decrease_deployment_small
					has_decision = welcome_refugees
					has_decision = start_rebuild
				}
			}
		}

		days_remove = 120

		days_re_enable = 250
		icon = political_actions

		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = {
				state = 113
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			ROOT = {
				set_temp_variable = { modify_balkan_Forces_status = -1000 }
				balkan_Forces_status = yes
				add_manpower = 1000
			}
			hidden_effect = {
				KOS = {
					set_temp_variable = { modify_balkan_Forces_status = -1000 }
					balkan_Forces_status = yes
				}
			}
			
		}

		ai_will_do = {
			factor = 100
		}
	}

	guarantee_country = {

		visible = {
			country_exists = KOS
			any_country = {
				has_country_flag = balkan_activity
				has_country_flag = balkan_major
			}
		}

		available = {
			any_other_country = {
				OR = {
					is_justifying_wargoal_against = KOS
					has_wargoal_against = KOS
					has_war_with = KOS
				}
			}
			custom_trigger_tooltip = {
				tooltip = PER_presidential_decisions_only_one_is_available_TT
				NOT = {
					has_decision = increase_deployment
					has_decision = decrease_deployment
					#has_decision = guarantee_country
					has_decision = western_influence
					has_decision = increase_deployment_small
					has_decision = decrease_deployment_small
					has_decision = welcome_refugees
					has_decision = start_rebuild
				}
			}
		}

		days_remove = 120

		days_re_enable = 250
		icon = political_actions

		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = {
				state = 113
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			give_guarantee = KOS
		}

		ai_will_do = {
			factor = 100
		}
	}
	
	western_influence = {

		visible = {
			country_exists = KOS
			has_country_flag = balkan_activity
			has_country_flag = balkan_major
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = PER_presidential_decisions_only_one_is_available_TT
				NOT = {
					has_decision = increase_deployment
					has_decision = decrease_deployment
					has_decision = guarantee_country
					#has_decision = western_influence
					has_decision = increase_deployment_small
					has_decision = decrease_deployment_small
					has_decision = welcome_refugees
					has_decision = start_rebuild
				}
			}
		}

		days_remove = 120

		days_re_enable = 250
		icon = political_actions

		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = {
				state = 113
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			KOS = {
				set_temp_variable = { percent_change = 2 }
				change_domestic_influence_percentage = yes
				if = {
					limit = {
						check_variable = {
						KOS_albania > 0.2
						}
					}
					custom_effect_tooltip = KOS_clamp_unity
					add_to_variable = { var = KOS_albania value = -0.02 }
				}
				if = {
					limit = {
						check_variable = {
						KOS_serbs > 0.2
						}
					}
					custom_effect_tooltip = KOS_serb_opinion
					add_to_variable = { var = KOS_serbs value = -0.02 }
				}
			}
			ROOT = {
				set_temp_variable = { percent_change = 3 }
				set_temp_variable = { tag_index = ROOT }
				set_temp_variable = { influence_target = KOS }
				change_influence_percentage = yes
			}
		}

		ai_will_do = {
			factor = 100
		}
	}
	
	increase_deployment_small = {

		visible = {
			country_exists = KOS
			has_country_flag = balkan_activity
			NOT = {
				has_country_flag = balkan_major
			}
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = KFOR_units3_TT
				KOS = {
					NOT = {
						check_variable = { balkan_Forces_status > 44999 }
					}
				}
				ROOT = {
					check_variable = { balkan_Forces_status < 1500 }
				}
			}
			custom_trigger_tooltip = {
				tooltip = PER_presidential_decisions_only_one_is_available_TT
				NOT = {
					has_decision = increase_deployment
					has_decision = decrease_deployment
					has_decision = guarantee_country
					has_decision = western_influence
					#has_decision = increase_deployment_small
					has_decision = decrease_deployment_small
					has_decision = welcome_refugees
					has_decision = start_rebuild
				}
			}
		}

		days_remove = 120

		days_re_enable = 250
		icon = political_actions

		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = {
				state = 113
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			ROOT = {
				set_temp_variable = { modify_balkan_Forces_status = 500 }
				balkan_Forces_status = yes
				add_manpower = -500
				if = {
					Limit = {
						check_variable = { balkan_Forces_status = 500 }
					}
					set_country_flag = balkan_activity_1
					clr_country_flag = balkan_activity_2
					clr_country_flag = balkan_activity_3
				}
				if = {
					Limit = {
						check_variable = { balkan_Forces_status = 1000 }
					}
					clr_country_flag = balkan_activity_1
					set_country_flag = balkan_activity_2
					clr_country_flag = balkan_activity_3
				}
				if = {
					Limit = {
						check_variable = { balkan_Forces_status = 1500 }
					}
					clr_country_flag = balkan_activity_1
					clr_country_flag = balkan_activity_2
					set_country_flag = balkan_activity_3
				}
			}
			hidden_effect = {
				KOS = {
					set_temp_variable = { modify_balkan_Forces_status = 500 }
					balkan_Forces_status = yes
				}
			}
			
		}

		ai_will_do = {
			factor = 100
		}
	}

	decrease_deployment_small = {

		visible = {
			country_exists = KOS
			has_country_flag = balkan_activity
			NOT = {
				has_country_flag = balkan_major
			}
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = KFOR_units2_TT
				ROOT = {
					check_variable = { balkan_Forces_status > 1500 }
				}
			}
			custom_trigger_tooltip = {
				tooltip = PER_presidential_decisions_only_one_is_available_TT
				NOT = {
					has_decision = increase_deployment
					has_decision = decrease_deployment
					has_decision = guarantee_country
					has_decision = western_influence
					has_decision = increase_deployment_small
					#has_decision = decrease_deployment_small
					has_decision = welcome_refugees
					has_decision = start_rebuild
				}
			}
		}

		days_remove = 120

		days_re_enable = 250
		icon = political_actions

		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = {
				state = 113
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			ROOT = {
				set_temp_variable = { modify_balkan_Forces_status = -500 }
				balkan_Forces_status = yes
				add_manpower = 500
			}
			hidden_effect = {
				KOS = {
					set_temp_variable = { modify_balkan_Forces_status = -500 }
					balkan_Forces_status = yes
				}
			}
			
		}

		ai_will_do = {
			factor = 100
		}
	}

	welcome_refugees = {

		visible = {
			country_exists = KOS
			has_country_flag = balkan_activity	
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = PER_presidential_decisions_only_one_is_available_TT
				NOT = {
					has_decision = increase_deployment
					has_decision = decrease_deployment
					has_decision = guarantee_country
					has_decision = western_influence
					has_decision = increase_deployment_small
					has_decision = decrease_deployment_small
					#has_decision = welcome_refugees
					has_decision = start_rebuild
				}
			}
		}

		days_remove = 120

		days_re_enable = 250
		icon = political_actions

		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = {
				state = 113
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			random_owned_state = {
				add_manpower = 1000
			}
		}

		ai_will_do = {
			factor = 100
		}
	}

	start_rebuild = {

		visible = {
			country_exists = KOS
			has_country_flag = balkan_activity	
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = PER_presidential_decisions_only_one_is_available_TT
				NOT = {
					has_decision = increase_deployment
					has_decision = decrease_deployment
					has_decision = guarantee_country
					has_decision = western_influence
					has_decision = increase_deployment_small
					has_decision = decrease_deployment_small
					has_decision = welcome_refugees
					#has_decision = start_rebuild
				}
			}
		}

		days_remove = 120

		days_re_enable = 250
		icon = political_actions

		fire_only_once = yes

		highlight_states = {
			highlight_state_targets = {
				state = 113
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision infiltrate_hezbollah"
			random_list = {
				20 = {

				}
				20 = {
					
				}
				20 = {
					
				}
				20 = {
					
				}
				20 = {
					
				}
			}
		}

		ai_will_do = {
			factor = 100
		}
	}

}