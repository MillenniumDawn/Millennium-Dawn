# Scripted AI desire for peace actions.
# See documentation.info file for more detailed scripting information.

peace_ai_desires = {
	# MAIN AI
		GENERIC_wants_its_cores = {
			peace_action_type = { take_states }
			enable = {
				tag = ROOT
				FROM = { tag = ROOT.FROM }
				ROOT.FROM.FROM.FROM = { is_core_of = ROOT.FROM }
			}
			ai_desire = 100
		}
		GENERIC_does_not_want_cores_of_other = {
			peace_action_type = { take_states }
			enable = {
				tag = ROOT
				FROM = { tag = ROOT.FROM }
				ROOT.FROM.FROM.FROM = { NOT = { is_core_of = ROOT.FROM } }
			}
			ai_desire = -120
		}
		GENERIC_wants_to_make_puppets = {
			peace_action_type = { puppet }
			enable = {
				tag = ROOT
				ROOT.FROM.FROM.FROM = { is_core_of = FROM }
			}
			ai_desire = 130
		}
# OTHER SHIT
	# # COUNTRY SPECIFIC AI
	# 	# SOV - RUSSIA
	# 		# RUSSIA PUPPETS CHECHENS
	# 			SOV_wants_to_puppet_chechnya = {
	# 				peace_action_type = puppet
	# 				enable = {
	# 					NOT = { is_historical_focus_on = yes }
	# 					ROOT.FROM = { tag = CHE }
	# 					ROOT.FROM.FROM.FROM = { is_core_of = CHE }
	# 					tag = SOV
	# 				}
	# 				ai_desire = 100 # 100 percent higher desire
	# 			}
	# 		# RUSSIA ANNEXES CHECHENS
	# 			SOV_wants_to_annex_chechnya = {
	# 				peace_action_type = take_states
	# 				enable = {
	# 					is_historical_focus_on = yes
	# 					tag = SOV # Negotiatior is soviet union
	# 					FROM = { tag = ROOT } # And the negotiator is also taking for themselves
	# 					FROM.FROM.FROM = { state = 672 } # CHECHNYA
	# 				}
	# 				ai_desire = 150 # 150 percent higher desire
	# 			}
	# 	# DRC - CONGO DR
	# 		DRC_cleans_up_the_congo = {
	# 			peace_action_type = take_states # TELLS DRC TO TAKE STATES IN PEACE DEAL
	# 			enable = {
	# 				tag = DRC # SETS THE NEGOTIATOR AI TO TRIGGER FOR DRC TAG
	# 				FROM = { tag = ROOT } # DRC IS TAKING FOR THEMSELVES
	# 				FROM.FROM.FROM = { is_core_of = DRC } # DRC WILL ATTEMPT TO TAKE ALL STATES THAT ARE CORES OF ITSELF
	# 				}
	# 			ai_desire = 150
	# 		}
	# 		# DRC - RWANDA/UGANDA
	# 			DRC_stop_yoinking_rwanda = {
	# 				peace_action_type = { take_states puppet liberate } # TELLS DRC NOT TO FUCK WITH RWANDA TOO MUCH
	# 				enable = {
	# 					FROM.FROM.FROM = { is_core_of = RWA } # SETS AI NEGOTIATING TO TARGET CORES OF RWANDA
	# 					FROM = { original_tag = RWA } # SETS LOSER TARGETING TO RWANDA
	# 					ROOT = {
	# 						original_tag = DRC
	# 						NOT = { has_government = nationalist }
	# 					} # SETS NEGOTIATOR AI TO DRC WHILST NOT NATIONALIST
	# 					AND = {
	# 						country_exists = MLC
	# 						country_exists = RCD
	# 					} # NEGOTIATOR AI ONLY APPLIES IF CONGOLESE REBEL FACTIONS EXIST
	# 					}
	# 				ai_desire = -150
	# 			}
	# 			DRC_stop_yoinking_uganda = {
	# 				peace_action_type = { take_states puppet liberate } # TELLS DRC NOT TO FUCK WITH UGANDA TOO MUCH
	# 				enable = {
	# 					FROM.FROM.FROM = { is_core_of = UGA } # SETS AI NEGOTIATING TO TARGET CORES OF UGANDA
	# 					FROM = { original_tag = UGA } # SETS LOSER TARGETING TO UGANDA
	# 					ROOT = {
	# 						original_tag = DRC
	# 						NOT = { has_government = nationalist }
	# 					} # SETS NEGOTIATOR AI TO DRC WHILST NOT NATIONALIST
	# 					AND = {
	# 						country_exists = MLC
	# 						country_exists = RCD
	# 					} # NEGOTIATOR AI ONLY APPLIES IF CONGOLESE REBEL FACTIONS EXIST
	# 					}
	# 				ai_desire = -150
	# 			}
	# 			DRC_yeets_kagame = {
	# 				peace_action_type = force_government # TELLS AI TO INSTALL A CHANGE OF GOVERNMENT
	# 				enable = {
	# 					ROOT = {
	# 						has_government = communism
	# 						original_tag = DRC
	# 					} # ONLY TRIGGERS IF THE NEGOTIATOR IS ONE OF THESE OPTIONS
	# 					ROOT.FROM = { tag = DRC } # SETS NEGOTIATOR TAG AS THE DRC
	# 					FROM.FROM = { tag = RWA } # SETS THE TAG OF THE NATION GETTING CIA'D AS RWA
	# 					country_exists = MLC
	# 					country_exists = RCD
	# 				}
	# 				ai_desire = 150
	# 			}
	# 			DRC_yeets_museveni = {
	# 				peace_action_type = force_government # TELLS AI TO INSTALL A CHANGE OF GOVERNMENT
	# 				enable = {
	# 					ROOT = {
	# 						has_government = communism
	# 						original_tag = DRC
	# 					} # ONLY TRIGGERS IF THE NEGOTIATOR IS ONE OF THESE OPTIONS
	# 					ROOT.FROM = { tag = DRC } # SETS NEGOTIATOR TAG AS THE DRC
	# 					FROM.FROM = { tag = UGA } # SETS THE TAG OF THE NATION GETTING CIA'D AS UGA
	# 					country_exists = MLC
	# 					country_exists = RCD
	# 				}
	# 				ai_desire = 150
	# 			}
	# 			DRC_puppets_museveni = {
	# 				peace_action_type = puppet # TELLS AI TO PUPPET
	# 				enable = {
	# 					ROOT.FROM = { tag = UGA } # SETS THE PUPPET TARGET AS UGA
	# 					ROOT.FROM.FROM.FROM = { is_core_of = UGA } # TELLS THE AI TO ATTEMPT TO CREATE NICE BORDERS FOR ITS PUPPET
	# 					tag = DRC # SETS AI BEHAVIOUR FOR NEGOTIATOR, IN THIS CASE, DRC
	# 				}
	# 				ai_desire = 150
	# 			}
	# 			DRC_puppets_kagame = {
	# 				peace_action_type = puppet # TELLS AI TO PUPPET
	# 				enable = {
	# 					ROOT.FROM = { tag = RWA } # SETS THE PUPPET TARGET AS UGA
	# 					ROOT.FROM.FROM.FROM = {
	# 						all_state = {
	# 							is_core_of = RWA
	# 						}
	# 					} # TELLS THE AI TO ATTEMPT TO CREATE NICE BORDERS FOR ITS PUPPET
	# 					tag = DRC # SETS AI BEHAVIOUR FOR NEGOTIATOR, IN THIS CASE, DRC
	# 				}
	# 				ai_desire = 150
	# 			}
	#
}