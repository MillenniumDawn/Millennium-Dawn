# Author(s): Bird
focus_tree = {
	id = RSK_focus
	country = {
		base = 0
		modifier = {
			add = 20
			original_tag = RSK
		}
	}

	continuous_focus_position = { x = 100 y = 1000 }

	# Entity Under Arms
	focus = {
		id = RSK_entity_under_arms
		icon = bos_apc_rush

		x = 3
		y = 0

		cost = 2

		search_filters = { FOCUS_FILTER_WAR_SUPPORT FOCUS_FILTER_MILITARY_LAWS }

		available = {
			has_war = yes
		}

		completion_reward = {
			log = "[GetDateText]: [Root.GetName]: Focus RSK_entity_under_arms"
			add_war_support = 0.10
			add_timed_idea = {
				idea = RSK_entity_under_arms
				days = 180
			}
		}

		ai_will_do = {
			base = 15
			modifier = {
				factor = 0
				has_war = no
			}
		}
	}

	# Serbian Volunteers
	focus = {
		id = RSK_serbian_volunteers
		icon = ultranationalist_volunteers

		x = -2
		y = 1
		relative_position_id = RSK_entity_under_arms

		cost = 3

		prerequisite = { focus = RSK_entity_under_arms }

		search_filters = { FOCUS_FILTER_MANPOWER FOCUS_FILTER_FOREIGN_POLICY }

		available = {
			has_war = yes
		}

		completion_reward = {
			log = "[GetDateText]: [Root.GetName]: Focus RSK_serbian_volunteers"
			add_timed_idea = {
				idea = RSK_serbian_volunteers
				days = 180
			}
			if = {
				limit = {
					country_exists = SER
					NOT = { has_war_with = SER }
				}
				SER = { country_event = { id = BOS_events.68 days = 2 } }
				custom_effect_tooltip = TT_IF_THEY_ACCEPT
				effect_tooltip = { custom_effect_tooltip = RSK_serbian_volunteers_accept_tt }
			}
		}

		ai_will_do = {
			base = 15
			modifier = {
				factor = 0
				has_war = no
			}
		}
	}

	# The Drina Supply Line
	focus = {
		id = RSK_drina_supply_line
		icon = lend_lease

		x = 2
		y = 1
		relative_position_id = RSK_entity_under_arms

		cost = 3

		prerequisite = { focus = RSK_entity_under_arms }

		search_filters = { FOCUS_FILTER_ARMY FOCUS_FILTER_MILITARY_LAWS }

		available = {
			has_war = yes
		}

		completion_reward = {
			log = "[GetDateText]: [Root.GetName]: Focus RSK_drina_supply_line"
			add_timed_idea = {
				idea = RSK_drina_supply_line
				days = 180
			}
		}

		ai_will_do = {
			base = 15
			modifier = {
				factor = 0
				has_war = no
			}
		}
	}

	# Mobilize the Entity
	focus = {
		id = RSK_mobilize_the_entity
		icon = BOS_tank_rush

		x = 0
		y = 2
		relative_position_id = RSK_entity_under_arms

		cost = 3

		prerequisite = { focus = RSK_serbian_volunteers }
		prerequisite = { focus = RSK_drina_supply_line }

		search_filters = { FOCUS_FILTER_MANPOWER FOCUS_FILTER_WAR_SUPPORT }

		available = {
			has_war = yes
		}

		completion_reward = {
			log = "[GetDateText]: [Root.GetName]: Focus RSK_mobilize_the_entity"
			add_timed_idea = {
				idea = RSK_mobilize_the_entity
				days = 180
			}
			if = {
				limit = { NOT = { has_template = "Milicija Divizija" } }
				division_template = {
					name = "Milicija Divizija"
					division_names_group = BOS_MIL
					is_locked = yes
					regiments = {
						Militia_Bat = { x = 0 y = 0 }
					}
				}
			}
			if = {
				limit = { controls_state = 129 }
				129 = {
					create_unit = {
						division = "name = \"Entitetska milicija\" division_template = \"Milicija Divizija\" start_experience_factor = 0.5"
						owner = RSK
						count = 2
					}
				}
			}
			else = {
				capital_scope = {
					create_unit = {
						division = "name = \"Entitetska milicija\" division_template = \"Milicija Divizija\" start_experience_factor = 0.5"
						owner = RSK
						count = 2
					}
				}
			}
		}

		ai_will_do = {
			base = 15
			modifier = {
				factor = 0
				has_war = no
			}
		}
	}

	# Banja Luka High Command
	focus = {
		id = RSK_banja_luka_high_command
		icon = BOS_junta

		x = 0
		y = 1
		relative_position_id = RSK_mobilize_the_entity

		cost = 3

		prerequisite = { focus = RSK_mobilize_the_entity }

		search_filters = { FOCUS_FILTER_MILITARY_LAWS FOCUS_FILTER_ARMY }

		available = {
			has_war = yes
		}

		completion_reward = {
			log = "[GetDateText]: [Root.GetName]: Focus RSK_banja_luka_high_command"
			army_experience = 15
			add_timed_idea = {
				idea = RSK_banja_luka_high_command
				days = 180
			}
		}

		ai_will_do = {
			base = 15
			modifier = {
				factor = 0
				has_war = no
			}
		}
	}

	# Hold the Line
	focus = {
		id = RSK_hold_the_line
		icon = border_conflict_bunker

		x = -1
		y = 1
		relative_position_id = RSK_banja_luka_high_command

		cost = 3

		prerequisite = { focus = RSK_banja_luka_high_command }
		mutually_exclusive = { focus = RSK_push_to_sarajevo }

		search_filters = { FOCUS_FILTER_ARMY FOCUS_FILTER_WAR_SUPPORT }

		available = {
			has_war = yes
		}

		completion_reward = {
			log = "[GetDateText]: [Root.GetName]: Focus RSK_hold_the_line"
			add_timed_idea = {
				idea = RSK_hold_the_line
				days = 180
			}
		}

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = no
			}
		}
	}

	# Push to Sarajevo
	focus = {
		id = RSK_push_to_sarajevo
		icon = military_fort

		x = 1
		y = 1
		relative_position_id = RSK_banja_luka_high_command

		cost = 3

		prerequisite = { focus = RSK_banja_luka_high_command }
		mutually_exclusive = { focus = RSK_hold_the_line }

		search_filters = { FOCUS_FILTER_ARMY FOCUS_FILTER_WAR_SUPPORT }

		available = {
			has_war = yes
		}

		completion_reward = {
			log = "[GetDateText]: [Root.GetName]: Focus RSK_push_to_sarajevo"
			add_timed_idea = {
				idea = RSK_push_to_sarajevo
				days = 180
			}
		}

		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = no
			}
		}
	}
}
